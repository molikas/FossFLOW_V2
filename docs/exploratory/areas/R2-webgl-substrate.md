# R2 — WebGL sprite-batch substrate (atlas, shaders, context loss)

**Status:** DONE · **Counted hypotheses:** 13 / 10 · **Bugs:** 4 · **Hypothesis ID prefix:** `GL-`

**Scope:** createSpriteBatch is the shared WebGL2 instanced renderer: one mipmapped shelf-packed atlas (content-keyed putCanvas/putImage, reserved dot+white texels, half-texel UV inset via atlasUVRect), 20-float per-instance staging, and a vertex shader that maps tile-space anchors + local basis vectors through u_view=(zoom*dpr, originDev) — pan/zoom is one uniform write + one drawArraysInstanced. shapeMode 1/2 does analytic edge-AA (fwidth distance-field) for lines/discs; premultiplied-alpha pipeline throughout. itemRaster rasterises node/label chips via Canvas2D at CHIP_SUPERSAMPLE*min(dpr,2). lineStyle walks dash/dot patterns by integer index (float-cursor stall fix) and builds AA-feathered stroke quads. contextLoss preventDefaults webglcontextlost so restore can fire; each layer rebuilds its batch on restore. isWebGL2Supported is the memoised one-shot gate for the whole substrate.

**Code:**
- `packages/axoview-lib/src/webgl/glSpriteBatch.ts`
- `packages/axoview-lib/src/webgl/itemRaster.ts`
- `packages/axoview-lib/src/webgl/lineStyle.ts`
- `packages/axoview-lib/src/webgl/contextLoss.ts`
- `packages/axoview-lib/src/webgl/__tests__/atlasUV.test.ts`
- `docs/adr/0038-webgl-instanced-render-substrate.md`

**Dedupe baseline:** [coverage-baseline.md](../coverage-baseline.md) sections — E2E: *Canvas projection modes / rendering / viewport*; Unit: *Lib WebGL rendering*. Then grep the suites directly.

## Seed seams (2026-07-29 mapping — starting points, not a ceiling)

- atlasFull semantics: an overflowing chip is silently SKIPPED for the current build (packSlot returns null) and the atlas only compacts on the NEXT beginInstances — if nothing dirties geometry afterwards, the missing chips persist on screen indefinitely; nothing schedules a follow-up rebuild when atlasFull is set.
- ConnectorsCanvas captures arrowUV/ringUV ONCE at effect setup (outside buildInstances); resetAtlas clears uvCache and resumes the shelf cursor just past dot+white — after any compaction of the 512 atlas the arrow/ring texels are overwritten while the captured UVRects still point at them (they are re-packed only on context RESTORE, not on compaction).
- isWebGL2Supported is memoised forever and is strictly weaker than createSpriteBatch (no shader compile / atlas alloc) — a browser that passes the probe but fails batch creation gets a console.warn and a permanently blank layer with no user-visible signal.
- Context budget: each Renderer opens 4 WebGL2 contexts and image-export mounts a second Renderer (8 live) against the browser's ~16-context cap; eviction force-loses the OLDEST context — recovery depends entirely on the webglcontextrestored path, and if createSpriteBatch returns null inside onRestored the layer goes blank with no retry loop.
- putImage caches by key (icon URL) with no version — an icon whose content changes under the same URL keeps the stale atlas texture until context loss or eviction of the whole batch.
- Premultiplied invariants: UNPACK_PREMULTIPLY_ALPHA_WEBGL + shader tint premultiply + ONE/ONE_MINUS_SRC_ALPHA must all agree; any new upload path or blend change reintroduces the grey-fringe artifact the comments document.
- walkDots/walkDashes cap at MAX_SPANS_PER_SEGMENT=20000 — a pathologically small dash period silently truncates the pattern mid-segment; the kStart epsilons (1e-6) can double-emit or skip a dot exactly at a vertex arc-length.
- itemRaster's single module-level scratch canvas is shared by NodesCanvas AND LabelsCanvas — safe only while putCanvas uploads synchronously; any future async/deferred upload corrupts one layer's chip with another's pixels.
- (mapper note) Everything here is pixel-blind to CI (jsdom has no WebGL2); the unit tests cover only atlasUVRect and lineStyle math. Bugs in this area manifest as visual artifacts only on real GPUs.

## Related harvested invariants (keyword-matched hint list — scan the [full table](../coverage-baseline.md#invariants) too)

- **(ADR 0023 Consequences + addendum D)** A connector anchored to an offset/unsnapped node must resolve to the RENDERED (offset) endpoint on both the WebGL bulk path and the DOM selected path. → *Select a connector attached to an off-grid node: the sparse DOM <Connector> (selection halo) and the WebGL ConnectorsCanvas body must agree on the endpoint. If one resolves via bare tile and the other via rendered position, selecting/deselecting makes the wire visibly jump at the node. The invariant suite asserts DOM/WebGL equality for rectangle vertices, not connector endpoints at offset nodes.*
- **(ADR 0044 §4 (iconScale resolution order))** Every render reader resolves viewItem.iconScale ?? icon.scale ?? 1 — DOM icon paths, WebGL NodesCanvas, selection-ring extent, and image export must agree. → *A reader outside the four audited ones — the hover outline box, NodeLabelHitLayer's chip stalk anchor, or the ADR 0012 popover's side-anchor offset — computes extent from icon.scale only: a per-node-resized icon shows a ring/label/popover anchored to the wrong extent. Unit tests cover the mode math and schema round-trip; a missed reader is visual-only and CI is pixel-blind (§11).*
- **(ADR 0031 §2 + ADR 0038 fold)** A floating Label renders ABOVE nodes (cross-layer z-order was the reason Labels became a first-class entity). → *The z-order is now encoded in the mount order of sibling WebGL canvases in Renderer.tsx — a refactor that reorders canvas mounting (or an overlay inserted between them, like CanvasCompositorOverlay tweaks) silently puts labels back under nodes. Tests assert model zIndex fields; nothing asserts the paint stacking of LabelsCanvas over NodesCanvas.*

## Known coverage gaps (from the baseline inventory)

- (Canvas projection modes / rendering / viewport) Mouse-wheel zoom anchoring at the cursor position
- (Canvas projection modes / rendering / viewport) Zoom clamp bounds via buttons/wheel (only touch pinch asserts [0.1,1])
- (Canvas projection modes / rendering / viewport) Fit-to-view exact framing (all items visible) — only 'value changed' asserted
- (Canvas projection modes / rendering / viewport) Projection mode persistence across reload
- (Canvas projection modes / rendering / viewport) 2D label stalk rendering parity
- (Lib WebGL rendering) packages/axoview-lib/src/webgl/glSpriteBatch.ts — zero tests (batching/draw-call assembly)
- (Lib WebGL rendering) packages/axoview-lib/src/webgl/itemRaster.ts — zero tests (icon rasterization to atlas)
- (Lib WebGL rendering) packages/axoview-lib/src/webgl/contextLoss.ts — zero tests (context-loss recovery)

## Hypotheses

| ID | Hypothesis | Source | Nearest existing tests | Probe | Verdict | Evidence |
|----|-----------|--------|------------------------|-------|---------|----------|
| GL-01 | `putImage` caches by key with **no version check** (unlike `putCanvas`, which compares `hit.version === version`), so once an icon URL is packed its texels are frozen: replacing a custom icon's content under the same key keeps the stale atlas texture for the life of the context | sibling drift (putCanvas vs putImage) | *none* — `glSpriteBatch.ts` has zero tests; `atlasUV.test.ts` covers only the pure inset math | src/__explore__/R2/atlas-gl-01-02-03-04-05.explore.test.ts | FALSIFIED | the sibling drift is real — `putCanvas` compares `hit.version === version` and `putImage` returns any cache hit unconditionally — but there is no reachable symptom. `NodesCanvas.putIcon` keys by the icon `url`, and a custom icon's URL is a data URI derived from its own bytes, so new artwork is a new key. The uploaded size is also derived from the image's own natural dimensions, so the same URL cannot arrive at two sizes. Recorded as a latent trap for any future caller that keys by a stable icon id. |
| GL-02 | When the atlas fills, `packSlot` returns null and the chip is skipped for that build, and **nothing schedules a follow-up rebuild** — the compaction only happens on the NEXT `beginInstances`, so if no geometry change follows, the missing chips persist on screen indefinitely | seed seam (atlasFull semantics) | *none* (see GL-01) | src/__explore__/R2/atlas-gl-01-02-03-04-05.explore.test.ts | BUG | confirmed on the real packer. An overflowing chip returns null and uploads nothing; the `SpriteBatch` surface exposes no flag, callback or counter for it (asserted against the exact key list), so the caller cannot know a chip is missing and cannot schedule the rebuild that would compact the atlas. Compaction happens only inside the NEXT `beginInstances`, which only a geometry change triggers. Reached in practice by GL-05. known_issues: *The chip atlas has no eviction* |
| GL-03 | A caller that holds a `UVRect` across a compaction (ConnectorsCanvas captures `__arrow__`/`__ring__` once at effect setup) keeps pointing at texels the compaction hands to other chips — the arrow renders as some other chip's pixels | seed seam (captured UV vs resetAtlas) | *none* (see GL-01) | src/__explore__/R2/atlas-gl-01-02-03-04-05.explore.test.ts | FALSIFIED | the shape is real and proven (after a compaction a different chip is uploaded onto the exact texels a previously returned `UVRect` still names, while the reserved dot/white region does survive), but it is unreachable today: `ConnectorsCanvas` and `RectanglesCanvas` are the only capture-once callers and both build a 512 atlas into which they pack at most two chips (`__arrow__`, `__ring__`) at effect setup — `atlasFull` can never be set on those batches. Latent trap for any future per-frame packing on those layers. |
| GL-04 | A chip wider or taller than the atlas is rejected by `packSlot`'s first guard **without setting `atlasFull`**, so no compaction is ever triggered and that chip is invisible forever — and the caller cannot tell it apart from a transient overflow | boundary | *none* (see GL-01) | src/__explore__/R2/atlas-gl-01-02-03-04-05.explore.test.ts | FALSIFIED | the code defect is exactly as predicted — `packSlot`'s `w + GUTTER > ATLAS` guard returns null WITHOUT setting `atlasFull`, so no compaction is ever armed, and the caller sees the same `null` as a recoverable overflow (both asserted). But no caller can produce an oversized chip: `measureNodeLabel` clamps `chipW` to `LABEL_CHIP_MAX_W`, node names are capped at 100 characters by the schema, and `itemRaster.getScratch` clamps both axes to at least 1. |
| GL-05 | Re-rasterising a chip under a NEW version packs a fresh slot and **leaks the old one**, so a session that restyles/renames repeatedly grows the atlas monotonically until it overflows — reaching GL-02 without any node-count growth | bug-class (stale derived cache) | *none* (see GL-01) | src/__explore__/R2/atlas-gl-01-02-03-04-05.explore.test.ts | BUG | confirmed, and the real mechanism is key churn rather than the version parameter (both `putCanvas` call sites pass a literal `0`; the content lives in `texKey`, which interpolates the node name and every style token). Every rename or restyle mints a new key, packs a new slot and leaks the old one — the atlas has no eviction at all, only the full reset. Measured: one logical chip restyled repeatedly fills a 256 atlas on its own, and six bumps of one chip occupy six distinct slots. Feeds straight into GL-02. Same known_issues entry. |
| GL-06 | `destroy()` deletes GL objects but never calls `WEBGL_lose_context.loseContext()`, so each unmounted Renderer's four contexts linger until GC; the image-export dialog mounts a second Renderer, so repeatedly opening it drives the tab past the browser's ~16-context cap and the browser force-loses the OLDEST context | seed seam (context budget) | *none*; `import-export-image.spec.ts` exports once and never re-opens | tests-exploratory/R2-webgl/gl-08-13-14.explore.spec.ts | FALSIFIED | `destroy()` really does free only the GL objects and never calls `WEBGL_lose_context.loseContext()`, but four open/close cycles of the image-export dialog (each mounting and unmounting a second Renderer, so 4 extra contexts per cycle) leave the editor's own node layer still painting, with no `Too many active WebGL contexts` warning. Chromium reclaims an unreferenced context's slot on GC well before the cap. The missing explicit release stays a latent risk for a session that cycles far more aggressively. |
| GL-07 | `isWebGL2Supported` is memoised for the tab's life and is strictly weaker than `createSpriteBatch` (no shader compile, no atlas alloc), so a context that passes the gate but fails batch creation yields a permanently blank layer with only a `console.warn` | seed seam (gate strength) | *none* (see GL-01) | src/__explore__/R2/atlas-gl-01-02-03-04-05.explore.test.ts | BUG | confirmed. A stub context that satisfies exactly what `isWebGL2Supported` checks (a `webgl2` context whose `createVertexArray` is a function) still fails `createSpriteBatch` on a shader-compile or link failure. `NodesCanvas` then `console.warn`s and returns — the node layer renders nothing, permanently, with no retry and nothing user-visible, while the `WebGLUnsupportedScreen` gate has already passed. The diagram simply appears empty. known_issues: *A GPU layer that fails to build renders nothing, silently* |
| GL-08 | A forced `webglcontextlost` + restore on a bulk canvas does NOT bring the layer back — the rebuild path drops the captured atlas sub-rects, or `createSpriteBatch` returns null inside `onRestored` with no retry loop | seed seam (context loss) + coverage gap | *none* — `contextLoss.ts` has zero tests; the file itself says "verify with a manual WEBGL_lose_context smoke" | tests-exploratory/R2-webgl/gl-08-13-14.explore.spec.ts | FALSIFIED | the recovery wiring works end to end. With a node placed and the layer painting, `WEBGL_lose_context.loseContext()` on the NodesCanvas context flips `isContextLost()` to true; `restoreContext()` flips it back and the layer repaints (non-transparent pixel count returns above zero) with store invariants intact. `contextLoss.ts` says in its own header to "verify with a manual WEBGL_lose_context smoke" — that smoke is now an automated probe. |
| GL-09 | `beginInstances()` clears `floatCount` but not `instCount`; a build that begins and never commits leaves `render()` drawing the previous frame's instance count against a staging buffer that has been truncated | interleaving (build lifecycle) | *none* (see GL-01) | src/__explore__/R2/atlas-gl-01-02-03-04-05.explore.test.ts | FALSIFIED | no tearing. `instDirty` is set only by `commitInstances`, so an uncommitted build never re-uploads: `render` keeps drawing the previous frame's instance count against the buffer that produced it (draws [2, 2], one `bufferData` of 40 floats). The next committed build is correct again. The last good frame persists — consistent, not garbage. |
| GL-10 | The reserved dot/white region is restored by `resetAtlas` via the shelf cursor, but `uvCache.clear()` also drops their cache entries — a later `putImage('__dot__')`/`putCanvas` collision re-packs over live texels | seed seam (reserved region) | *none* (see GL-01) | src/__explore__/R2/atlas-gl-01-02-03-04-05.explore.test.ts | FALSIFIED | the reserved region holds. `resetAtlas` does clear `uvCache` (so the `__dot__` / `__white__` entries go), but the shelf cursor is restored to `dotShelf*` — past both — so the next chip packed after a compaction lands at x >= 40 and the closure-held `dot` / `white` UVRects are byte-identical before and after (asserted as the CONTROL case in the GL-03 block). No caller re-packs those keys. |
| GL-11 | `atlasUVRect` produces a NEGATIVE `uS`/`vS` for a zero-width or zero-height slot, so the quad samples the atlas backwards into a neighbour's pixels instead of degenerating cleanly | boundary | `atlasUV.test.ts` covers the 1px slot collapse but not 0 | src/__explore__/R2/atlas-gl-01-02-03-04-05.explore.test.ts | FALSIFIED | `atlasUVRect(x, y, 0, 0, 4096)` really does return `uS = vS = -1/4096` (negative, so the quad would sample backwards), but no caller can reach it: `itemRaster.getScratch` clamps both axes with `Math.max(1, Math.ceil(...))` (asserted against the source) and `putIcon` clamps its downscaled size the same way. `w = 1` collapses cleanly to the texel centre, which `atlasUV.test.ts` already covers. |
| GL-12 | The atlas is a fixed `min(atlasSize, MAX_TEXTURE_SIZE)`; on a device whose `MAX_TEXTURE_SIZE` is 2048 the NodesCanvas atlas silently shrinks to a quarter of its slot budget, so GL-02 becomes reachable at ordinary node counts with no diagnostic | boundary + cross-device | *none* (see GL-01) | src/__explore__/R2/atlas-gl-01-02-03-04-05.explore.test.ts | BUG | confirmed and measured on the real packer: with `MAX_TEXTURE_SIZE = 2048` the atlas is clamped to 2048 and holds fewer than a third of the 85px chips a 4096 atlas holds, with no diagnostic anywhere. It is a pure amplifier for GL-02/GL-05 — on a device with a small texture cap the leak reaches overflow at a diagram size that is unremarkable on a desktop GPU. Same known_issues entry. |
| GL-13 | The four bulk canvases paint into separate WebGL contexts whose stacking is the mount order in `Renderer.tsx`; nothing asserts that LabelsCanvas composites ABOVE NodesCanvas, so a re-order silently puts floating Labels back under nodes (the reason Labels became a first-class entity, ADR 0031 §2) | harvested invariant (ADR 0031 §2 + ADR 0038 fold) | `label-entity.spec.ts` "render/z-order: the Label paints on LabelsCanvas, DOM-after NodesCanvas (above nodes)" — **asserts exactly this**, missed at novelty-check time because it is filed under *Floating labels + node labels* in the baseline, not *Lib WebGL rendering* | tests-exploratory/R2-webgl/gl-08-13-14.explore.spec.ts | DUPLICATE | withdrawn. The probe was written and passes (both canvases compute to the same `zIndex`, and `LabelsCanvas` follows `NodesCanvas` in document order), but the core assertion is already made by `label-entity.spec.ts`, so per APPROACH §1 it does not count. Kept in the spec as a cheap four-layer generalisation; the counted follow-up is R3/GPU-13, which asserts the CONSEQUENCE (one shared stacking context, so a per-element `zIndex` can never cross an entity type). |
| GL-14 | With `preserveDrawingBuffer: true` the bulk canvases can be read back, so the "CI is pixel-blind" assumption is wrong: a node placed on an empty diagram must leave non-transparent pixels in the NodesCanvas backing store, and today nothing checks that the GPU layer painted at all | oracle construction (differential: store vs painted pixels) | *none* — `canvas-node-render.spec.ts` asserts DOM/hit-test agreement, never the painted buffer | tests-exploratory/R2-webgl/gl-08-13-14.explore.spec.ts | FALSIFIED | the layer paints correctly — and the more useful result is the oracle itself. Because every bulk canvas is created with `preserveDrawingBuffer: true`, `drawImage`-ing it into a 2D canvas and counting non-transparent alpha works: an empty diagram reads exactly 0 painted pixels and a single placed node reads > 0. R2's "everything here is pixel-blind to CI" mapper note is wrong; this oracle is reusable by R3-R5 and is what makes GL-06 and GL-08 assertable at all. |
| GL-15 | The premultiplied-alpha contract (`UNPACK_PREMULTIPLY_ALPHA_WEBGL` + shader tint premultiply + `ONE/ONE_MINUS_SRC_ALPHA`) is asserted nowhere; a translucent tint composites as a grey-fringed edge rather than a clean blend | seed seam (premultiplied invariants) | *none* (see GL-01) | — | DEFERRED | needs a translucent surface with a known expected composite (a rectangle at a set `fillOpacity` over a known backdrop) and a per-channel read-back comparison against the premultiplied-vs-straight predictions. The read-back oracle from GL-14 makes it buildable, but distinguishing a correct premultiplied blend from the grey-fringe artifact needs a reference image the campaign does not have. Reason: > 45 min to build a trustworthy reference. Manual test: draw a translucent rectangle over a dark backdrop and look for a grey halo at the edge. |

## Product questions (SUSPECT verdicts)

*none* — R2 produced four bugs and no open design questions. GL-15 is DEFERRED (see its row).

## Reusable rigs this area added

- **`src/__explore__/R2/glStub.ts`** — a recording WebGL2 stub. `glSpriteBatch` feature-checks `createVertexArray` / `vertexAttribDivisor` / `getParameter` so a canvas-mock falls back to null; supplying those drives the REAL packer, UV math and staging buffer while recording every upload and draw. It also carries `installDrawing2DStub()`, because the shared `canvasStub` provides only `font`/`measureText` and `createSpriteBatch` needs the 2D drawing calls for its built-in dot and white texel (without it construction throws `dctx.clearRect is not a function` — which under `it.failing` reads as a confirmed bug).
- **The read-back pixel oracle** in `tests-exploratory/R2-webgl/gl-08-13-14.explore.spec.ts`: every bulk canvas uses `preserveDrawingBuffer: true`, so `drawImage` into a 2D canvas plus an alpha count answers "did this GPU layer paint?". R2's mapper note ("everything here is pixel-blind to CI") is wrong — R3-R5 should use this.
