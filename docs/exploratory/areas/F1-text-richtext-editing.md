# F1 — Text, labels-as-text & rich-text editing (inline canvas edit, notes, sanitization)

**Status:** DONE · **Counted hypotheses:** 16 / 10 · **Bugs:** 10 · **Suspects:** 1 · **Hypothesis ID prefix:** `TXT-`

**Scope:** Inline canvas text editing (ADR 0034), the Quill-based `RichTextEditor` + `TextBoxInlineEditor`, dual-scope strip formatting, markdown list autofill, link cards (Ctrl+K), HTML sanitization (ADR 0029), name↔label decouple (ADR 0032), text-style field convention (ADR 0033).

**Code:**
- `packages/axoview-lib/src/components/RichTextEditor/`
- `packages/axoview-lib/src/utils/ (quill*, richTextTransform, sanitizeHtml, foldTextBoxStyleFlags, isoMath.richtext)`
- `packages/axoview-lib/src/hooks/useInlineRename.ts, useTextBox.ts`

**Dedupe baseline:** [coverage-baseline.md](../coverage-baseline.md) sections — E2E: *Textboxes*, *Floating labels + node labels*; Unit: *Lib rich-text / HTML / sanitization utils*. Then grep the suites directly.

## Related harvested invariants (keyword-matched hint list — scan the [full table](../coverage-baseline.md#invariants) too)

- **(ADR 0022 §4 / ADR 0034 §1 (commit contracts))** Inline editors commit on left-click-away and cancel on right-click-away/Escape; clicks inside the strip or any MUI portal overlay must NOT end a text-box edit session. → *The click-away allowlist is selector-based (.MuiPopper-root etc.): a new overlay (the ADR 0039 color-picker popover, a future emoji picker) rendered with a different portal class ends the edit session mid-formatting, committing a half-styled box. useInlineRename.test covers canvas click-away; no test clicks each strip popover type during a live text-box session.*
- **(ADR 0034 §1 + Lucid-parity pass (empty-box lifecycle))** An edit session that ends still-empty deletes the text box — commit-empty and cancel-on-never-committed alike; no invisible zero-width ghost may remain. → *End a session whose content is Quill's structural residue (`<p><br></p>`, or whitespace-only after &nbsp; normalization): if the emptiness check compares raw HTML rather than stripped text, an invisible ghost box survives, is lasso-selectable, and counts in Ctrl+A. Tests cover the plain-empty case, not the structural-residue cases.*
- **(ADR 0034 §4 + testing.md S1-brick guard)** No dead writes: every strip write must be schema-legal at the write site — a strip range wider than a schema cap bricks saved diagrams at safeParse on reload (the connector-label 24→40 lesson). → *ADR 0044 group icon-resize: a uniform factor multiplies each member's startScale preserving ratios — a member already at 2.5× times factor 1.3 commits 3.25, outside the schema's hard [0.1,3] → the whole diagram fails safeParse on next load. TransformNode.test covers the single-node clamp; nothing asserts per-member clamping under group factor multiplication (and per-member clamping would itself violate 'relative sizes preserved').*
- **(ADR 0034 §5 rule 7 (content fidelity))** Sanitizer allowlist ≥ editor format whitelist; any textBox.content editor round-trips losslessly; commits sanitize write-side. → *Add a new Quill format (e.g. range background-color, mirroring the existing range color) without extending the DOMPurify profile: the write-side sanitize silently strips it on commit, so formatting applied in the editor vanishes at rest. The align style-attributor survival is pinned by test; no generic allowlist⊇whitelist assertion exists, so each new format re-opens the hole.*
- **(ADR 0034 round-2 (normalizeQuillHtmlSpaces))** &nbsp;-serialized spaces are converted to real spaces on commit AND on load, so fixed-width boxes wrap; auto boxes render `pre`, fixed boxes `pre-wrap`. → *A read-only path that loads content without the editor-load normalization — the /display/drive viewer or a share-snapshot render — shows a legacy &nbsp;-heavy fixed-width box as one unbreakable line overflowing the box. Load normalization lives in the editing load chokepoint; no test loads legacy content through the read-only display routes.*
- **(ADR 0032 amendment (label ?? name fallback + seed))** Render source = label ?? name; seedNodeLabel copies name→label at LOAD so renaming identity name in Layers never moves canvas text. → *Create a node in-session (QuickAdd: name='Untitled', no label), then rename it in the Layers panel WITHOUT reloading: the seed only runs at load, the fallback renders name — so the canvas text moves with the identity rename, reproducing the exact #1 cross-persona confusion the amendment fixed, but only for never-reloaded nodes. Seed tests are load-path only.*
- **(ADR 0032 connector amendment §4 (nameSeeded marker))** The name→labels[] seed is idempotent via a nameSeeded marker stamped on every connector the pass touches; a name typed later is pure identity and never re-seeded. → *Paste a connector from the clipboard (or import a zip diagram) whose reconstruction drops the nameSeeded marker while keeping name: the next load re-seeds name into a midpoint label — duplicate label chips appear after every paste→save→reload cycle. Seed idempotency tests never route through clipboard/zip reconstruction.*
- **(ADR 0001 import semantics §1)** Project-zip import rewrites every ID and updates all cross-references: folderId, parentId, and cross-diagram link refs inside diagram models. → *Cross-diagram links now also live in Quill content HTML (ADR 0034 link-to-diagram in text runs and link cards) and in connector labels' headerLink — the importer's rewrite list predates these surfaces ('item-level link fields, view connector refs'). Import a zip whose text-box content links to a sibling diagram: the href still carries the OLD id → dead link. projectZip.test asserts ID rewriting for the original ref sites only.*
- **(ADR 0029 + sanitizeHtml hook)** User-authored HTML is sanitized before the single dangerouslySetInnerHTML sink, and the sanitizer forces rel='noopener noreferrer' on every anchor with href. → *The rel-forcing hook lives inside sanitizeHtml — link surfaces built directly in React (view-mode popover headerLink, connector-label link chips, TextBoxLinkCard's 'open in new tab') get target=_blank from their own JSX; any of them omitting rel=noopener reintroduces reverse-tabnabbing on user-supplied URLs, invisible to the sanitizer tests which only cover the HTML path.*

## Known coverage gaps (from the baseline inventory)

- (Textboxes) Bold/italic/underline inline formatting controls
- (Textboxes) Font family/size/color on a textbox
- (Textboxes) Undo/redo during or immediately after a text edit session
- (Textboxes) Clicking a committed link inside a textbox (edit mode vs view mode behavior)
- (Textboxes) Very long content overflow/clipping behavior
- (Textboxes) Textbox copy/paste as an entity
- (Floating labels + node labels) Label styling (color/font/size on a single label)
- (Floating labels + node labels) Multi-line label text
- (Floating labels + node labels) Label copy/paste/duplicate
- (Floating labels + node labels) Undo of a label TEXT edit (drag undo covered, text-edit undo not)
- (Floating labels + node labels) Label stalk rendering in 2D projection
- (Floating labels + node labels) Label behavior when its anchor node is deleted

## Hypotheses

Probe files:
- `M` = `packages/axoview-lib/src/__explore__/F1/measure-txt-01-02-11-14.explore.test.ts`
- `T` = `packages/axoview-lib/src/__explore__/F1/transforms-txt-10-12-13.explore.test.ts`
- `Z` = `packages/axoview-app/src/__explore__/F1/zip-txt-09-10.explore.test.ts`
- `E` = `packages/axoview-e2e/tests-exploratory/F1-text/text-txt-03-04-05-15.explore.spec.ts`
- `S` = `packages/axoview-e2e/tests-exploratory/F1-text/strip-txt-06-07-08-16.explore.spec.ts`

| ID | Hypothesis | Source | Nearest existing tests | Probe | Verdict | Evidence |
|----|-----------|--------|------------------------|-------|---------|----------|
| TXT-01 | A text box whose `content` is legacy PLAIN text with newlines measures as ONE row (`countHtmlLines` early-returns 1 for non-HTML) and one concatenated line wide, while the resting render's `white-space: pre` paints N rows — the stored scene size, selection outline and hit area cover only the first row | boundary / legacy input | `isoMath.richtext.test.ts` ("plain text counts as 1 unit" pins single-line only) | `M` | **BUG** | 3-line plain content → `size.height === 1`, and `width` > the same text as HTML (all 3 lines measured as one run). Repo fixtures (`view-mode-info-diagram.json`) already store plain-text content, so import/hand-edited JSON is the live producer |
| TXT-02 | HTML content built from `<div>` blocks or `<br>` line breaks (what DOMPurify keeps from an external paste / imported diagram) is counted as 1 row — `countHtmlLines`' regex only knows p/li/h1-6/blockquote/pre | bug-class: stale derived measure | `isoMath.richtext.test.ts` (`<custom>` fallback only) | `M` | **BUG** | `<div>a</div><div>b</div><div>c</div>` → height 1; `<p>a<br>b<br>c</p>` → height 1 (one `</p>`). Both survive `sanitizeHtml` intact, so they reach the HTML sink and paint 3 rows |
| TXT-03 | A node's `notes` rich HTML is never sanitized on load (`useInitialDataManager` sanitizes `view.textBoxes[].content` only), so an imported/shared diagram's `<img src=x onerror=…>` executes when the Notes editor mounts it through Quill's `clipboard.convert` innerHTML | ADR 0029 contract | `sanitizeHtml.test.ts` (util only) | `E` | FALSIFIED | The payload IS stored verbatim (nothing on the load or write path sanitizes `notes`), and the view-mode popover does mount it — but Quill 2's `clipboard.convert` parses with `DOMParser` into an inert document and rebuilds from whitelisted blots, so no `onerror` reaches the DOM and none fires. `notes` has no `dangerouslySetInnerHTML` consumer anywhere; ADR 0029's "one genuine sink" claim holds. Residual risk noted below |
| TXT-04 | Undoing the empty-box discard resurrects an invisible zero-content ghost text box that is selectable and counts in Ctrl+A — the state ADR 0034's empty-box lifecycle exists to prevent | interleaving: undo × empty lifecycle | `textbox-text-edit-move.spec.ts` (discard only) | `E` | **BUG** | Place a text box, abandon it with Escape (the ADR's discard fires, count 0), one Ctrl+Z → the box is back with `content: ''` and `scene.textBoxes[id].size === {width:1,height:1}`. Invisible, but a real entity: selectable, lasso-able, counted by Ctrl+A, and saved |
| TXT-05 | `seedNodeLabel` runs only at LOAD, so renaming a node's identity `name` in the Layers panel for a node created in-session moves the on-canvas text (render is `label ?? name`) — the exact confusion the ADR 0032 amendment fixed | ADR 0032 contract | `node-label-decouple.spec.ts` (loaded nodes), `seedNodeLabel.test.ts` | `E` | **BUG** | A placed node is `{name:'Untitled'}` with NO `label` (`PlaceIcon.ts`). The Layers rename writes `name` only, `label` stays absent, and the node's on-canvas text goes `Untitled` → `Renamed identity`. Reload first and the same rename moves nothing — the behaviour depends on whether the node has ever been through a load |
| TXT-06 | `useInlineRename`'s click-away has no strip / MUI-portal allowlist (its `TextBoxInlineEditor` sibling has one) and binds only `pointerdown`, so pressing any strip control while inline-renaming a Label / node label / connector label ends the session | bug-class: sibling drift | `useInlineRename.test.tsx` (canvas click-away) | `S` | **BUG** | Real-mouse press on `strip-text-size`: during a TEXT BOX session the editor survives (control test, same button, same gesture); during a floating-Label inline rename the editor unmounts. Every strip control is therefore unusable mid-rename on the three plain-text surfaces |
| TXT-07 | A floating Label placed and abandoned (Escape, or click-away after clearing the text) survives as a literal "Label" chip — `LabelInlineEditor.commit` reverts empty text and `Label` mode has no empty-lifecycle discard, unlike the text box | parity: empty-entity lifecycle | `label-edit-and-placement-cancel.spec.ts` | `S` | SUSPECT | Both halves confirmed: Escape right after placement leaves `text:'Label'`, and clearing the text then committing silently restores `'Label'`. Both are deliberate in code (`LabelInlineEditor.commit`'s comment), but they are the opposite of the text box's empty-box lifecycle on the same gesture. Product question below |
| TXT-08 | Escape mid-edit-session discards only the strip writes routed through Quill (range colour, horizontal align, B/I/U/S, link); the element-level ones written straight to the model (vertical align, font size, line spacing) survive the cancel — one control's two axes have opposite cancel semantics | ADR 0034 §2 dual scope | `textbox-ops.spec.ts` (align write, no cancel case) | `S` | **BUG** | One alignment popover, both axes pressed mid-session, then Escape: `verticalAlign === 'middle'` persisted, `text-align: center` gone. The vertical write is even confirmed present in the model *during* the session, so the cancel demonstrably rolls back one axis of the same control and not the other |
| TXT-09 | Project-ZIP / JSON import rewrites diagram ids but not the `#diagram:<id>` hrefs authored inside text-box content HTML, so imported cross-diagram text links dead-end | ADR 0001 import semantics | `projectZip.test.ts` (original ref sites) | `Z` | **BUG** | `rewriteRefsInModel` rewrites exactly one key, `link`. Control: `items[0].link` IS remapped in the same run; the text box's `href="#diagram:<old>"` is copied verbatim and the old id exists nowhere in the imported project |
| TXT-10 | Clipboard paste / ZIP reconstruction of a connector drops the `nameSeeded` marker while keeping `name`, so the next load re-seeds the name into a second midpoint label — duplicate chips after every paste→save→reload | ADR 0032 connector amendment | `seedConnectorLabel.test.ts` (idempotency, not via clipboard) | `T`, `Z` | FALSIFIED | Both reconstructions are whole-object copies (`{...c, id, anchors}` in `useCopyPaste`, a key-preserving deep walk in `rewriteRefsInModel`), so the marker survives and the re-seed early-returns. The negative control — dropping the marker — does produce the predicted duplicate label, so the probe can distinguish the two |
| TXT-11 | The strip's whole-content transforms are lossy for content the leaf-block walk doesn't model: `applyListFormat(…, true)` clears `body.innerHTML` and rebuilds from `collectLeafBlocks`, so nested/legacy structure (table cells, nested lists, `<div>` wrappers) loses text | boundary / degenerate input | `richTextTransform.test.ts` (p/li/h/plain) | `M` | FALSIFIED | No text is lost over `<div>`, nested lists or table structure — `BLOCK_TAGS` covers DIV/TABLE/TBODY/TR/TD. `<br>`-in-one-block collapses to a single `<li>` but keeps every character, and that shape is not editor-producible |
| TXT-12 | `updateTextBox`'s re-measure list omits fields the rendered geometry depends on, so a write that changes the footprint leaves `scene.textBoxes[id].size` stale | bug-class: stale derived cache | `stores/reducers/__tests__/textBox.test.ts` | `T` | FALSIFIED | Differential oracle (reducer-stored size vs a fresh `getTextBoxDimensions`) over every field the measurement reads — content/fontSize/lineHeight/width/height — all agree, and each write is first shown to change the measurement so the parity check has teeth. `orientation`/`verticalAlign` correctly skip it (the measurement never reads them) |
| TXT-13 | The strip's whole-content B/I/U/S is not a round trip over PARTIALLY formatted content: pressing B (whole-content "apply", because `getWholeContentFormats` reads a partly-bold box as not-bold) then B again unwraps EVERY bold run — destroying per-range formatting the user applied inside the editor | round-trip oracle (§4.4) | `richTextTransform.test.ts` "round-trips apply → remove" (starts from UNFORMATTED content) | `T` | **BUG** | `<p>plain <strong>bold</strong> tail</p>` → B on → B off → `<p>plain bold tail</p>`. Same for italic/underline/strike (one shared code path). Two strip presses with no editor session silently flatten the box's formatting |
| TXT-14 | `ensureHtmlContent`/`TextBox`'s HTML sniff is `content.trim().startsWith('<')`, so legacy plain text that begins with `<` (`<T> generics`, `<3`) is parsed as HTML and mangled by DOMPurify | boundary / degenerate input | `richTextTransform.test.ts` (`isHtmlContent`) | `M` | **BUG** | `sanitizeHtml('<T> is a type parameter')` → `' is a type parameter'` — the token is dropped. The load path writes the sanitized string BACK into the model, so one open+save loses it permanently. `ensureHtmlContent`'s escape branch — the protection `TextBoxInlineEditor`'s comment claims ("catalog I-23") — is unreachable for exactly this input |
| TXT-15 | A text box whose edit session ends still-empty is deleted, but nothing drops it from `uiState.selectedIds` / the transform controls, so the empty-discard leaves a selection pointing at a dead id (INV-2) | cross-store invariant | `expectStoreInvariants` INV-2 | `E` | **BUG** | `discardEmpty` calls `setItemControls(null)` — which clears `itemControls` and `selectedConnectorLabel` but **not** `selectedIds`, because a multi-selection legitimately has `itemControls === null`. After the discard `itemControls` is null and `selectedIds` still holds `{type:'TEXTBOX', id:<deleted>}`; the paired `expectStoreInvariants` probe fails INV-2. Sibling of HIST-13, but distinct: here the code *intends* to deselect and calls the half-deselecting API |
| TXT-16 | A floating Label's inline editor is mounted `multiline: true`, so a newline is authorable — but nothing downstream of the commit models multi-line Label text (chip measurement, the GPU chip, the hit proxy) | anomaly capture (while probing TXT-07) | none — "Multi-line label text" is a listed baseline gap | `S` | FALSIFIED | Multi-line Labels work end to end: the commit stores `'AAA\nBBB'`, `labelChip.ts` splits on `\n` and lays out per-line (`lines.length * lineH` chip height), and the hit proxy comes out two lines tall — with a single-line control proving the height oracle can tell them apart. One correction to the code's own documentation: `useInlineRename`'s guard is `e.key === 'Enter' && (!multiline || !e.shiftKey)`, so with `multiline` the PLAIN Enter commits and Shift+Enter is the newline — the reverse of the hook's JSDoc ("plain Enter inserts a newline and only Enter commits via blur") |

**Next:** area closed (16 counted, 10 bugs, 1 suspect). Nothing outstanding.

## Standing thread this area adds

**F-a. Two implementations of one editing contract, and the second one is always the plain-text sibling.**
`TextBoxInlineEditor` (Quill, ADR 0034) and `useInlineRename` (contentEditable, ADR 0022 §4)
are two takes on "inline canvas editing", and the text box got every refinement:
the strip/portal click-away allowlist (TXT-06), the `mousedown` companion binding,
the empty-entity lifecycle (TXT-07), the mid-session live-measure. The three
plain-text surfaces (floating Label, node name, connector label) share the older
hook and inherit none of it. Any area touching an editing surface should ask
which of the two it is on.

## Product questions (SUSPECT verdicts)

| ID | Question | Recommendation |
|----|----------|----------------|
| TXT-07 | Should a floating Label follow the text box's empty-entity lifecycle — placed-then-abandoned is discarded, emptied-then-committed is deleted — instead of keeping the literal placeholder word "Label"? | **Yes, partly.** As built, `Label` mode creates the chip with `LABEL_DEFAULTS.text = 'Label'` and drops into inline edit; Escape leaves that placeholder on the canvas, and clearing the text then committing silently restores it (`LabelInlineEditor.commit` reverts when `!text.trim()`). Both are deliberate in code, but they are the opposite of what the *same two gestures* do to a text box one tool over, and the revert gives no feedback — the user sees their deletion undone with no message. Recommendation: (a) discard a Label whose FIRST edit session ends without a commit (matching the text-box place-and-type contract exactly), and (b) keep the revert for an existing Label but say so, or delete it — either is defensible, silence is not. Note this interacts with TXT-06: the abandon path is easy to hit by accident, because reaching for the strip mid-rename also ends the session |

> **CLOSED 2026-07-30 — owner decisions** ([DECISIONS.md](../DECISIONS.md)): TXT-07 — full text-box lifecycle parity: abandoned first edit discards the Label, empty commit deletes it (undoable); the silent revert goes.

## Residual risks noted while probing (not findings)

- **`notes` is stored and round-tripped unsanitized** (TXT-03). The load path
  sanitizes `view.textBoxes[].content` and nothing else, so an imported or
  shared diagram's `notes` keeps whatever HTML it carried. Today that is safe
  because the only consumers are Quill (`RichTextEditor`, which rebuilds from a
  `DOMParser`-parsed inert document and a blot whitelist) and `stripHtmlTags`.
  The first `dangerouslySetInnerHTML` consumer of `notes` inherits a live stored-XSS
  hole with no warning — ADR 0029's "one genuine sink" is a property of the
  current call graph, not an enforced invariant.
