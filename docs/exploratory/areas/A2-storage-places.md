# A2 — Storage providers & places model (local/session/Drive, move-to-Drive)

**Status:** DONE · **Counted hypotheses:** 15 / 10 · **Bugs:** 13 · **Suspects:** 1 · **Hypothesis ID prefix:** `STOR-`

**Next:** area closed — 16 rows, all resolved (STOR-15 `DEFERRED`, needs two live Google accounts). Continue with A3.

**Reusable rigs left behind.** `__explore__/A2/local-place-*` carries a `recordFetch(answer)` helper that captures url/method/parsed-body per call (what the existing `LocalStorageProvider.test` fetch mock cannot do) and a `trapKey(key)` per-key `QuotaExceededError` trap. `__explore__/A2/drive-stor-*` drives the real `GoogleDriveProvider` with a stateful fetch double that commits a write *before* answering 503 — the shape any "is this retry safe?" probe needs. Two file-scoping traps cost a run each: a `jest.doMock` + `jest.resetModules()` pair leaks across tests in one file (it fed the STOR-11 probe a stubbed `fetchRuntimeConfig`, whose fetch count then silently read 0), and re-importing a React component through a reset module registry gives a null dispatcher — so `manager-stor-10`, `config-stor-11` and `remount-stor-12` each live alone.

**Scope:** StorageManager is a module-level singleton delegating StorageProvider calls to the active provider ('local' | 'google-drive'). LocalStorageProvider dual-paths: server REST (/api/diagrams etc., when /api/config says serverStorage) with sessionStorage/localStorage fallback. GoogleDriveProvider maps diagrams/folders to Drive files under a marker-discovered root folder with lean-save (ADR 0003: strip pack icons, record requiredPacks), retry/backoff, and 401->markExpired. driveTransfer implements MOVE semantics session->Drive (create, verify, delete source). AppStorageContext boots from a cached 800ms /api/config probe (ADR 0009 D2) and derives serverStorageAvailable/remoteStorageActive/defaultPlaceId.

**Code:**
- `packages/axoview-app/src/providers/AppStorageContext.tsx`
- `packages/axoview-app/src/services/storage/StorageManager.ts`
- `packages/axoview-app/src/services/storage/providers/LocalStorageProvider.ts`
- `packages/axoview-app/src/services/storage/providers/GoogleDriveProvider.ts`
- `packages/axoview-app/src/services/storage/leanModel.ts`
- `packages/axoview-app/src/services/storage/driveTransfer.ts`
- `packages/axoview-app/src/services/storage/types.ts`
- `packages/axoview-app/src/hooks/useRuntimeConfig.ts`

**Dedupe baseline:** [coverage-baseline.md](../coverage-baseline.md) sections — E2E: *File explorer / diagram management*; Unit: *App storage providers, transfer & storage context*, *App Google Drive services (picker, public read, sharing)*. Then grep the suites directly.

## Seed seams (2026-07-29 mapping — starting points, not a ceiling)

- Route-change remount desync: the manager singleton keeps its active provider across EditorPage remounts, but AppStorageProvider's activeProviderIdState resets to 'local' — after opening a Drive diagram then navigating to any /display/* route and back, React state says 'local' (remoteStorageActive=false) while manager.getActiveProvider() is still Drive, so mode branches and actual reads/writes disagree
- LocalStorageProvider silently falls back to sessionStorage on ANY server list/load error (listDiagrams/loadDiagram/listFolders/getTreeManifest catch-all) — a transient backend outage makes the tree show per-tab data while saves still throw against the server, splitting the workspace
- Mixed lifetimes in the local place: diagram blobs+index live in sessionStorage ('axoview_diagram_*'/'axoview_diagrams'), folders and tree manifest in localStorage ('axoview-folders'/'axoview-tree-manifest') — closing the tab keeps folders but loses all diagrams, leaving a persistent orphan folder tree and dangling folderIds
- GoogleDriveProvider.listDiagrams(undefined) queries ALL app-marked JSON files in Drive, not just under the root — a file whose parent is neither root nor a listed folder gets folderId=<unknown parent> and becomes invisible in the composed tree while still counting in listings (sessionHasContent, icon-usage scans, export scope)
- Drive root cache 'axoview-drive-root' in localStorage is not keyed by Google account — after switching accounts in the same browser, a cached root that account B can access (e.g., shared) passes folderExists and B silently writes into A's folder
- GoogleDriveProvider.request() retries 429/5xx by replaying the SAME request including non-idempotent multipart POST creates — a 500-after-write on createDiagram/createFolder mints duplicate files
- sessionListDiagrams / localGetFolders / getTreeManifest parse stored JSON with no try/catch — one corrupt sessionStorage/localStorage entry throws through every list call and bricks the tree
- leanIfModel: when the input is already lean AND some item icon is unresolved, it preserves existing requiredPacks — but if that field was already lost (older save), the derived list is empty and the pack hint is permanently wiped on the next autosave round-trip
- fetchRuntimeConfig's 800ms AbortSignal.timeout: a slow-but-healthy backend gets misclassified as Local mode, cached for the whole session — server-stored diagrams silently invisible until reload
- (merged from "Google Drive storage provider, places model & move-to-Drive") Root duplication races: ensureRoot() on any write auto-creates DEFAULT_ROOT_NAME while the first-connect DriveRootFolderDialog is pending; configureRoot() adopts+renames an existing marker root, but two tabs/devices interleaving probeRoot→createRootFolder can still mint two axoviewRoot markers, after which findRootByMarker() is nondeterministic (files[0]) and diagrams split across roots.
- (merged from "Google Drive storage provider, places model & move-to-Drive") saveTreeManifest find-then-create race: two concurrent saves both get findManifestId=null and each uploadCreate a separate axoview-manifest.json; thereafter reads pick files[0] — folder tree flickers between two manifests.
- (merged from "Google Drive storage provider, places model & move-to-Drive") moveItem read-modify-write on parents (fetch parents → addParents/removeParents): concurrent move from another tab loses one; folderId mapping everywhere assumes parents[0] and parent===root→null — a multi-parent legacy file or a file whose parent is outside the root tree misfiles at root.
- (merged from "Google Drive storage provider, places model & move-to-Drive") listDiagrams(undefined) queries by app-marker only (account-wide), not by root subtree — an axoview-marked file sitting outside the configured root (e.g. after the user moved it in Drive's own UI) appears with folderId=<unknown parent> that no listed folder matches; check how the explorer renders that orphan.
- (merged from "Google Drive storage provider, places model & move-to-Drive") driveTransfer non-atomicity: crash/network failure between drive.createDiagram success and source.deleteDiagram leaves a duplicate in both places (documented as move-not-copy); its name de-collision and folder-path matching use a one-shot local listing (driveFolders/driveDiagrams arrays) that goes stale against concurrent Drive-side changes mid-run; folder chain matches by NAME only.
- (merged from "Google Drive storage provider, places model & move-to-Drive") Place-follow race: setActiveProviderId flips the singleton StorageManager's active provider when a diagram opens — an autosave for the previously open local diagram still in flight resolves storage.saveDiagram against the NEW provider (Drive), writing a local diagram's blob to a Drive file id (or 404ing). The seam is between DiagramLifecycleProvider's open path and any queued save.
- (merged from "Google Drive storage provider, places model & move-to-Drive") 403 permanent-classification: only /rateLimitExceeded/i retries; storageQuotaExceeded, sharingRateLimitExceeded and Workspace policy 403s throw immediately — verify every write path (autosave especially) surfaces that per ADR 0011 instead of losing the save silently.
- (merged from "Google Drive storage provider, places model & move-to-Drive") leanIfModel strips pack icons on every Drive save (ADR 0003) — seam with the readonly/display loaders which must rehydrate via iconPackManager.loadPacksForDiagram; a diagram saved lean then opened through a path that skips rehydration renders iconless.

## Related harvested invariants (keyword-matched hint list — scan the [full table](../coverage-baseline.md#invariants) too)

- **(testing.md ADR 0023 additions (snap-grid.spec freeze test))** Turning global snap ON does not re-snap existing off-grid items. → *A future load-time normalization or 'cleanup' migration that drops offset when snap is globally on — the e2e freeze test covers the toggle in-session, but a load-path normalization (e.g. in useInitialDataManager, where seeds already run) would re-snap on reload and no reload-with-global-snap-on test exists.*
- **(ADR 0037 §2 (active provider follows open diagram))** Switching the open diagram to another place flushes the pending autosave to the OLD place before setting the new active provider. → *With a dirty session diagram (debounced autosave pending), immediately open a Drive diagram: if the flush is fire-and-forget rather than awaited before the provider swap, the session autosave either writes through the Drive provider (wrong place) or is cancelled (silent data loss). driveTransfer/authStore tests don't cover the open-diagram provider-swap flush ordering.*
- **(ADR 0036 §2 + known_issues (root-folder detection))** ADR 0036 promises the provider detects a deleted/trashed Drive root folder; as-built, isAvailable() only checks auth and the cached root id is never revalidated. → *Trash the app folder in Drive's own UI mid-session: autosaves keep 200-OK patching files in the trash for the rest of the session; loss surfaces only at next full listing. The cheap fix (invalidate on zero-listing or 404) is catalogued but unimplemented — any test asserting 'save succeeded ⇒ durable' is false here.*
- **(ADR 0035 / authStore.test.ts)** The Google token is NEVER persisted — only the identity/profile hint survives reloads; silent reconnect re-mints via GIS. → *The regression test spies on localStorage.setItem only. A convenience change that stashes the token in sessionStorage, IndexedDB, or a cookie (e.g. to survive the popup-blocker boot problem) evades the spy entirely and ships green while violating the ADR's central security contract.*
- **(features.md (viewer-controlled projection, PR #84))** canvasMode in view-only mode is viewer-local UI state persisted only to that viewer's localStorage — switching projection can neither dirty nor save the diagram. → *A viewer switches to 2D on the /display route; the localStorage key is shared with the editor — the OWNER later opens the editor in the same browser and their diagram opens in 2D with a recentered scroll they never chose, and if any editor-side code treats canvasMode as model-adjacent (e.g. included in a future save payload or dirty-diff), a pure viewer action dirties the document. Tests assert non-dirty in view mode only, not the shared-key bleed into edit mode.*
- **(canvas-interaction.md §2 (isRendererInteraction gate))** mousedown/mouseup gate on isRendererInteraction; mousemove deliberately does NOT — any move to scoped listeners must replace window-binding with setPointerCapture or drags break when the cursor leaves the box. → *A drag that strays over a NEW overlay child that stops propagation (a future minimap, the annotation palette when open in edit mode): moves keep flowing (window-bound) but the mouseup lands gated-out if the overlay swallows it → the drag never commits and DRAG_ITEMS stays armed, committing on the NEXT unrelated mouseup. No test releases a drag over each overlay surface.*
- **(local-mode autosave / session keys (known_issues e2e entry + features.md))** Session-place work autosaves to localStorage and survives reloads; explorer persistence is a coherent triple (axoview-diagrams, axoview-last-opened, axoview-last-opened-data). → *A quota-exceeded write during autosave (5MB cap, gauge at 90%+): if the three keys are written non-atomically and one write throws, axoview-last-opened can point at an entry missing from axoview-diagrams — reload lands on the empty state or the wrong diagram while the save indicator claimed success. LocalStorageProvider tests never inject QuotaExceededError mid-triple.*

## Known coverage gaps (from the baseline inventory)

- (File explorer / diagram management) Dragging a diagram into/out of a folder
- (File explorer / diagram management) Folder rename and folder delete (incl. non-empty folder)
- (File explorer / diagram management) Deleting the currently-open diagram (what the editor shows after)
- (File explorer / diagram management) Duplicate diagram/folder names
- (File explorer / diagram management) Creating a NEW diagram from the explorer while one is open
- (App storage providers, transfer & storage context) packages/axoview-app/src/services/storage/StorageManager.ts — zero tests (provider selection/failover orchestration)
- (App storage providers, transfer & storage context) packages/axoview-app/src/services/storage/leanModel.ts — zero tests
- (App storage providers, transfer & storage context) packages/axoview-app/src/services/iconPackManager.ts and iconUsage.ts — zero tests

## Hypotheses

| ID | Hypothesis | Source | Nearest existing tests | Probe | Verdict | Evidence |
|----|-----------|--------|------------------------|-------|---------|----------|
| STOR-01 | `serverCreateDiagram` never applies `leanIfModel`, while its session sibling and both Drive paths do — so on a server deploy the FIRST write of every diagram persists the whole icon catalog and records no `requiredPacks` | sibling drift / ADR 0003 | `LocalStorageProvider.test` "createDiagram() returns new id from server" (never inspects the body); "saveDiagram() strips pack icons" (save path only) | `__explore__/A2/local-place-stor-01-03-04-05-06-07-14` | BUG | T1 confirmed. `createDiagram` POSTs all 3 icons with no `requiredPacks`; the very next `saveDiagram` PUTs `icons: [my-logo]`, `requiredPacks: [aws]`. Control: the SESSION create IS lean, so the drift is server-create-only. known_issues: A2/STOR-01. |
| STOR-02 | `deleteFolder(id, recursive)` means three different things: the server path forwards `?recursive=`, Drive ignores the flag and always cascades, session ignores it the other way and deletes only the folder row | parity oracle (§4.5) | none — folder delete has no test in any provider | `__explore__/A2/drive-stor-02-08-09-13-16` | BUG | T1 confirmed, all three paths in one test. Server forwards `recursive=false` / `recursive=true`; Drive sends `{trashed:true}` for both (cascading either way); session leaves every diagram in place for both. So `recursive:true` in the session place is weaker than `recursive:false` on Drive. known_issues: A2/STOR-02. |
| STOR-03 | Deleting a non-empty folder in the session place leaves its diagrams in `axoview_diagrams` pointing at a folder that no longer exists: invisible in the tree, still consuming the budget, still counted by every `listDiagrams()` consumer | boundary + coverage gap "folder delete (incl. non-empty)" | none | `__explore__/A2/local-place-stor-01-03-04-05-06-07-14` | BUG | T1 confirmed. After `deleteFolder(f, false)` the folder is gone from `listFolders()` while the diagram is still in `listDiagrams()` with `folderId` naming the dead folder, absent from the root listing, and its bytes still in sessionStorage. `recursive: true` behaves identically - the recursive branch only widens the FOLDER sweep. known_issues: A2/STOR-03. |
| STOR-04 | One failed server list call silently swaps the whole workspace for the empty per-tab session one, with no error surfaced anywhere, while the paired write still targets the server and throws | seed seam + ADR 0011 | `LocalStorageProvider.test` "listDiagrams() falls back to sessionStorage when server unavailable" (asserts the fallback happens, not its silence or the write asymmetry) | `__explore__/A2/local-place-stor-01-03-04-05-06-07-14` | BUG | T1 confirmed. With every request rejecting, `listDiagrams()` resolves to `[]` with zero `console.warn` and zero `console.error`, while `saveDiagram` on the same provider in the same state rejects - the read half and the write half disagree about whether the backend exists. known_issues: A2/STOR-04. |
| STOR-05 | `sessionListDiagrams` / `localGetFolders` / `getTreeManifest` parse with no try/catch, so one corrupt entry throws through every list call and bricks the file tree — while `renameDiagram`'s blob parse in the same file IS guarded | seed seam | `LocalStorageProvider.test` "leaves listing rename in place when blob is corrupted" (the one guarded parse) | `__explore__/A2/local-place-stor-01-03-04-05-06-07-14` | BUG | T1 confirmed. A corrupt `axoview_diagrams` / `axoview-folders` / `axoview-tree-manifest` value makes `listDiagrams` / `listFolders` / `getTreeManifest` reject with SyntaxError; in server mode the fallback sits INSIDE the catch, so its throw escapes the try that was meant to make listing failure-proof. Contrast asserted in the same test: the one guarded parse (`renameDiagram`) survives. known_issues: A2/STOR-05. |
| STOR-06 | `sessionSaveDiagram` writes the blob before the index, so a quota failure on the index write leaves an orphan blob consuming the budget and a diagram that no listing shows | harvested invariant (non-atomic triple) | none — no test injects QuotaExceededError mid-write | `__explore__/A2/local-place-stor-01-03-04-05-06-07-14` | BUG | T1 confirmed with a per-key QuotaExceededError trap. `saveDiagram` rejects, the blob is already in sessionStorage, and `listDiagrams()` returns `[]` - unreachable bytes on a 5 MB budget that nothing can show or delete. known_issues: A2/STOR-06. |
| STOR-07 | Only some session write paths dispatch `axoview-session-changed`: `renameDiagram`, `restoreDiagram` and the diagram branch of `moveItem` don't, so the storage gauge and the `sessionWorkUnexported` export guard miss those mutations | ritual drift (thread S-a) | none | `__explore__/A2/local-place-stor-01-03-04-05-06-07-14` | BUG | T1 confirmed by counting `axoview-session-changed`: save 1, delete 1, rename 0, restore 0, moveItem 0 - with the precondition that the rename really did rewrite the index, so 0 is the omission and not a no-op. known_issues: A2/STOR-07. |
| STOR-08 | `GoogleDriveProvider.request()` replays the SAME non-idempotent multipart POST on 429/5xx/network error, so a 500 returned after the write mints a duplicate Drive file | seed seam | `GoogleDriveProvider.test` "retries a transient 503 then succeeds" / "gives up after exhausting retries" — both drive `loadDiagram`, a GET | `__explore__/A2/drive-stor-02-08-09-13-16` | BUG | T1 confirmed. One `createDiagram` against a Drive double that commits the write then answers 503 issues TWO multipart POSTs and leaves two files; the call succeeds and returns the second id, so the orphan is untracked and silent. Existing retry tests drive `loadDiagram`, a GET. known_issues: A2/STOR-08. |
| STOR-09 | `driveTransfer` reports `ok: false` and drops `driveId` when the SOURCE DELETE fails after a verified Drive create, so the diagram now exists in both places, the caller is told the move failed, and a retry mints a second Drive copy | seed seam / non-atomicity | `driveTransfer.test` "a failed Drive create keeps the source copy (no delete)" — the create leg only | `__explore__/A2/drive-stor-02-08-09-13-16` | BUG | T1 confirmed. Create succeeds, source delete throws: the result is `ok:false` with `driveId`/`driveName` undefined, so the caller cannot reconcile the copy it now owns; a second run with the same selection creates a second Drive copy. known_issues: A2/STOR-09. |
| STOR-10 | `StorageManager.setServerStorage` pushes `usingServer` onto the ACTIVE provider only, so called while Drive is active the local provider never learns it is server-backed and silently serves sessionStorage forever | seed seam | none — `StorageManager.ts` has zero tests | `__explore__/A2/manager-stor-10` | FALSIFIED | The mechanism is real and reproduced (Drive active + `setServerStorage(true)` leaves `local.usingServer === false`, and `setActiveProvider('local')` never re-syncs) - but the prediction of a silent sessionStorage workspace is not reachable: rendering the real `AppStorageProvider` with a server-backed config shows `getProvider('local').usingServer === true`, because the singleton runs `setActiveProvider('local')` at module scope. Latent until a second caller appears. |
| STOR-11 | A `/api/config` probe that merely exceeds the 800 ms timeout is cached as Local mode for the whole session with only a `console.warn`, so a server deploy's diagrams are invisible until the user reloads | seed seam | `useRuntimeConfig.test` "aborts a hanging fetch … falls back to defaults" + "caches the resolved config across calls" — each half, never the composition or the user-visible consequence | `__explore__/A2/config-stor-11` | SUSPECT | T1 confirmed behaviourally, with a 400 ms control that IS detected: a backend answering in 1.2 s yields `serverStorage:false`, one `console.warn`, and `cached` is never re-probed for the life of the page. Whether a timed-out probe should be retried is a product call (ADR 0009 D2 deliberately chose one probe) - recommendation in the product-questions list. |
| STOR-12 | Route-change remount desync: the module-level manager keeps its active provider while `AppStorageProvider`'s `activeProviderIdState` resets to `'local'`, so after Drive diagram → `/display/*` → back, `remoteStorageActive` is false while reads and writes still go to Drive | seed seam | none | `__explore__/A2/remount-stor-12` + `tests-exploratory/A2-storage/remount-stor-12` | BUG | Both halves measured. jsdom: after unmount+remount the context reports `activeProviderId: local` / `remoteStorageActive: false` while `storageManager.getActiveProvider().id` is still `google-drive`. Browser: a `/display` round trip re-runs the ref-guarded explorer initialiser, so the editor tree really does remount - the desync is reachable by navigating, and this also confirms A1/LIFE-05 reachability. known_issues: A2/STOR-12. |
| STOR-13 | `GoogleDriveProvider.listDiagrams(undefined)` queries account-wide by app marker rather than by root subtree, so a file the user moved out of the root in Drive's own UI returns a `folderId` matching no listed folder | seed seam | `GoogleDriveProvider.test` "listDiagrams maps Drive files → DiagramMeta[]" (all files under root) | `__explore__/A2/drive-stor-02-08-09-13-16` | BUG | T1 confirmed. The listing URL carries no root term (asserted after decode), and a file whose `parents[0]` is an unrelated folder comes back with that raw id as `folderId` while `listFolders()` knows nothing about it - present in every count, placeable in no tree. known_issues: A2/STOR-13. |
| STOR-14 | The app's `leanIfModel` keeps only `collection === 'imported'`, a stricter rule than ADR 0003 and than the lib's `stripDefaultIcons` (which keeps unknown ids and overridden defaults) — so every SAVE discards icons that every EXPORT preserves, and `mergeBundledFixtures` cannot restore them | sibling drift / ADR 0003 contract | `leanSave.test.ts` (lib, tests `stripDefaultIcons`); `leanModel.ts` has zero tests | `__explore__/A2/local-place-stor-01-03-04-05-06-07-14` | BUG | T1 confirmed. An icon with an id no bundle supplies and `collection: retired-pack` is stripped by `leanIfModel` (and its pack recorded as required, so it returns as a tombstone), and so is a bundled icon with a changed `name` - the exact case ADR 0003 lists as an acceptance criterion ("override wins") and that the lib's `stripDefaultIcons` implements. Save and export disagree about what counts as user data. known_issues: A2/STOR-14. |
| STOR-15 | The Drive root cache `axoview-drive-root` is not keyed by Google account, so after switching accounts in one browser the cached root is adopted without any ownership check | seed seam | `GoogleDriveProvider.test` "root discovery: adopts an existing marker folder" | deferred - needs two live Google accounts | DEFERRED | Not counted. The cache key `axoview-drive-root` carries no account identifier and no sign-out path clears it, but whether the harm is reachable turns on a platform behaviour a mock cannot answer: with the `drive.file` scope (the only scope requested) account B should get a 404 for a folder the app created for account A, and `folderExists` catches everything and returns false, which clears the cache and re-discovers by marker. Manual test: sign in as A, create a root, sign out, sign in as B in the same browser, and check whether B writes into A's folder. |
| STOR-16 | `getTreeManifest` falls through to localStorage on a server error while `saveTreeManifest` WRITES to localStorage on one, so a failed manifest save lands locally and the stale server copy silently wins on the next healthy read | sibling drift / ritual drift | none — the manifest has no test on either provider | `__explore__/A2/drive-stor-02-08-09-13-16` | BUG | T1 confirmed. In server mode a failed manifest PUT resolves (no throw, no signal) and writes localStorage instead; once the backend recovers `getTreeManifest` prefers the server copy, so the user's folder ordering silently reverts. The read and write halves fall back to the same store with opposite authority. known_issues: A2/STOR-16. |

## Product questions (SUSPECT verdicts)

- **STOR-11 — should a timed-out `/api/config` probe be retried, or is one shot the contract?** As built, `fetchRuntimeConfig` writes its Local-mode fallback into the module-level `cached` and `if (cached) return cached` wins for the life of the page. Measured: a backend that answers correctly in 1.2 s (cold start, loaded host, tunnelled dev backend) yields `serverStorage: false` with one `console.warn` and no second probe ever; the same backend at 400 ms is detected correctly. On a server deploy that means the user's entire server-stored workspace is invisible until they happen to reload, and the file tree shows an empty *local* place rather than an error. ADR 0009 D2 deliberately collapsed two probes into one, so "probe again" is not obviously right. **Recommendation:** keep the single 800 ms probe for the fast path but do not cache a *transport* failure as an answer — cache only a response that was actually received, and let the next caller re-probe. That is a two-line change (`cached = …` only inside the success branch, with `inflight` still deduping concurrent boot callers) and it preserves the ADR's intent while removing the permanence. Failing that, surface it: a Local-mode boot that was *inferred from a timeout* should say so, because today it is indistinguishable from a genuine Local deploy.

> **CLOSED 2026-07-30 — owner decisions** ([DECISIONS.md](../DECISIONS.md)): STOR-11 — adopt cache-success-only: `fetchRuntimeConfig` caches only received responses; a timeout no longer latches local mode for the page life.
