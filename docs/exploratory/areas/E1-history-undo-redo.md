# E1 — History & undo/redo engine (dual-store patches)

**Status:** DONE · **Counted hypotheses:** 15 / 10 · **Bugs:** 9 · **Hypothesis ID prefix:** `HIST-`

**Scope:** modelStore owns the document (version/title/description/colors/icons/items/views) plus a 50-entry immer-patch undo stack; sceneStore owns derived render caches (scene.connectors[id].path(+unroutable), scene.textBoxes[id].size) plus its own independent 50-entry stack. Every history entry is stamped with a module-global logical-action sequence (historySequence.ts, D-7); useHistory steps only the stack(s) whose top entry carries max(undo)/min(redo) seq, then resyncScene re-routes connectors whose path is empty via SYNC_SCENE written skipHistory. Mutations: actions.set(partial, skipHistory) computes patches against a pendingPre snapshot captured by saveToHistory(); freezePendingPre/unfreezePendingPre implement live-drag one-entry commits.

**Code:**
- `packages/axoview-lib/src/stores/modelStore.tsx`
- `packages/axoview-lib/src/stores/sceneStore.tsx`
- `packages/axoview-lib/src/stores/historySequence.ts`
- `packages/axoview-lib/src/hooks/useHistory.ts`

**Dedupe baseline:** [coverage-baseline.md](../coverage-baseline.md) sections — E2E: *Undo/redo / clipboard / hotkeys / modes*; Unit: *Lib store reducers + zustand stores*, *Lib hooks*, *Lib perf/refactor regression suite (__perf_refactor_regression__)*. Then grep the suites directly.

## Seed seams (2026-07-29 mapping — starting points, not a ceiling)

- Orphaned pendingPre: saveToHistoryBeforeChange() runs, then the reducer throws (e.g. updateViewItem's validateView throw) so no set() follows — pendingPre stays non-null. The NEXT skipHistory=true write (changeView's SYNC_SCENE, resyncScene, computePathsAsync batch) consumes it and pushes a bogus history entry stamped with a stale seq; undo then reverts a diff the user never made.
- No-op set swallows the snapshot: modelStore.tsx line ~193 / sceneStore.tsx line ~184 — when patches.length===0 the guard returns early but pendingPre was ALREADY nulled. In a coordinated sequence (coordinator saveToHistory → op1 set(skip=true) that happens to be a net no-op → op2 set(skip=true) with real changes), op2 finds pendingPre null and applies with NO history entry — the real change becomes un-undoable.
- Seq is read at set() time, not pinned at allocation: commitDragTransaction stamps currentHistorySequence() (the live global counter). Any allocateHistorySequence() between beginDragTransaction and commit — e.g. a keyboard-shortcut action mid-drag, or a second mounted Axoview instance (counter is module-global across provider pairs) — mis-stamps the entry, so useHistory's max/min pairing pops model and scene halves of one action separately (the invisible-connector class of bug).
- Asymmetric stack trimming: MAX_HISTORY_SIZE=50 per store, trimmed independently (newPast.shift()). After >50 model-only actions the model stack drops the entry whose seq still pairs with an old scene entry; deep undo then steps the scene without its model half → model/scene divergence.
- resyncScene writes scene skipHistory after undo/redo, mutating the object shape that queued redo patches were computed against; immer applyPatches with 'add'/'remove' ops against the resynced connectors map can mis-apply or throw, and redo can resurrect paths inconsistent with the resynced state.
- useHistory.transaction and useSceneActions.transaction are two DIFFERENT machineries with per-hook-instance refs. A useHistory.transaction wrapping scene CRUD does not suppress useSceneActions' saveToHistoryBeforeChange (separate transactionInProgress ref) — each op overwrites pendingPre and allocates a fresh seq, splitting the 'atomic' group into N entries. Same per-instance hazard for dragInProgress: beginDragTransaction in one component does not guard batch updates issued through another component's useSceneActions instance.
- Undo history is global but scene cache + uiState.view are per-active-view: undoing an action made in view A while view B is active applies model patches to view A's data, but resyncScene only repairs the ACTIVE view's scene; undoing a CREATE_VIEW/DELETE_VIEW never restores uiState.view (not in any history), leaving view pointing at a nonexistent id (useSceneData silently falls back to views[0]).
- (mapper note) modelStore comments record prior bug classes here (MQA #5 no-op-set clobbering redo, D-7 dual-stack drift, D-8 provisional paste paths) — this seam has a track record.

## Related harvested invariants (keyword-matched hint list — scan the [full table](../coverage-baseline.md#invariants) too)

- **(canvas-interaction.md §6.2 (drag transactions))** beginDragTransaction/commitDragTransaction must pair exactly once per gesture; a leaked bracket suppresses saveToHistoryBeforeChange for ALL subsequent edits, silently dropping undo history. → *Start a node drag, then lose the mouseup (release outside the window / alt-tab / browser context-menu steals the event): exit runs lazily only on the NEXT mouse event, so if the user's next action is keyboard-only (Delete, Ctrl+Z), the bracket may stay open and later edits produce no undo entries. connector.dragPerf.test.tsx tests the happy 40-tick path, never a lost mouseup.*
- **(canvas-interaction.md §8 (no rollbackDragTransaction))** Every mode exit safety-net COMMITS; node CSS-preview drags abort-to-origin only by accident. A mid-drag tool hotkey is an undocumented partial abort: nodes snap back, textbox/rect/anchor moves commit. → *Press 'r' mid multi-drag of nodes+textboxes: nodes revert, textboxes commit their translation → the group's relative layout silently corrupts, with ONE history entry that undo can't cleanly revert to the pre-drag state for the node members (they never wrote). No test exercises tool-hotkey-mid-drag.*
- **(canvas-interaction.md §8.1 + known_issues D-9)** One keystroke reverts exactly one logical action across both history stacks (sequence stamping); scene history is global but the scene store holds only the current view. → *Edit on Page 1, switch to Page 2, Ctrl+Z: the scene patch applies to Page 2's scene (phantom/stale scene.connectors[id]) while the model undo reverts Page 1 — documented open (D-9) with NO e2e repro committed. Any new page-switch path (e.g. cross-diagram link navigation within a file) widens the window.*
- **(ADR 0021 item 7 (D-8 fix) + D-9)** After undo/redo, resyncScene re-routes connectors with missing/empty paths — but only in the ACTIVE view. → *Paste connectors on Page 1 → switch to Page 2 before async routing completes → undo/redo: resyncScene scans only the active view, so Page 1's pasted connectors stay pathless (invisible) until a later edit touches them. useHistory tests exercise single-view scenarios only.*
- **(ADR 0022 addendum (arrow keys) + ADR 0006)** Arrow keys nudge a selected ITEM/RECTANGLE/TEXTBOX one tile per press as a single-undo transaction; with nothing selected they pan. → *Arrow-nudge a selected floating LABEL or an off-grid (offset) item: Labels are absent from the enumerated nudge types (the recurring wired-into-some-surfaces bug), and nudging an off-grid item by one integer tile while keeping its offset may collide-check against the wrong cell. No test nudges a Label or an offset item.*

## Known coverage gaps (from the baseline inventory)

- (Undo/redo / clipboard / hotkeys / modes) Undo of a DELETE that restores an item together with its attached connectors
- (Undo/redo / clipboard / hotkeys / modes) Undo of resize, rotate, z-order, or style operations
- (Undo/redo / clipboard / hotkeys / modes) Redo-stack invalidation after undo-then-new-action
- (Undo/redo / clipboard / hotkeys / modes) Paste POSITION semantics (only counts asserted, not where the paste lands)
- (Undo/redo / clipboard / hotkeys / modes) Copy/paste of a multi-item selection or across diagrams
- (Undo/redo / clipboard / hotkeys / modes) History depth cap / memory behavior on long sessions
- (Lib store reducers + zustand stores) packages/axoview-lib/src/stores/historySequence.ts — zero direct tests
- (Lib store reducers + zustand stores) packages/axoview-lib/src/stores/localeStore.tsx — zero tests (translation lookup/namespace fallback)
- (Lib store reducers + zustand stores) packages/axoview-lib/src/stores/uiStateStore.tsx — only indirectly tested via multiSelect.contract; most UI-state actions untested directly
- (Lib hooks) zero-test hooks in packages/axoview-lib/src/hooks/: useDiagramUtils.ts (fit-to-screen), useDirtyTracker.ts, useConnector.ts, useSceneActions.ts, useSceneData.ts, useModelItem.ts, useViewItem.ts, useView.ts, useRectangle.ts, useTextBox.ts, useTextBoxProps.ts, useLabel.ts, useLayerActions.ts, useLayerContext.ts, useIcon.tsx, useIconCategories.ts, useIconFiltering.ts, useImageAspect.ts, useKeyboardIconPlacement.ts, useCanvasModeToggle.ts, useColor.ts
- (Lib hooks) useScene.ts is covered only via __perf_refactor_regression__ list-shape/reference-stability tests; pasteItems/action surface has no direct unit tests outside paste.bulkPerf
- (Lib perf/refactor regression suite (__perf_refactor_regression__)) many contracts here are source-text greps rather than behavior (exportImageDialog.initialLoad, f2.rendererScope, interactionManager.depStability, saveTracking, viewTabs, i18n, toolMenu/quickIconSelector string bans) — they pin implementation text, not runtime behavior; renames could silently weaken them

## Hypotheses

Proposed 2026-07-29 (all 15 recorded before probing began, per APPROACH §1).

| ID | Hypothesis | Source | Nearest existing tests | Probe | Verdict | Evidence |
|----|-----------|--------|------------------------|-------|---------|----------|
| HIST-01 | `useLayerActions.commit()` calls `modelStore.saveToHistory()` **without** `allocateHistorySequence()` and never calls `sceneStore.saveToHistory()`, so a layer op's model entry is stamped with the PREVIOUS action's seq. After `create connector → toggle layer visibility → Ctrl+Z`, `useHistory.undo` sees equal seqs and pops the layer model entry AND the connector's older scene entry together, leaving a model connector with no scene path (the D-7 invisible-connector symptom, on a surface the D-7 fix missed) | bug-class recurrence (sibling drift / missed wiring) | `layer.test.ts` (reducer only, no history); `undo.dualStackSkew.test.tsx` (place-icon interleave, not layer ops); `useLayerActions.ts` has ZERO tests | `__explore__/hist-01-04.explore.test.tsx` | BUG | Confirmed. Layer entry stamped with the previous action's seq (equal seqs asserted); one undo pops model+scene. Text-box case: `scene.textBoxes[tb-1]` lost on the layer undo and never repaired (resyncScene handles connectors only). Connector case: repaired, but the short scene stack makes the NEXT undo leave an orphan `scene.connectors[id]`. Filed: known_issues "Layer / z-order ops inherit the previous action's history sequence". *(Evidence re-verified 2026-07-29 with `installCanvasStub()`: the text-box legs originally ran without a canvas measurer, so `createTextBox` threw during setup and the `it.failing` bodies failed for the wrong reason. Re-run with a working measurer — verdict unchanged, and a characterization test now positively pins "text box in model, scene size gone".)* |
| HIST-02 | Cross-store redo invalidation gap: a new *model-only* action after an undo clears `model.history.future` but the scene store's `set()` early-returns on `patches.length===0`, so `scene.history.future` survives. `canRedo` stays true and Redo re-applies a stale scene patch → orphan `scene.connectors[id]` with no backing model connector (INV-3) | bug-class recurrence (dual-stack skew) + baseline gap "redo-stack invalidation after undo-then-new-action" | `useHistory.realStore.test.tsx` #3 (model store only, single stack); `connector.createUndoRedo.test.tsx` (asserts canRedo true, never redoes after a new action) | `__explore__/hist-02-03.explore.test.tsx` | BUG | Confirmed. After draw-connector → undo → place-icon: `model.future` cleared, `scene.future` still length 1; `canRedo` true; Ctrl+Y writes back `scene.connectors[conn-1]` with no model connector (INV-3 orphan). Filed: known_issues "Redo stays armed on the scene stack…" |
| HIST-03 | `MAX_HISTORY_SIZE` trim asymmetry: 50 model-only actions after one dual-store action evict the model half of seq N while the scene half survives at the stack bottom; the 51st Ctrl+Z steps the scene stack alone, reverting scene state for an action whose model half no longer exists. (known_issues D-7 claims this sub-case "is resolved by the same fix" — falsify the claim) | ADR/known-issue contract violation | `useHistory.realStore.test.tsx` #4 (model cap only); `sceneStore.test.ts` cap test (scene only); neither crosses the two stacks | `__explore__/hist-02-03.explore.test.tsx` | BUG | Confirmed, and it falsifies the D-7 entry's claim that the trim sub-case "is resolved by the same fix". After 1 text-box create + 50 place-icons, the model half of the shared seq is evicted while the scene half survives; draining the stacks reverts `scene.textBoxes[tb-1]` alone → text box with no size (INV-5b), unrepaired (resyncScene covers connectors only — a passing characterization test pins that masking). Filed: known_issues "Independent 50-entry history trimming…". *(Text-box leg re-verified with `installCanvasStub()` — see the note on HIST-01; verdict unchanged, characterization added.)* |
| HIST-04 | `useSceneActions.createView` never calls `saveToHistoryBeforeChange` and writes `skipHistory=true`, so creating a page pushes NO history entry — Ctrl+Z straight after "New page" silently reverts the *previous* action while the new page stays (and `deleteView` IS undoable, so the pair is asymmetric) | parity oracle (create vs delete) | `view.test.ts` CREATE_VIEW (reducer only); `viewOps.integration.test.tsx` (CRUD, no history assertions) | `__explore__/hist-01-04.explore.test.tsx` | BUG | Confirmed. `createView` leaves `history.past` depth unchanged (1→1); after rename→new-page, one undo reverts the rename and keeps the page. `deleteView` records an entry, so the pair is asymmetric. Filed: known_issues "Creating a page is not undoable…" |
| HIST-05 | Orphaned pendingPre: a reducer throw between `saveToHistoryBeforeChange()` and `setState()` leaves pendingPre armed in both stores; the next `skipHistory` write (`changeView`'s SYNC_SCENE on a page switch) consumes it and pushes a bogus entry stamped with a stale seq, so a later Ctrl+Z reverts a diff the user never made | seed seam #1 | none — no test drives a throwing reducer through `useSceneActions` | `__explore__/hist-05-08.explore.test.tsx` | BUG | Confirmed. A throwing `updateViewItem` records nothing itself but leaves `pendingPre` armed on both stores; the following `switchView` (SYNC_SCENE, written skipHistory) then adds one entry to `scene.history.past`. Filed: known_issues "A failed edit arms the undo snapshot…" |
| HIST-06 | Leaked drag bracket: `beginDragTransaction` without `commitDragTransaction` leaves `pendingPreFrozen`/`dragInProgress` set, so every later edit applies with no history entry while `canUndo()` still returns true — Ctrl+Z then reverts the pre-drag action, producing a model state that never existed | invariant harvest (canvas-interaction §6.2) | `connector.dragPerf.test.tsx` (happy 40-tick path, always commits); `DragItems.modes.test.ts` (mode exit always commits) | `__explore__/hist-05-08.explore.test.tsx` | BUG | Confirmed. After begin-without-commit, two further edits add 0 history entries while `canUndo` stays true; the next undo replaces the whole `views` array and destroys both un-recorded edits (rename reverts to `View`). Also pins that patches are whole-subtree replaces, not fine-grained diffs. Filed: known_issues "A leaked drag bracket…" |
| HIST-07 | `dragInProgress` is a per-hook-instance ref: an edit issued through a *different* component's `useSceneActions` while a drag is open calls `saveToHistory()`, overwriting the frozen pre-drag snapshot with mid-drag state; the drag's commit entry then diffs from mid-drag, so undo restores the item to a mid-drag position rather than its origin | seed seam #6 | none — every drag test uses one hook instance | `__explore__/hist-05-08.explore.test.tsx` | BUG | Confirmed. With two hook instances under one provider pair, a foreign `updateView` mid-drag re-arms `pendingPre`; undo leaves node-A at the mid-drag tile (2,2) instead of its origin (0,0). Filed: known_issues "A mid-drag edit from another component…" |
| HIST-08 | `useHistory.transaction` and `useSceneActions.transaction` keep separate `transactionInProgress` refs, so N scene CRUD ops wrapped in `useHistory.transaction` push N history entries instead of 1 and one Ctrl+Z undoes only the last op | seed seam #6 | `useHistory.realStore.test.tsx` #6/#7 (transaction over raw `model.actions.set`, never over `useSceneActions` CRUD) | `__explore__/hist-05-08.explore.test.tsx` | SUSPECT | Confirmed mechanically — 3 scene CRUD ops inside `useHistory.transaction` push 3 entries (control: `useSceneActions.transaction` pushes 1), and one Ctrl+Z removes only the last. NOT filed as a bug: `useHistory().transaction` has zero product callers (only `undo/redo/canUndo/canRedo` are consumed), so it is a broken-contract dead API. Product question below. |
| HIST-09 | D-9 cross-view undo: an edit on page 1 followed by a page switch and Ctrl+Z applies page 1's scene patch to page 2's scene (phantom/stale `scene.connectors[id]`, INV-3) | known_issues D-9 (code-traced, "e2e repro to be added") | `undo-redo-cross-cutting.spec.ts` + `undo-redo-dual-stack.spec.ts` — both single-page | `tests-exploratory/E1-history/hist-09-10.explore.spec.ts` | BUG (known) | Confirmed — first committed repro for D-9. Create page 2, work on page 1 (3 nodes + 2 connectors), switch to page 2, Ctrl+Z: page 2 owns 0 connectors but its scene cache holds a page-1 path (INV-3 orphan). No new known_issues entry; the existing D-9 residual under the D-7 heading now cites this spec. |
| HIST-10 | Undo of `deleteView` restores the page in the model but nothing restores `ui.view`, so Ctrl+Z after deleting the ACTIVE page appears to do nothing — the canvas keeps showing the fallback page and the restored page is only reachable by clicking its tab | seed seam #7 | `viewOps.integration.test.tsx` (no ui.view assertions); no e2e deletes a page and undoes | `tests-exploratory/E1-history/hist-09-10.explore.spec.ts` | SUSPECT | Prediction confirmed: after deleting the active page and pressing Ctrl+Z the page returns to `model.views` but `ui.view` stays on the fallback page — the canvas does not change (the tab does reappear). Whether undo should restore the active page is a product call — question below. |
| HIST-11 | Undo of a `Ctrl+]` z-order change does not restore the original `zIndex` (REORDER_VIEWITEM renumbers, so the recorded inverse patch is computed against a renumbered array) | baseline gap "undo of z-order operations" | `z-order.spec.ts` (asserts increment/decrement, never undo); `layer.test.ts` reorderViewItem (reducer only) | `tests-exploratory/E1-history/hist-11-13.explore.spec.ts` | FALSIFIED | Prediction wrong — Ctrl+] then Ctrl+Z restores every item's `zIndex` exactly, invariants clean. Note the flow dodges HIST-01: the preceding action (place icon) is model-only, so the mis-stamped z-order entry has no scene half to drag down with it. Interleaving a z-order change AFTER a connector/text-box edit is the HIST-01 repro. |
| HIST-12 | Undo of a Delete that cascaded connectors restores item + connectors in the model but leaves the connectors without scene paths (invisible), because the cascade's scene deletions and `resyncScene`'s guard interact wrongly | baseline gap "undo of a DELETE that restores an item together with its attached connectors" | `viewItem.test.ts` cascade (reducer only, no undo); `hotkeys.spec.ts` Delete (no undo leg) | `tests-exploratory/E1-history/hist-11-13.explore.spec.ts` | FALSIFIED | Prediction wrong — deleting a node cascades its connector away and Ctrl+Z brings both back with a routable scene path (no pathless connector, invariants clean). The cascade's scene deletions are recorded in the scene entry, so the undo restores them directly; `resyncScene` is not even needed. |
| HIST-13 | After a Delete, `ui.selectedIds` still references the deleted item (INV-2); Ctrl+Z→Ctrl+Y round-trips the model but leaves selection pointing at a re-deleted id | cross-store invariant (INV-2) | `multiSelect.contract.test.ts` (slice coherence, no delete/undo); `hotkeys.spec.ts` Delete (count only) | `tests-exploratory/E1-history/hist-11-13.explore.spec.ts` | BUG | Confirmed by the shared oracle: `expectStoreInvariants` trips INV-2 immediately after Delete — `selectedIds` still holds `{type:'ITEM', id:<deleted>}` and keeps it through undo and redo, until the next click. Feeds the HIST-05 throw path. Filed: known_issues "Deleting a selected item leaves it selected…" |
| HIST-14 | Off-grid × history: the drag commit writes `offset: undefined` for re-snapped items and immer drops patches for properties set to `undefined`, so undo of a snap-back does not restore the original off-grid `offset` (offset-omission bug class applied to the history surface) | bug-class recurrence (offset omission) | `offGrid.ts` helpers + off-grid e2e (no undo leg); `rectangleTextbox.dragPerf.test.tsx` (entry counts only) | `__explore__/hist-14-15.explore.test.tsx` | FALSIFIED | Prediction wrong — the history surface handles `offset` correctly. A drag that clears `offset` undoes with tile AND offset restored, and undo→redo→undo is idempotent on both. Mechanism: entries carry whole-subtree `replace` patches (see HIST-06 evidence), so an `undefined` property is never a dropped patch. Off-grid is safe here. |
| HIST-15 | Depth cap silently loses the base state: after >50 edits, pressing Ctrl+Z until `canUndo()` is false leaves the model in a state that is NOT the pre-edit state, with no user-visible signal that history was truncated | boundary / baseline gap "history depth cap on long sessions" | `useHistory.realStore.test.tsx` #4 (asserts `past.length===50`, never the resulting model) | `__explore__/hist-14-15.explore.test.tsx` | SUSPECT | Prediction confirmed: after 60 edits, undoing until `canUndo` is false leaves 10 edits applied (62 → 12 items, not the original 2) with no user-visible signal. Whether a silent partial undo is acceptable is a product call — question below. |

## Product questions (SUSPECT verdicts)

- **HIST-08 — should `useHistory().transaction` be fixed or deleted?** It advertises
  "groups multiple operations into single history entry", but it only guards its
  own `transactionInProgress` ref; `useSceneActions`' write helpers keep their own
  ref, so each op inside the wrapper still allocates a sequence, re-arms
  `pendingPre` and pushes its own entry (probe: 3 ops → 3 entries; one Ctrl+Z
  removes only the last). No product code calls it — `ToolMenu` and
  `useInteractionManager` consume only `undo/redo/canUndo/canRedo` — so this is a
  latent trap rather than a live bug. Either delete it, or make it delegate to
  `useSceneActions.transaction` (the one that works).
- **HIST-15 — should a truncated undo stack be surfaced?** With `MAX_HISTORY_SIZE=50`
  per store, a session of 60 edits that is undone until `canUndo()` reports false
  still has the first 10 edits applied — the document silently stops short of its
  original state and nothing tells the user. Options: surface "older changes can no
  longer be undone", raise or make the cap configurable, or accept as-is and document it.
- **HIST-10 — should undoing a page delete take you back to that page?** `uiState.view`
  is in neither history stack, so Ctrl+Z after deleting the page you were on
  restores it to the model while leaving you on the fallback page: the tab
  reappears but the canvas does not change. The same gap makes an undone page
  *creation* leave `ui.view` dangling (see HIST-04's e2e evidence, filed).

**Next:** area closed 2026-07-29 — all 15 proposed hypotheses resolved (9 BUG incl. 1 known, 3 SUSPECT, 3 FALSIFIED), nothing DEFERRED. The strongest unexplored seam left behind for a later wave: the coarse patch granularity discovered while probing HIST-06 (every entry's inverse patch replaces the whole `views` array) — E3/E4 should ask what else that clobbers, e.g. an async write landing between an edit and its undo.
