# S1 — Google identity & token lifecycle (GIS auth store, gates)

**Status:** DONE · **Counted hypotheses:** 16 / 10 · **Bugs:** 12 · **Suspects:** 1 · **Hypothesis ID prefix:** `AUTH-`

> Overlaps A5 boot-chrome auth seams — dedupe across ledgers. Live-OAuth-only seams get DEFERRED with a manual-test sketch.

**Scope:** In-memory GIS implicit-grant token (~1h, no refresh token) behind a zustand state machine: UNAUTHENTICATED / AUTHENTICATING / RECONNECTING / AUTHENTICATED / REFRESHING / SESSION_EXPIRED / DRIVE_ACCESS_REQUIRED. getValidToken() is the sole token accessor: returns current token if >5min to expiry, else fires a silent prompt:'' refresh; concurrent callers piggyback on a shared waiter list. Handles granular-consent (drive.file checkbox unchecked → DRIVE_ACCESS_REQUIRED hard stop), boot silent reconnect from a localStorage profile hint with login_hint, stale-error absorption when an interactive sign-in supersedes a silent request, and a global auth timeout (COOP-blocked popup recovery).

**Code:**
- `packages/axoview-app/src/stores/authStore.ts`
- `packages/axoview-app/src/components/DriveAccessRequiredDialog.tsx`
- `docs/adr/0035-google-identity-and-drive-authorization.md`

**Dedupe baseline:** [coverage-baseline.md](../coverage-baseline.md) sections — E2E: *Share + Drive display*; Unit: *App auth + notification stores*, *Cloudflare Worker API + auth*. Then grep the suites directly.

## Seed seams (2026-07-29 mapping — starting points, not a ceiling)

- Token expiry mid-operation: a multi-request flow (GoogleDriveProvider retry loop, driveTransfer move loop, permissions.list page drain) calls getValidToken per request — a refresh that fails mid-sequence flips to SESSION_EXPIRED and the remaining requests throw 401 DriveError; check partial-state cleanup (e.g. driveTransfer created-on-Drive-but-not-yet-deleted-locally).
- Single global pendingAuthTimeout slot: grantDriveAccess() can fire while a REFRESHING request is in flight (it doesn't check status) — it re-arms the timer and sets _absorbStaleError:false, so the superseded refresh's late error is treated as the consent popup's own cancellation, resetting AUTHENTICATING under the user mid-consent.
- Waiter resolution on scope-less grant: _onToken's DRIVE_ACCESS_REQUIRED branch resolves getValidToken piggybackers via w.resolve() → they read accessToken=null and return null — callers see 'Not signed in' errors concurrently with the blocking re-consent dialog (double error surfaces).
- signOut vs in-flight Drive writes: ADR 0035 rule 3 (flush before revoke) is enforced only at AuthControl's call site — DriveDisplayGate.handleSwitchAccount calls signOut() directly; any autosave awaiting getValidToken resolves null and the write is silently dropped.
- Account switch leaves per-account caches: fresh grant re-fetches the profile, but localStorage axoview-drive-root (GoogleDriveProvider root cache) and axoview.recentShareEmails persist across accounts — folderExists() will 404 account A's root under account B's token and heal, but the in-memory this.rootFolderId short-circuits resolveRoot() without revalidation for the life of the page.
- markExpired() from a 401 during REFRESHING/RECONNECTING: it doesn't guard on status, so a late Drive 401 landing while a fresh sign-in is AUTHENTICATING would... (it does check status===SESSION_EXPIRED only) — it wipes _waiters of the in-flight interactive sign-in, rejecting the promise the sign-in button is awaiting.
- (mapper note) State machine is probe-able WITHOUT live Google: stores/__tests__ drive it via _setBridge with a fake requestToken/_onToken/_onError. Real GIS behaviors (COOP popup blocking, fedcm, interaction_required with multiple accounts, actual 1h expiry) need live auth. In e2e, needs-signin walls are reachable unauthenticated; anything past the sign-in button is not.

## Related harvested invariants (keyword-matched hint list — scan the [full table](../coverage-baseline.md#invariants) too)

- **(ADR 0037 §2 (active provider follows open diagram))** Switching the open diagram to another place flushes the pending autosave to the OLD place before setting the new active provider. → *With a dirty session diagram (debounced autosave pending), immediately open a Drive diagram: if the flush is fire-and-forget rather than awaited before the provider swap, the session autosave either writes through the Drive provider (wrong place) or is cancelled (silent data loss). driveTransfer/authStore tests don't cover the open-diagram provider-swap flush ordering.*
- **(ADR 0037 §5 (move semantics))** Move-to-Drive is create → verify returned id → only then delete from source; a failed item stays in session and is reported; moving the OPEN diagram reopens it from its new Drive id. → *Move the open dirty diagram: create+verify succeed, source delete succeeds, then the reopen-from-Drive fetch fails (token expired mid-flow): the canvas still shows the old session-backed state with a currentDiagram id that no longer exists anywhere → next autosave targets a deleted id. driveTransfer.test covers create/verify/delete ordering, not the reopen leg.*
- **(ADR 0036 §2 + known_issues (root-folder detection))** ADR 0036 promises the provider detects a deleted/trashed Drive root folder; as-built, isAvailable() only checks auth and the cached root id is never revalidated. → *Trash the app folder in Drive's own UI mid-session: autosaves keep 200-OK patching files in the trash for the rest of the session; loss surfaces only at next full listing. The cheap fix (invalidate on zero-listing or 404) is catalogued but unimplemented — any test asserting 'save succeeded ⇒ durable' is false here.*
- **(ADR 0035 / authStore.test.ts)** The Google token is NEVER persisted — only the identity/profile hint survives reloads; silent reconnect re-mints via GIS. → *The regression test spies on localStorage.setItem only. A convenience change that stashes the token in sessionStorage, IndexedDB, or a cookie (e.g. to survive the popup-blocker boot problem) evades the spy entirely and ships green while violating the ADR's central security contract.*
- **(ADR 0029 + sanitizeHtml hook)** User-authored HTML is sanitized before the single dangerouslySetInnerHTML sink, and the sanitizer forces rel='noopener noreferrer' on every anchor with href. → *The rel-forcing hook lives inside sanitizeHtml — link surfaces built directly in React (view-mode popover headerLink, connector-label link chips, TextBoxLinkCard's 'open in new tab') get target=_blank from their own JSX; any of them omitting rel=noopener reintroduces reverse-tabnabbing on user-supplied URLs, invisible to the sanitizer tests which only cover the HTML path.*
- **(ADR 0042 §2 + worker app.ts (resourceKey fix))** The public Drive read-proxy validates fileId and (since 2026-07-29) resourceKey on the allowlist /^[A-Za-z0-9_-]{10,120}$/, with a malformed resourceKey DROPPED so 'no valid request regresses'. → *Google resource keys can be shorter than 10 chars or contain other legal characters — a legitimate short resourceKey now gets silently dropped, so an 'anyone with the link' file that REQUIRES the key returns 404 from Drive and the viewer hits the auth-gate ladder with no hint. The worker test verifies the drop behavior, not that real-world resourceKey formats pass the same regex as fileIds.*
- **(canvas-interaction.md §2 (isRendererInteraction gate))** mousedown/mouseup gate on isRendererInteraction; mousemove deliberately does NOT — any move to scoped listeners must replace window-binding with setPointerCapture or drags break when the cursor leaves the box. → *A drag that strays over a NEW overlay child that stops propagation (a future minimap, the annotation palette when open in edit mode): moves keep flowing (window-bound) but the mouseup lands gated-out if the overlay swallows it → the drag never commits and DRAG_ITEMS stays armed, committing on the NEXT unrelated mouseup. No test releases a drag over each overlay surface.*

## Known coverage gaps (from the baseline inventory)

- (Share + Drive display) Re-sharing the same diagram (same UUID vs new link)
- (Share + Drive display) Share link revocation/expiry
- (Share + Drive display) Interactions inside the shared readonly view (zoom/pan/layer switching)
- (Share + Drive display) Signed-in (OAuth) Drive rung — only the anonymous proxy path is tested
- (Share + Drive display) Sharing a diagram that links to other diagrams (link behavior in shared view)
- (App auth + notification stores) packages/axoview-app/src/stores/diagnosticsStore.ts — zero tests

## Hypotheses

Novelty baseline for the whole block: `stores/__tests__/authStore.test.ts` (22 tests — read in full), `services/storage/__tests__/GoogleDriveProvider.test.ts` (18 tests), `components/__tests__/DriveAccessRequiredDialog.test.tsx` (2 tests), `tests/drive-display.spec.ts` (anonymous proxy rung only). Nothing in any of them drives `markDriveScopeMissing`/`markExpired`/`grantDriveAccess` **while a GIS request is in flight**, and nothing asserts a consumer-side consequence of a store state (AuthControl gating, provider 403 routing).

| ID | Hypothesis | Source | Nearest existing tests | Probe | Verdict | Evidence |
|----|-----------|--------|------------------------|-------|---------|----------|
| AUTH-01 | `markDriveScopeMissing()` settles nothing (no `_waiters` drain, no `clearAuthTimeout`) where its sibling `markExpired()` does both — a scope-403 landing while a silent refresh is in flight leaves every piggybacked `getValidToken()` promise **unsettled forever**, so the awaiting Drive write hangs with no error and no toast | sibling drift / seed seam 2 | `markExpired settles an in-flight refresh` (asserts the sibling, never this one); `markDriveScopeMissing parks an authenticated session` (no in-flight request) | `__explore__/S1/auth-01-02-04.explore.test.ts` | BUG | The waiter survives `markDriveScopeMissing` (1, not 0) and the `getValidToken` promise never settles — not on a late `_onToken`/`_onError` (both bail on the status guard) nor on the armed 25 s silent timeout (same guard). The `markExpired()` CONTROL on the identical setup settles it to `null`. known_issues: *A Drive scope-403 during a token refresh hangs the awaiting write forever* |
| AUTH-02 | `grantDriveAccess()` hardcodes `_absorbStaleError: false` where `signIn()` derives it from the prior status — so a superseded silent request's late error is taken for the consent popup's own cancellation: UNAUTHENTICATED + "Sign-in cancelled" mid-consent, and the real grant that follows is discarded by `_onToken` | mode-transition race / seed seam 2 | `a late silent-reconnect error cannot cancel an interactive signIn` (covers `signIn`, not `grantDriveAccess`) | `__explore__/S1/auth-01-02-04.explore.test.ts` | BUG | After `markDriveScopeMissing` + `grantDriveAccess`, `_absorbStaleError === false`; the superseded refresh's error takes the else branch → UNAUTHENTICATED + info "Sign-in cancelled", and the consent grant that follows is discarded (`accessToken` null). CONTROL: the same chain driven through `signIn()` absorbs the error and reaches AUTHENTICATED. known_issues: *"Grant Drive access" can end in a signed-out session with a "Sign-in cancelled" toast* |
| AUTH-03 | The stuck-popup timeout is defeated by its own absorb flag: while `_absorbStaleError` is set, the synthesised timeout `_onError` is **absorbed** and nothing re-arms the timer → the store spins in AUTHENTICATING forever (permanent toolbar spinner), the exact failure the timeout exists to prevent | ADR 0035 §3 amendment 2 contract | `a stuck request times out and recovers` (no superseded silent request in flight) | `__explore__/S1/auth-03-07.explore.test.ts` | BUG | With a boot reconnect superseded by an interactive `signIn()`, `_absorbStaleError` is armed; the 120 s safety net's synthetic `_onError` hits the absorb branch, which returns before `clearAuthTimeout()` — and the timer callback already nulled itself, so nothing re-arms. Status stays AUTHENTICATING after a further simulated hour, both waiters unsettled (AuthControl's bare spinner, reload-only recovery). CONTROL: the same timeout on a plain `signIn()` recovers to UNAUTHENTICATED. known_issues: *The stuck-popup auth timeout is swallowed by the stale-error absorber* |
| AUTH-04 | `markExpired()` has no in-flight guard: a stale Drive 401 landing while the user is completing an interactive sign-in wipes `_waiters`, flips to SESSION_EXPIRED, pushes a second persistent expired toast, and the grant that lands moments later is ignored by `_onToken` → the user signs in successfully and stays signed out | interleaving matrix | `signOut settles an in-flight refresh and a late grant cannot resurrect` (signOut, and resurrection is *desired* there) | `__explore__/S1/auth-01-02-04.explore.test.ts` | BUG | `markExpired()` during AUTHENTICATING → SESSION_EXPIRED, `_waiters` emptied, `signIn()` resolves, one persistent expired toast pushed while the popup is still open — and the granted token is then dropped on `_onToken`'s status guard (`accessToken` stays null). known_issues: *A stale Drive 401 during sign-in discards the sign-in the user just completed* |
| AUTH-05 | A grant whose `userinfo` fetch fails leaves `user === null`, and `AuthControl` gates the whole signed-in menu on `!!user` → an AUTHENTICATED session with a working Drive token renders the *never-signed-in* person icon: no name, no sign-out affordance, and no profile hint for the next boot reconnect | parity / error-path | `a successful grant persists the profile hint` (asserts only the happy path); AuthControl has **no** unit test | `__explore__/S1/auth-05-10-11.explore.test.tsx` | BUG | With `fetch` rejecting, a full-scope grant reaches `status: 'AUTHENTICATED'` with `getValidToken()` returning the live token — and `user === null`, no profile hint. Rendering the real `AuthControl` then falls through to the never-signed-in branch: no `auth-avatar`, `auth-account` instead, and the menu offers "Sign in with Google" with **no** `auth-signout` item. CONTROL: the identical grant with userinfo succeeding renders the avatar and the sign-out item. known_issues: *A userinfo failure makes a working Google session render as signed out, with no way to sign out* |
| AUTH-06 | `_onToken`'s scope-less hard stop resolves the `getValidToken` piggybackers with `w.resolve()`, and they read the just-nulled `accessToken` → `null`, so every in-flight Drive call throws `DriveError('Not signed in to Google', 401)` and its error surface races the blocking DRIVE_ACCESS_REQUIRED dialog | seed seam 3 | `driveScopeGranted tracks the ACTUALLY granted scopes` (calls `getValidToken` *after* the fact, never piggybacked) | `__explore__/S1/auth-06-08-09-14.explore.test.ts` | BUG | A `saveDiagram` parked on the refresh waiter (0 Drive fetches) rejects with `DriveError('Not signed in to Google', 401)` the moment the scope-less grant lands — the hard stop nulls `accessToken` and then calls `w.resolve()`, so the piggybacker reads null. The generic 401 error surface races the blocking DRIVE_ACCESS_REQUIRED dialog, and the caller cannot tell "re-consent needed" from "signed out". known_issues: *A scope-less grant fails in-flight Drive writes as "Not signed in" behind the re-consent dialog* |
| AUTH-07 | `signIn()` is not idempotent — called again while AUTHENTICATING (LocalModeBanner's sign-in button and the persistent expired toast's action are not disabled) it fires a second GIS request and re-arms the timeout, and with `_absorbStaleError` false the first popup's cancellation cancels the second attempt | boundary / degenerate | none — no test calls `signIn()` twice | `__explore__/S1/auth-03-07.explore.test.ts` | BUG | A second `signIn()` during AUTHENTICATING fires a second `requestToken` (2 calls, 2 waiters) with `_absorbStaleError` false, so the first popup's `popup_closed` drops the store to UNAUTHENTICATED + "Sign-in cancelled" and the second popup's grant is then discarded. Reachable: neither `LocalModeBanner`'s sign-in button nor the persistent expired toast's action is disabled on AUTHENTICATING. known_issues: *A second sign-in click opens a second Google popup, and closing the first cancels it* |
| AUTH-08 | `request()` classifies rate-limit vs permanent 403 and then throws `status: 403` for both, and `handleCreateBlankDiagram`'s catch treats **any** DriveError 403 as insufficient scope → an exhausted rate-limit parks a healthy session in DRIVE_ACCESS_REQUIRED and nulls a valid token | bug-class recurrence (over-broad classification) | `a rate-limit 403 IS retried` (asserts the retry, not the thrown status); `a permanent 403 fails fast` | `__explore__/S1/auth-06-08-09-14.explore.test.ts` | BUG | `request()` computes `isRateLimit` and then throws `DriveError(msg, 403)` for both branches, so the retried rate-limit error (4 fetches) is indistinguishable from the fail-fast insufficient-scope error (1 fetch) at the throw site. `handleCreateBlankDiagram`'s only predicate is `name === 'DriveError' && status === 403`, so an exhausted rate limit parks a healthy session in DRIVE_ACCESS_REQUIRED and nulls a valid token. known_issues: *An exhausted Drive rate limit is mistaken for a missing scope and parks the session* |
| AUTH-09 | Conversely the provider never routes an insufficient-scope 403 to `markDriveScopeMissing()` — only `handleCreateBlankDiagram` does — so a scope revoked out-of-band surfaces as a generic failure on save, load, list and rename, and the DRIVE_ACCESS_REQUIRED recovery ladder is unreachable from every path but "create blank diagram" | sibling drift | `401 forces SESSION_EXPIRED and throws` (the 401 twin is wired; the 403 twin is not tested) | `__explore__/S1/auth-06-08-09-14.explore.test.ts` | BUG | An insufficient-scope 403 on `saveDiagram` leaves `status === 'AUTHENTICATED'` and `driveScopeGranted === true` — the provider wires the 401→`markExpired()` twin but not the 403→`markDriveScopeMissing()` one, so the re-consent ladder is reachable only from `handleCreateBlankDiagram`. Save, load, list, rename and the manifest all dead-end in a generic toast. known_issues: *A revoked Drive scope only reaches the re-consent dialog from "new diagram"* |
| AUTH-10 | AuthProvider attaches its one-shot gesture-retry listeners inside a `.then()` that resolves *after* the effect cleanup already ran and nulled `gestureCleanupRef`, so under StrictMode's double-invoke (dev default in `index.tsx`) the first run's `pointerdown`/`keydown` capture listeners are never removed | anomaly / lifecycle | none — AuthProvider has no test | `__explore__/S1/auth-05-10-11.explore.test.tsx` | FALSIFIED | No leak. RIG check first confirms StrictMode really double-invokes effects here (an effect body runs twice), so this is a verdict and not a rig artifact. Measured on unmount: `removeEventListener('pointerdown', retry, true)` IS called and a post-unmount `pointerdown` fires no GIS request — identical to the non-StrictMode CONTROL. Reason: the `!scriptReady` guard makes the *arming* pass an update (`scriptReady` false→true, driven by the provider's `onScriptLoadSuccess` effect, which runs after its children's), and StrictMode double-invokes only the mount pass — so the cleanup React retains is the live one. |
| AUTH-11 | `DriveDisplayGate.handleSwitchAccount()` calls `signOut()` (clearing `user` + the hint) *before* `signIn()`; cancelling the chooser therefore leaves the viewer strictly worse off than before — the gate loses the email it was explaining with and the avatar's reconnect affordance | cross-feature pair | `drive-display.spec.ts` covers the anonymous rung only | `__explore__/S1/auth-05-10-11.explore.test.tsx` | BUG | Clicking "Use a different Google account" in the no-Picker branch runs `signOut()` first: `user` → null and the profile hint is removed while the chooser is still open. Driving `_onError('popup_closed')` then leaves UNAUTHENTICATED with the gate's copy no longer naming the account (`personal@example.com` gone from the DOM), the avatar's `needsReconnect` affordance false, and `retryDriveDisplayRead` never called — strictly worse than before the click. known_issues: *Cancelling "Use a different Google account" signs the viewer out of the account that was working* |
| AUTH-12 | `markDriveScopeMissing()` keeps the profile hint that `_onToken`'s identical hard stop clears, so a mid-session scope revocation followed by a reload re-arms the boot reconnect straight back into the dead state | sibling drift | `a reconnect returning identity-only … forgets the account` (asserts the `_onToken` twin only) | `__explore__/S1/auth-12-13-15-16.explore.test.ts` | BUG | `markDriveScopeMissing()` leaves `axoview-google-profile` intact where `_onToken`'s identical hard stop calls `clearProfileHint()` "so a reload lands on a clean signed-out state instead of looping back into this prompt". A fresh module load (page-reload stand-in) is remembered + UNAUTHENTICATED, so AuthBridge re-arms the silent reconnect with the hint and the still-scope-less grant lands back in DRIVE_ACCESS_REQUIRED. CONTROL: the `_onToken` twin on the same condition clears the hint. known_issues: *A mid-session Drive scope loss keeps the account remembered, so a reload walks back into the blocking dialog* |
| AUTH-13 | `loadProfileHint()` accepts `{name: ''}`, so an empty-name hint yields a truthy `user` with no email: `attemptSilentReconnect()` fires the hint-less `prompt:''` the login_hint exists to avoid, and AuthControl renders a `?` avatar for an account it cannot name | boundary | none — the hint's validation is never probed with degenerate values | `__explore__/S1/auth-12-13-15-16.explore.test.ts` | SUSPECT | Confirmed as behaviour, recorded as a product question rather than a bug. `loadProfileHint()` type-checks only `name`, so `{name:''}` yields a truthy "remembered" user that renders a `?` avatar with an amber reconnect dot, and `{name:'Igor', email:''}` — reachable because `fetchUserInfo` persists `email: data.email &#124;&#124; ''` — arms `attemptSilentReconnect()` with a bare `{prompt:''}`, the hint-less request the `login_hint` exists to avoid. CONTROL: an absent or non-string `name` IS rejected, so validation is not simply missing — it just stops one field short. Recommendation in the product-questions list. |
| AUTH-14 | `isAvailable()` returns false during REFRESHING while `getValidToken()` treats REFRESHING as usable and `AuthControl` renders it as signed-in — a three-way disagreement about "is Drive up?" | parity oracle | `LocalStorageProvider.test.ts` asserts its own `isAvailable` | `__explore__/S1/auth-06-08-09-14.explore.test.ts` | FALSIFIED | Confirmed as a real three-way disagreement (`isAvailable()` false / `getValidToken()` piggybacks / AuthControl signed-in during REFRESHING) but **inert**: `GoogleDriveProvider.isAvailable()` and its `StorageManager.isAvailable()` wrapper have no caller anywhere in `src/`. Filed nothing per the "true but unreachable" rule; the probe pins the grep contract so a future caller re-opens it. |
| AUTH-15 | ADR 0035 rule 1 ("the token never touches localStorage/sessionStorage/IndexedDB") is only test-enforced for `localStorage.setItem` — the token reaches `sessionStorage`, `document.cookie` or IndexedDB through some path the spy cannot see | ADR-contract / persistence sweep (harvested invariant) | `the access token is never written to localStorage` (one sink of four) | `__explore__/S1/auth-12-13-15-16.explore.test.ts` | FALSIFIED | Contract holds across all four sinks, not just the one the regression test spies on: after a full grant + `getValidToken()` + `signOut()`, the token string appears in no `localStorage` value, no `sessionStorage` value, and `document.cookie` is byte-identical to before; no IndexedDB global is even touched. The probe pins the widened invariant so a future "survive the popup blocker" convenience change cannot pass by choosing a different sink. |
| AUTH-16 | `signOut()` clears the profile hint but neither the `axoview-drive-root` cache nor `GoogleDriveProvider.rootFolderId`, so a second account inherits account A's root: the in-memory id short-circuits `resolveRoot()` for the life of the page, past the `folderExists()` healing that the cache path gets | stale derived cache / seed seam 5 | `root discovery: adopts an existing marker folder`, `configureRoot adopts + renames` (single-account only) | `__explore__/S1/auth-12-13-15-16.explore.test.ts` | BUG | After account A resolves its root (cache hit + one `folderExists()` validation) and `signOut()` runs, both `axoview-drive-root` and the provider's in-memory `rootFolderId` still read `root-of-account-A`. Account B's first `listDiagrams(null)` then issues exactly one request — `'root-of-account-A' in parents` under `Bearer token-B`, with no revalidation — and returns `[]`; `createDiagram` POSTs `"parents":["root-of-account-A"]`. The in-memory id short-circuits `resolveRoot()` past the healing the localStorage path would have got. known_issues: *Signing in as a second Google account reuses the first account's Drive root folder id* |

## Product questions (SUSPECT verdicts)

**AUTH-13 — should a profile hint with no usable email arm the boot reconnect at all?**

As-built, `loadProfileHint()` type-checks only `name`, so two degenerate hints pass:
`{name: ''}` (a truthy "remembered" account that renders a `?` avatar with an amber
reconnect dot and cannot be named anywhere in the UI) and `{name: 'Igor', email: ''}`
— the reachable one, because `fetchUserInfo` persists `email: data.email || ''`, so
any userinfo response lacking an email writes it. The email-less hint arms
`attemptSilentReconnect()` with a bare `{prompt: ''}`, which is precisely the
hint-less request `login_hint` exists to avoid: it fails with "interaction
required" in any multi-account browser, and because the boot reconnect degrades
*quietly* by design, the user just sees a permanently signed-out avatar with no
diagnostic beyond the `authDebug` line.

Nothing in ADR 0035 says what a partial hint should mean, and the validator clearly
intends *some* checking (a missing or non-string `name` IS rejected) — it just stops
one field short of the field the feature actually depends on.

**Recommendation:** treat the hint as valid only when it carries a non-empty `email`
(the field that makes the silent path work) as well as a non-empty `name`; otherwise
drop it and render the clean never-signed-in control. That is strictly better than a
remembered-looking account whose reconnect cannot succeed. Owner call because it
changes what "remember me" means for an account whose userinfo response is
incomplete.

> **CLOSED 2026-07-30 — owner decisions** ([DECISIONS.md](../DECISIONS.md)): AUTH-13 — a hint is valid only with non-empty name AND email; otherwise drop it and show the never-signed-in control; stop persisting empty-email profiles.

**Next:** area closed — nothing outstanding. Next in the S track: S2 (share backend).
