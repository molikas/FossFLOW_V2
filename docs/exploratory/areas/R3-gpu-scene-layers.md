# R3 — Bulk GPU scene layers (build/invalidation, style parity, LOD)

**Status:** DONE · **Counted hypotheses:** 15 / 10 · **Bugs:** 7 · **Hypothesis ID prefix:** `GPU-`

*The 7 BUG verdicts (GPU-01, 03, 04, 05, 09, 14, 15) land in 4 known_issues entries, not 7: GPU-01 and GPU-03 are one `getImage` fix and share an entry; GPU-05 extends GPU-04's entry (one threshold decision closes both); GPU-14 is an addendum to R2's atlas entry (same root, same fix directions).*

**Scope:** Four memoised canvas components own one sprite batch each. buildInstances (O(N), scene-change only) resolves painter's order (resolveRenderOrder: layer order, zIndex, -x-y), rasterises/packs chips and icons, and emits instances; drawGLBatch (per frame) recomputes counterScale + backing store and issues one draw. Invalidation is dual-path: zustand subscribers (scroll/zoom -> drawNow synchronously in the pan rAF; content flags -> geomDirty) plus React effects on props/context (nodes, layers, visibleIds, strategy.projectionName, theme). NodesCanvas additionally gates labels by LOD (zoom >= 0.25 unless readableLabels), manages the icon decode() gate (black-atlas-tile prevention), and skips hybrid-promoted nodes. ConnectorsCanvas/RectanglesCanvas probe the projection's linear map per build (widthScale, L matrix) to scale authored unprojected widths and iso-shear the arrow; RectanglesCanvas insets fills by halfStroke against the analytic-AA border. computeBackingStore clamps dpr so W*dpr fits canvas caps, feeding BOTH buffer size and u_view.

**Code:**
- `packages/axoview-lib/src/components/SceneLayers/Nodes/NodesCanvas.tsx`
- `packages/axoview-lib/src/components/SceneLayers/Labels/LabelsCanvas.tsx`
- `packages/axoview-lib/src/components/SceneLayers/Connectors/ConnectorsCanvas.tsx`
- `packages/axoview-lib/src/components/SceneLayers/Rectangles/RectanglesCanvas.tsx`
- `packages/axoview-lib/src/utils/labelChip.ts`
- `packages/axoview-lib/src/utils/renderOrder.ts`
- `packages/axoview-lib/src/utils/renderTarget.ts`

**Dedupe baseline:** [coverage-baseline.md](../coverage-baseline.md) sections — E2E: *Canvas projection modes / rendering / viewport*; Unit: *Lib WebGL rendering*, *Lib UI components*. Then grep the suites directly.

## Seed seams (2026-07-29 mapping — starting points, not a ceiling)

- The geomDirty invalidation matrix differs per layer: NodesCanvas watches readableLabels/previewHideLabels/exportHideLabels/editorMode + model items/icons; LabelsCanvas watches labelMove/labelMoves/inlineEditLabelId; Connectors watches sceneStore.connectors + model.colors; Rectangles watches only model.colors via subscription (rect geometry arrives via props). Any new store field that changes drawn output but is missing from the right subscriber leaves a stale GPU frame until an unrelated change rebuilds — the classic bug shape here.
- Icon pipeline: img.decode() rejection falls back to `img.onload = markReady` — for a URL that is already complete but broken (naturalWidth 0) neither fires, so the icon never draws and canvas.dataset.allIconsDrawn stays 'false' forever (blocks the export readiness gate); also getImage caches the Image by URL before decode succeeds, so a transient network failure is never retried.
- LOD gaps: node name chips vanish below zoom 0.25 (LABEL_LOD_ZOOM) while NodeLabelHitLayer removes its hit divs below zoom 0.4 (HIT_MIN_ZOOM) — between 0.25 and 0.4 labels are visible but their drag hit-targets exist, below 0.25 with readableLabels ON chips still draw but hit divs are gone: draw/hit visibility is decided in two files with different thresholds.
- Cross-layer z-order is fixed by DOM stacking (RectanglesCanvas < Grid < ConnectorsCanvas < TextBoxes < NodesCanvas < LabelsCanvas < ConnectorLabels); per-element zIndex sorts only WITHIN a canvas — a zIndex intended to lift a connector above a node can never work, and Grid lines intentionally paint OVER rectangle fills (surprising to a fresh reader; changing child order in Renderer silently reshuffles the scene).
- widthScale / the L matrix are probed from getTilePos unit steps at BUILD time — correctness after an iso<->2D switch depends on each layer's React effect deps re-dirtying geometry (ConnectorsCanvas keys on getTilePosition identity, Nodes/Labels/Rectangles on strategy.projectionName); the LabelsCanvas comment records exactly this bug class (GPU chips stranded at the old projection while DOM hit proxies move).
- computeBackingStore's contract: the clamped dpr MUST feed both the buffer size and u_view scale/origin — chip SUPERSAMPLE still uses raw min(devicePixelRatio,2), so on 3x screens chips are rasterised at 2x but rendered on a (possibly clamped) higher-dpr buffer: soft-but-accepted; a future edit mixing the two dprs desyncs scene and buffer (browser upscales, everything blurs/offsets).
- GPU node chip text is measured (measureNodeLabel innerW = min(250-2padX, nameW)) but fillText draws the FULL name clipped hard at the texture edge — the DOM Label path ellipsizes/hides overflow; long names render differently between the bulk (GPU) and the selected (DOM) node.
- buildCount / drawCount / allIconsDrawn dataset attributes are the perf-harness anti-cheat contract (must stay FLAT during pan) — an innocent-looking call to buildInstances from a per-frame path passes all visual checks but breaks the no-per-frame-CPU invariant the e2e harness asserts.
- Rounded rectangle corners are approximated as sharp on the GPU bulk (noted in RectanglesCanvas header) — a deliberate parity gap between the bulk and the dragged DOM rect.
- (mapper note) All four layers use the same skeleton (refs mirroring props, scheduleDraw/drawNow, contextLost flag). Because the pattern is copy-adapted, a fix applied to one layer routinely misses the others — compare all four when hunting.

## Related harvested invariants (keyword-matched hint list — scan the [full table](../coverage-baseline.md#invariants) too)

- **(ADR 0006 §3 / canvas-interaction.md §7 I-1)** selectedIds can only ever contain interactable refs — locked/hidden-layer items are excluded from every selection path (Ctrl+A, lasso, click, context menu). → *Select 5 items, then toggle their layer to hidden (or locked) in the LayersPanel while the selection is live: nothing re-validates selectedIds on a layer-state change, so Delete or a group drag then mutates now-hidden/locked items. All guards test the acquisition paths, not selection invalidation on layer toggle.*
- **(rendering guidelines §10 + ADR 0038 §5)** buildInstances runs only on scene/geometry change or LOD crossing; pan/zoom is one uniform write + one draw call, and data-build-count must stay flat across a pan. → *Adding visibleIds/lockedIds to a canvas's rebuild deps (per §15) with an unstable Set identity recreated on every store tick → geometry rebuilds per frame during pan. The anti-cheat only runs in the perf harness (PR-time, small-N), so an identity-instability regression on a rarely-run path ships green.*
- **(ADR 0034 §1 + Lucid-parity pass (empty-box lifecycle))** An edit session that ends still-empty deletes the text box — commit-empty and cancel-on-never-committed alike; no invisible zero-width ghost may remain. → *End a session whose content is Quill's structural residue (`<p><br></p>`, or whitespace-only after &nbsp; normalization): if the emptiness check compares raw HTML rather than stripped text, an invisible ghost box survives, is lasso-selectable, and counts in Ctrl+A. Tests cover the plain-empty case, not the structural-residue cases.*
- **(ADR 0036 §2 + known_issues (root-folder detection))** ADR 0036 promises the provider detects a deleted/trashed Drive root folder; as-built, isAvailable() only checks auth and the cached root id is never revalidated. → *Trash the app folder in Drive's own UI mid-session: autosaves keep 200-OK patching files in the trash for the rest of the session; loss surfaces only at next full listing. The cheap fix (invalidate on zero-listing or 404) is catalogued but unimplemented — any test asserting 'save succeeded ⇒ durable' is false here.*
- **(ADR 0027 §4 + as-built note)** The context menu is the SOLE per-item command surface; no command is reachable only via a removed gesture; the catalogue keeps item-type parity (unsnap/collision/Add note for every eligible type). → *The ADR's as-built note admits there is NO unit test for CanvasContextMenu and only 'Unsnap from grid' is e2e-exercised. A new element type or a menu refactor that drops 'Disable collision' or 'Add note' for one type orphans that command entirely (its old surfaces are deleted) and every suite stays green.*
- **(ADR 0012 + known_issues (passive badges))** View-mode clickability: a node is clickable when it has link, headerLink, description, or notes; the info popover shows for any content-bearing item with parity across node/connector/rectangle/text box; items with no content show nothing. → *Floating Labels (added after ADR 0012) carry headerLink — if the view-mode popover/click path never gained a LABEL branch, a linked Label is dead in present mode (hover shows pointing-finger via one path but click opens nothing). The parity tests enumerate the original four types; nothing asserts the fifth.*

## Known coverage gaps (from the baseline inventory)

- (Canvas projection modes / rendering / viewport) Mouse-wheel zoom anchoring at the cursor position
- (Canvas projection modes / rendering / viewport) Zoom clamp bounds via buttons/wheel (only touch pinch asserts [0.1,1])
- (Canvas projection modes / rendering / viewport) Fit-to-view exact framing (all items visible) — only 'value changed' asserted
- (Canvas projection modes / rendering / viewport) Projection mode persistence across reload
- (Canvas projection modes / rendering / viewport) 2D label stalk rendering parity
- (Lib WebGL rendering) packages/axoview-lib/src/webgl/glSpriteBatch.ts — zero tests (batching/draw-call assembly)
- (Lib WebGL rendering) packages/axoview-lib/src/webgl/itemRaster.ts — zero tests (icon rasterization to atlas)
- (Lib WebGL rendering) packages/axoview-lib/src/webgl/contextLoss.ts — zero tests (context-loss recovery)
- (Lib UI components) zero-test lib components: Renderer/Renderer.tsx, SceneLayer, Grid (formula tested only via perf grep-free math), AnnotationLayer/AnnotationLayer.tsx, AnnotationPalette, CanvasContextMenu, ConnectorAnchorOverlay, ConnectorSettings, LayersPanel, ZoomControls, SettingsDialog, ZoomSettings, HotkeySettings, IconPackSettings, HelpDialog, LeftDock, BottomDock, RightSidebar.tsx, DragAndDrop, ModeHint, PreviewLayerSwitcher, PreviewCanvasModeToggle, TopBarStyleControls, ElementLinkCard, CanvasCompositorOverlay, DOMErrorBoundary, Cursor, Lasso/FreehandLasso overlays, Svg, UiElement
- (Lib UI components) ItemControls panels (NodeSettings/ConnectorSettings/RectangleSettings/TextBoxSettings etc.) have only the two-section parity test — no per-control behavior tests
- (Lib UI components) ExportImageDialog behavior beyond waitForIconsDrawn is covered only by static source-grep tests (initialLoad/memo)


**Next:** R3 is **DONE** — 15 / 10 counted, every row resolved, nothing deferred. Nothing to pick up here; the next area in wave order is R4.

What this area leaves behind for R4 / R5:

1. **`tests-exploratory/_rig/glOracles.ts`** — shared rendering oracles: `paintedPixels` / `paintedPixelsAll` (read-back over the four bulk canvases), `layerCounters` / `countersAll` (the `data-build-count` / `data-draw-count` / `data-all-icons-drawn` / `data-labels-drawn` attributes each layer publishes — `buildCount` is the invalidation oracle), `loseContext` / `restoreContext` / `isContextLost`, `setCanvasMode` / `setZoom`. **The "pixel-blind to CI" mapper note for this area is wrong.**
2. **Seed any scene shape straight through the store.** `model.actions.set({ views })` writes the active view wholesale, which is how GPU-14 got 300 on-screen Labels and GPU-08 got a configured layer set without touching the LayersPanel (`injectLabels` in `gpu-14-15.explore.spec.ts`, `seedLayer` in `gpu-08-layer-visibility.explore.spec.ts`). Both are the cheap route to high-N and multi-layer states.
3. **Two reusable pixel oracles beyond "did it paint?".** `chipProbe` in `gpu-05-09-12.explore.spec.ts` locates a chip by its longest near-white run and profiles the dark pixels per column — the way to ask "was this text cut / is this padding respected?" without a screenshot. The bulk probe in `gpu-14-15.explore.spec.ts` reduces a canvas to a painted bbox plus corner tests, which answers shape questions (square vs rounded) in 2D mode.
4. **Break an asset with `page.route`, not the store**, when the bridge may not survive (see the export-dialog trap now in COLDSTART). Serving 404 → 200 for the same url is also how "does anything retry?" gets a request-count oracle (GPU-03).
5. **`src/__explore__/R2/glStub.ts`** — the recording WebGL2 stub for jest-side sprite-batch work, plus `installDrawing2DStub()`.

Threads this area adds to the rendering block:

- **R-f. Bulk vs DOM is a parity surface, and it drifts wherever the two paths each decide something.** Three of this area's bugs are the same shape as R1's: the bulk hard-cuts label text on one line while the DOM wraps it (GPU-09); the bulk draws square rectangle corners while the DOM rounds them (GPU-15); the chip draws with no zoom gate while the hit layer gates at 0.4 (GPU-04/05). The hybrid guarantees the two are never on screen together, which is exactly why nothing catches them — **any R4/R5 hypothesis about a hybrid-promoted entity should diff the promoted and unpromoted renderings, not test one.**
- **R-g. Invalidation was the predicted bug farm and it held up.** GPU-06, GPU-08 and GPU-12 all predicted per-layer `geomDirty` drift and all three were FALSIFIED: the props-based deps (`nodes` / `rectangles` / `layers` / `visibleIds` / `strategy.projectionName`) cover far more than the store subscriptions suggest, and all four layers rebuild together. Reading the subscription list alone under-reads the invalidation path — check the effect deps too before predicting a stale frame.
- **R-h. The readiness/completeness signals are icon-only.** `data-all-icons-drawn` tracks icon bitmaps and nothing else, so a build that dropped chips (atlas overflow, GPU-14) reports ready and the export captures the incomplete frame. Any area that consumes a "is the canvas ready?" signal should ask what that signal actually covers.

## Hypotheses

| ID | Hypothesis | Source | Nearest existing tests | Probe | Verdict | Evidence |
|----|-----------|--------|------------------------|-------|---------|----------|
| GPU-01 | A broken icon URL strands the layer forever: `decode()` rejects, the fallback tests `img.complete && naturalWidth > 0` (false for a failed load) and so assigns `img.onload = markReady` — a handler that can never fire because the load already errored. There is no `onerror` path at all, so `decodedRef` never gains the url, `getImage` returns null for the rest of the session, and `data-all-icons-drawn` stays `"false"` permanently | seed seam (icon pipeline) | *none* — `waitForIconsDrawn.test.ts` sets the dataset flag by hand; nothing drives the decode path | tests-exploratory/R3-gpu-layers/gpu-01-03-icons.explore.spec.ts | BUG | confirmed, with the whole chain asserted rather than inferred: a HEALTHY icon reaches `data-all-icons-drawn="true"` first (so a later `"false"` is the finding, not a flag that never works), the store really carries the new url, `data-build-count` advances (the layer re-ran `getImage`), and the 404 was really requested (`requests=1`). 8 s later — 20× the export dialog's initial icon budget — the flag is still `"false"` and painted pixels fell from 3186 to 1437 (chip + stalk still draw, the icon is gone). known_issues: *One unreachable icon url disables the icon layer's readiness flag for the session* |
| GPU-02 | Because `data-all-icons-drawn` never flips, EVERY image export on a diagram with one broken icon pays the full `waitForIconsDrawn` timeout, twice (initial capture + recapture), and still exports without that icon — a silent minutes-scale regression from one dangling reference (which CLIP-14 already showed loads clean) | cross-feature (R3 x export) + bug-class recurrence | `waitForIconsDrawn.test.ts` (hand-set flag), `import-export-image.spec.ts` (healthy icons only) | tests-exploratory/R3-gpu-layers/gpu-02-export-gate.explore.spec.ts | FALSIFIED | the magnitude is wrong and the user-visible cost is nil. Both waits are bounded in `ExportImageDialog` — `ICONS_READY_TIMEOUT_MS` 400 before the first capture, `ICONS_RECAPTURE_TIMEOUT_MS` 2000 for the recapture poll — and the recapture poll runs AFTER the first preview is delivered, so it never blocks anything the user is waiting for. Measured on the same page, one node, dialog opened twice: broken icon **1329 ms** to a finished first export, reachable icon **1407 ms** (delta −78 ms; the ≤400 ms gate is inside the noise of mounting the hidden Axoview). The export still produced a full PNG preview (462 KB). What is real is a wasted 2 s of background rAF polling per export and the missing icon — both already covered by GPU-01's entry. **Rig trap found here and folded into COLDSTART: `Axoview`'s debug-bridge effect does `delete window.__axoview__` on cleanup, and the export dialog mounts a SECOND Axoview — so closing the dialog deletes the MAIN app's bridge, and while it is open the bridge points at the hidden export instance.** |
| GPU-03 | `getImage` inserts the `Image` into the cache BEFORE the decode resolves and only ever returns `decoded.has(url) ? existing : null`, so a transient load failure is cached as a permanent one — the icon cannot recover without a full remount, and nothing retries | seed seam (icon pipeline) + stale derived cache | *none* (see GPU-01) | tests-exploratory/R3-gpu-layers/gpu-01-03-icons.explore.spec.ts | BUG | confirmed with the request count as the oracle. The url is served 503 once (`requests=1`, flag `"false"`), then the route starts serving a valid PNG and the layer is forced through four further geometry rebuilds (a 2D↔ISO round trip plus two `readableLabels` toggles — `data-build-count` 6→10, asserted as a precondition so "no retry" cannot mean "nothing asked"). The request count stays at **1** and the flag stays `"false"`: `iconCacheRef` holds the failed `Image` keyed by url, so every later build takes the `existing` branch and returns null without going near the network. Same known_issues entry as GPU-01 (one `getImage` fix closes both). |
| GPU-04 | Draw visibility and hit visibility are decided in two files with different thresholds: floating Label chips paint from `LabelsCanvas` with no zoom gate, while `LabelHitLayer` mounts its hit divs only at `zoom >= HIT_MIN_ZOOM` (0.4). Below 0.4 a Label is visible but completely inert — not selectable, not draggable, no context menu | seed seam (LOD gaps) | `readable-labels.spec.ts`, `label-drag.spec.ts`, `label-entity.spec.ts` — all at default zoom | tests-exploratory/R3-gpu-layers/gpu-04-06-07-08-13.explore.spec.ts | BUG | confirmed with the precondition asserted (a zero proxy count would otherwise look exactly like a wrong selector). At default zoom — verified `>= 0.4`, i.e. above `HIT_MIN_ZOOM` — a committed floating Label paints on `LabelsCanvas` AND has at least one `[data-label-hit-id]` proxy. At `zoom = 0.3` the chip still paints and the proxy count is **0**: the Label is visible and completely inert — not selectable, not draggable, no context menu. `LabelsCanvas` has no zoom gate at all while `LabelHitLayer` gates at 0.4, so draw visibility and hit visibility are decided in two files with different thresholds. known_issues: *A floating Label is visible but inert below zoom 0.4* |
| GPU-05 | The `readableLabels` setting widens the same gap for node names: chips are forced to draw below `LABEL_LOD_ZOOM` (0.25) while the hit layer stays absent below 0.4, so turning the accessibility setting ON produces MORE visible-but-inert labels, not fewer | seed seam (LOD gaps) + parity | `readable-labels.spec.ts` asserts the chips draw, never that they are grabbable | tests-exploratory/R3-gpu-layers/gpu-05-09-12.explore.spec.ts | BUG | confirmed for node names, counting the NODE hit proxies specifically (`[data-axoview-id="canvas-label-hit"]` — the floating-Label layer publishes the same `data-label-hit-id`, so the bare attribute would have conflated the two). Precondition asserted at default zoom: `data-labels-drawn > 0` AND proxies > 0, so the zeros below mean something. At `zoom = 0.15` with the setting **off**: `labelsDrawn=0`, proxies=0 — consistent, nothing visible and nothing to grab. With it **on**: `labelsDrawn=1`, proxies still **0**. The setting whose purpose is keeping labels readable when zoomed out is the one that manufactures inert ones. Folded into GPU-04's known_issues entry (one threshold fix closes both) rather than filed separately. |
| GPU-06 | An iso<->2D switch re-dirties geometry on some bulk layers and not others (each layer keys on a different dep — `getTilePosition` identity vs `strategy.projectionName`), so at least one of the four canvases keeps painting at the old projection until an unrelated edit rebuilds it — the exact bug class the LabelsCanvas comment records | seed seam (projection deps) + harvested invariant (rendering guidelines §8) | `canvas-modes.spec.ts` (nodes only, via DOM/hit-test agreement); nothing checks all four painted buffers | tests-exploratory/R3-gpu-layers/gpu-04-06-07-08-13.explore.spec.ts | FALSIFIED | every bulk layer that had drawn instances rebuilt across an ISOMETRIC -> 2D switch. The probe asserts as a precondition that at least TWO layers carried geometry before concluding (a sweep that exercised one layer would otherwise read as "all four are fine"), then requires `data-build-count` to advance on each of them. The per-layer dep drift the seed predicted (`getTilePosition` identity vs `strategy.projectionName`) does not produce a stale projection today. |
| GPU-07 | The four layers are copy-adapted from one skeleton, so context-loss recovery has drifted: a forced `WEBGL_lose_context` + restore brings some canvases back and leaves others blank (R2/GL-08 only ever exercised NodesCanvas) | sibling drift (mapper note: a fix in one layer misses the others) | *none* — `contextLoss.ts` has zero tests; GL-08 covers one of four layers | tests-exploratory/R3-gpu-layers/gpu-04-06-07-08-13.explore.spec.ts | FALSIFIED | the copy-adapted skeleton has NOT drifted on this path. Each of the four canvases in turn: forced `WEBGL_lose_context`, polled `isContextLost()` to true, restored, polled back to false, and re-read its painted-pixel count. No layer that was painting before its context was lost is blank afterwards, and store invariants hold across all four cycles. Extends R2/GL-08 (which covered NodesCanvas alone) to the full set. |
| GPU-08 | The `geomDirty` matrix differs per layer, so a layer-visibility toggle rebuilds some bulk canvases and not others — an entity stays painted after its layer is hidden, or stays missing after it is shown | seed seam (invalidation matrix) | `layers.spec.ts` asserts DOM/store state, never the painted GPU buffers | tests-exploratory/R3-gpu-layers/gpu-08-layer-visibility.explore.spec.ts | FALSIFIED | **un-deferred** — the earlier skip was a rig gap, not a finding: a blank-diagram boot configures no layers, so the hypothesis had no subject. Seeding the layer set directly through `model.actions.set({ views })` (the move GPU-14 supplied) removes the need for the LayersPanel entirely. Scene: a node and a rectangle, i.e. two entity types on two different bulk canvases, so this cannot pass by exercising one layer and calling it four. With the layer seeded VISIBLE (which also switches off every layer's `layers.length === 0` "draw all" escape hatch — that hatch is why the empty-layers boot could not answer this) nodes paint 3186 px and rectangles 10155 px. Flipping `visible: false`: both go to **0** and all four canvases rebuilt (`data-build-count` 6→7 / 8→9). No per-layer drift on this path. |
| GPU-09 | The GPU chip draws the FULL node name with no `maxWidth` and no clipping path while `measureNodeLabel` clamps the chip to `LABEL_CHIP_MAX_W`, so a long name is hard-cut mid-glyph at the texture edge on the bulk path while the DOM path ellipsizes — the same node reads differently selected vs unselected | seed seam (chip text) + parity (bulk vs DOM) | `cross-type-label-size.spec.ts` (sizing), `readable-labels.spec.ts` — neither uses an overflowing name | tests-exploratory/R3-gpu-layers/gpu-05-09-12.explore.spec.ts | BUG | confirmed, with one correction: the DOM path **wraps**, it does not ellipsize. Oracle: a self-locating pixel probe — the chip is a near-white rounded rect, so the longest near-white run in the read-back buffer IS the chip, and dark pixels in the inner half of its right padding band mean the glyphs ran to the border. The SHORT-name control is what makes it evidence: `rightPad=[3,7,3,0,0,0,0,0]`, inner half **0** (a fitting name ends exactly at `chipW − padX`, so only its AA tints the outer columns — the first version of this probe failed its own control on exactly that, which is why the band is now measured per column). Long name (68 chars, needs 612 px against a 226 px interior): chip clamped to 160 device px at zoom 0.65 (≈246 scene px), `rightPad=[1,7,0,0,8,5,4,4]`, inner half **21** — text into the border, cut mid-glyph. The DOM label for the same node (selector scoped to the renderer subtree, since the sidebar shows the name too) holds all 68 chars over 3 lines and shows 2. known_issues: *A long node name is cut mid-glyph on the canvas and wrapped in the DOM* |
| GPU-10 | `computeBackingStore` clamps `dpr` against `DEFAULT_RENDER_CAPS`, and its own doc says the clamped value MUST feed the view scale AND the device origin as well as the buffer size; if any of the four layers feeds the raw `devicePixelRatio` into `u_view` while sizing the buffer from the clamped one, the scene and its buffer desync and the whole layer is offset | seed seam (backing store) + ADR 0038 deferred item | `renderTarget` unit tests cover the pure math; no layer wiring is asserted | src/__explore__/R3/backing-store-gpu-10-11.explore.test.ts | FALSIFIED | the copy-adapted skeleton got this one right in all four layers. Per layer, asserted: it calls `computeBackingStore(W, H, window.devicePixelRatio \|\| 1)`; every `originXDev` / `originYDev` line multiplies by the returned `dpr` (or its `v.dpr` alias) and mentions `devicePixelRatio` nowhere; the `b.render(...)` call passes the clamped `bw, bh` and `zoom * dpr`. No layer mixes the two. (Wiring is asserted against the layer sources — the same idiom as R2/atlas-gl's reachability checks and `ExportImageDialog`'s own static tests — because mounting four WebGL2 layers under jsdom to read back one uniform costs far more than it proves.) |
| GPU-11 | Chip supersampling uses the raw `min(devicePixelRatio, 2)` while the backing store uses the clamped dpr, so on a clamped viewport chips are rasterised for a resolution the buffer no longer has | seed seam (backing store) | *none* (see GPU-10) | src/__explore__/R3/backing-store-gpu-10-11.explore.test.ts | FALSIFIED | the mixed read is real (both chip rasterisers use `Math.min(f.dpr, 2) * CHIP_SUPERSAMPLE` off the raw ratio) but the predicted consequence does not occur — the R2 "right about the code, inert in the product" lesson. Swept 8 viewports × 5 dprs through the real `computeBackingStore`: **exactly one** combination clamps at all, 7680×4320 @ 3× (23040 device px against the 16384 cap). 5120×2160@3 and 7680×4320@2 both come to 15360 px and fit. So on every viewport that exists as a browser window, clamped dpr === raw dpr and there is nothing to desync. And in that single reachable case the divergence runs the OTHER way: the surface keeps dpr 2.13 while chips cap at 2, i.e. chips are marginally *softer* than the buffer — which is already true of any dpr > 2 screen, clamped or not, and is the documented `CHIP_SUPERSAMPLE` trade. Nothing is offset either way, because the scene math stays on one dpr (GPU-10). |
| GPU-12 | A rectangle style change that is not a colour (line style, opacity, stroke width) does not reach `RectanglesCanvas`, whose only store subscription is `model.colors` — the bulk rectangle keeps its old style until an unrelated rebuild | seed seam (invalidation matrix) | `rectangle-ops.spec.ts`, `bulk-style.spec.ts` — model state only, no painted-buffer check | tests-exploratory/R3-gpu-layers/gpu-05-09-12.explore.spec.ts | FALSIFIED | the seed seam under-read the invalidation path. `model.colors` is the only *store subscription*, but rectangle geometry AND style both arrive as the `rectangles` **prop**, which is a dep of the layer's `geomDirty` effect — so any style write to the view rebuilds. Measured: a real drawn rectangle painting on `axoview-rectangles-canvas`, then `borderStyle: 'DOTTED'` + `borderWidth: 4` written to the view (store write asserted as a precondition); `data-build-count` 5→6 and the painted buffer changed 39861→40052 px. |
| GPU-13 | Per-element `zIndex` sorts only WITHIN a canvas while cross-layer order is fixed by DOM stacking, so a `zIndex` set on a connector can never lift it above a node — the z-order controls silently do nothing across entity types | seed seam (cross-layer z-order) | `z-order.spec.ts`, `rectangle-overlap-zorder.spec.ts` — both single-type | tests-exploratory/R3-gpu-layers/gpu-04-06-07-08-13.explore.spec.ts | SUSPECT | confirmed as described, and it is a design consequence rather than a defect. All four bulk canvases compute to the SAME `zIndex` and sit in ascending document order, so cross-type paint order is fixed entirely by mount order in `Renderer.tsx` and a per-element `zIndex` sorts only within its own canvas — a `zIndex` set on a connector can never lift it above a node, and the z-order controls silently do nothing across entity types. No ADR states whether cross-type z-order should be reachable. (Note: the Labels-after-Nodes half of this is already asserted by `label-entity.spec.ts`; what is novel is the four-layer rule and the inertness of cross-type `zIndex`.) -> product question |
| GPU-14 | A build that overflowed the atlas (R2/GL-02) leaves chips skipped, and because `data-build-count` only advances on a rebuild, the layer reports a completed build while painting an incomplete frame — the export readiness gate cannot tell the difference either | cross-area (R2 -> R3) + anomaly capture | *none* | tests-exploratory/R3-gpu-layers/gpu-14-15.explore.spec.ts | BUG | confirmed in the browser, and the reachability is worse than R2 assumed ("only the culling-defeated fit-to-view harness reaches this path"). Floating Labels are the cheap route: they are viewport-culled by TILE, so packing them into the visible tile window defeats the cull, and `LabelsCanvas` asks `createSpriteBatch(canvas)` for the **default 4096** atlas where `NodesCanvas` asks 8192 — it overflows ~4× sooner. Ramped 120 → 300: at 120 the layer drew 120/120 chips; at **300 it drew 276**, 24 chips silently missing from the frame, while `data-build-count` advanced exactly as for a complete build. A second test injects the 300 in ONE step on a fresh page to rule out the obvious confound — that the ramp's first 120 chips had leaked into the atlas (there is no eviction, which is R2's finding) — and gets the same 276, so the threshold is the chips themselves and not fragmentation. And the readiness gate is blind to it — with 276/300 chips drawn the node layer still published `data-all-icons-drawn="true"`, so `waitForIconsDrawn` says ready and the export captures the incomplete frame. The only trace anywhere is `data-draw-count` sitting below the entity count, which no consumer compares. Folded into R2's atlas entry (same root, and the fix directions there are the fix directions here). |
| GPU-15 | Rounded rectangle corners are approximated as sharp on the GPU bulk but drawn round on the dragged DOM rect, so a rectangle visibly changes shape at the moment it is grabbed | seed seam (documented parity gap) | `rectangle-move-resize.spec.ts` (geometry only) | tests-exploratory/R3-gpu-layers/gpu-14-15.explore.spec.ts | BUG | confirmed — cosmetic, but the shape really does change on grab. Probed in 2D so the footprint is axis-aligned and "is the corner square?" is one pixel read. Idle: the bulk painted bbox is 261×261 px (4×4 tiles at zoom 0.65) and **all four** corners of that bbox are painted 2 px in — square. Mid-drag, with the precondition asserted (`mode.type === 'DRAG_ITEMS'` and a `[data-drag-id]` element present, so the `rx` read means something), the DOM rect publishes `rx="21.5"` — `cornerRadius` 22 less halfStroke. The two are never on screen together (the Renderer keeps only the dragged rect in the DOM), which is why nothing caught it. known_issues: *A grouping rectangle changes shape from rounded to square when you grab it* |

## Product questions (SUSPECT verdicts)

| ID | Question | State |
|----|----------|-------|
| GPU-13 | Should a per-element `zIndex` be able to cross an entity type — e.g. lift a connector above a node? | **OPEN** — it cannot today and cannot be made to without restructuring the renderer: all four bulk canvases share one stacking context and cross-type order is fixed by mount order in `Renderer.tsx`, so `zIndex` sorts only within a canvas. The z-order controls are therefore silently inert across types (bring-to-front on a connector does nothing relative to nodes). Either the controls should say so, or cross-type order needs a real mechanism (a single canvas, or per-entity depth). |

> **CLOSED 2026-07-30 — owner decisions** ([DECISIONS.md](../DECISIONS.md)): GPU-13 — build real cross-type depth (owner chose the full mechanism over scoping the controls; renderer restructure, ADR 0038 amendment + design pass first).
