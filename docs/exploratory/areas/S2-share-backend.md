# S2 — Share backend: session snapshots, routes, Express/Worker parity

**Status:** DONE · **Counted hypotheses:** 15 / 10 · **Bugs:** 12 · **Suspects:** 1 · **Hypothesis ID prefix:** `SHARE-`

**Scope:** Framework-agnostic route handlers (routes.js) for diagrams/folders/tree-manifest plus the snapshot share lifecycle: POST /api/diagrams/:id/share copies the diagram into public/<uuid> (nanoid-style 21-char uuid, reused if diagram.shareUuid present), DELETE unshares, GET /api/public/diagrams/:uuid is the unauthenticated read. fs adapter maps keys to flat <STORAGE_PATH>/<id>.json files with tmp+rename atomicity. The Hono worker serves /api/config and the anonymous Drive read proxy but 503s all other /api/* (serverStorage:false); Express (Docker) serves the full storage surface. auth.ts implements none/shared-token/cf-access modes with a public-route bypass. LocalStorageProvider's server path is the app-side client of these routes; shareUrl.ts re-anchors the returned uuid to the page origin.

**Code:**
- `packages/axoview-backend/src/routes.js`
- `packages/axoview-backend/src/adapters/fs.js`
- `packages/axoview-backend/server.js`
- `packages/axoview-worker/src/app.ts`
- `packages/axoview-worker/src/auth.ts`
- `functions/api/[[path]].ts`
- `packages/axoview-app/src/services/storage/providers/LocalStorageProvider.ts`
- `packages/axoview-app/src/utils/shareUrl.ts`

**Dedupe baseline:** [coverage-baseline.md](../coverage-baseline.md) sections — E2E: *Share + Drive display*, *Save + error dialogs*; Unit: *Backend (Express/Docker) storage + share API*, *Cloudflare Worker API + auth*. Then grep the suites directly.

## Seed seams (2026-07-29 mapping — starting points, not a ceiling)

- shareUuid stripped by full-save: routes.js saveDiagram (PUT, line ~126) writes `{...body, id, lastModified}` replacing the stored diagram — the app's autosave payload (leanIfModel of the editor model) does not carry shareUuid, so the first autosave after sharing silently deletes shareUuid from diagrams/<id> while public/<uuid> stays live. Next share mints a SECOND uuid (orphaned first snapshot readable forever); unshare/deleteDiagram can no longer clean the orphan. Fully probe-able with plain HTTP: create → share → PUT without shareUuid → share again → compare uuids.
- Reserved-id collision: assertId's ID_PATTERN accepts 'folders', 'tree-manifest', 'metadata', 'diagrams-index'; fs.js keyToPath flattens diagrams/<id> to <STORAGE_PATH>/<id>.json, so POST /api/diagrams with body.id='folders' clobbers folders.json (listDiagramMeta hides it afterwards). Probe-able without Google.
- folders.json lost-update race: createFolder/renameFolder/moveFolder/deleteFolder all read the whole array then rewrite it with no lock/version — two concurrent requests (e.g. project import burst + a user rename) drop one write.
- Non-recursive deleteFolder orphans grandchildren: only the target folder leaves folders.json; child folders keep parentId pointing at the deleted id, and sweepOrphanedDiagrams sweeps only diagrams whose folderId is in toDelete — descendants' diagrams survive but become invisible/inconsistent depending on how the tree walks parents.
- Concurrent shareDiagram double-mint: two POST /share for the same never-shared diagram both read shareUuid-less state, each generates a uuid and writes its own public snapshot; only the last diagram write records its uuid — the other snapshot is an untracked live orphan.
- Cross-deployment /display/p links: worker auth.ts isPublicRoute whitelists GET /api/public/diagrams/:uuid but app.ts has no handler for it — the catch-all returns 503 'Server storage is disabled'. A snapshot link minted on a Docker deployment opened against a Cloudflare deployment dead-ends; the client only suppresses the fetch when its own config said serverStorage:false, which is the CF case, so it shows the LocalModeShareErrorDialog copy — verify that copy makes sense for a recipient (not a local-mode owner).
- Backend-returned url field is host-derived (publicBaseUrl) and known-wrong in dev (:3001); shareUrlFromUuid papers over it client-side — any consumer of result.url (tests, future callers) regresses.
- Express body-size / worker 10MB bodyLimit asymmetry: a diagram that saves fine on Docker may 413 only through the worker paths; and the worker proxy's size gate trusts Drive's `size` field which is absent for some file types (Number(undefined ?? '0')=0 passes the cap, then alt=media streams the full body).
- (mapper note) Entirely probe-able WITHOUT Google credentials: routes.js is pure functions over an adapter (jest suite exists in packages/axoview-backend/src/__tests__), and the Express server runs against a temp STORAGE_PATH; worker has a vitest suite (packages/axoview-worker/src/__tests__). Playwright pattern for the client side: share-error.spec.ts mocks **/api/config, **/api/diagrams*, **/api/diagrams/*/share with page.route.

## Related harvested invariants (keyword-matched hint list — scan the [full table](../coverage-baseline.md#invariants) too)

- **(ADR 0021 item 7 (D-8 fix) + D-9)** After undo/redo, resyncScene re-routes connectors with missing/empty paths — but only in the ACTIVE view. → *Paste connectors on Page 1 → switch to Page 2 before async routing completes → undo/redo: resyncScene scans only the active view, so Page 1's pasted connectors stay pathless (invisible) until a later edit touches them. useHistory tests exercise single-view scenarios only.*
- **(rendering guidelines §8)** Any GPU layer whose geometry is projected must list strategy.projectionName in its rebuild deps; a DOM hit-proxy and its GPU paint must share one projection. → *Switch iso→2D in VIEW mode using the new viewer projection toggle (PR #84): if any of the four bulk canvases (or a future one) omits projectionName from deps, its paint stays in the old projection while hit-proxies move — the exact Labels bug, now reachable from a brand-new code path (PreviewCanvasModeToggle) that no e2e drives through all four layers.*
- **(ADR 0034 round-2 (normalizeQuillHtmlSpaces))** &nbsp;-serialized spaces are converted to real spaces on commit AND on load, so fixed-width boxes wrap; auto boxes render `pre`, fixed boxes `pre-wrap`. → *A read-only path that loads content without the editor-load normalization — the /display/drive viewer or a share-snapshot render — shows a legacy &nbsp;-heavy fixed-width box as one unbreakable line overflowing the box. Load normalization lives in the editing load chokepoint; no test loads legacy content through the read-only display routes.*
- **(ADR 0032 connector amendment §4 (nameSeeded marker))** The name→labels[] seed is idempotent via a nameSeeded marker stamped on every connector the pass touches; a name typed later is pure identity and never re-seeded. → *Paste a connector from the clipboard (or import a zip diagram) whose reconstruction drops the nameSeeded marker while keeping name: the next load re-seeds name into a midpoint label — duplicate label chips appear after every paste→save→reload cycle. Seed idempotency tests never route through clipboard/zip reconstruction.*
- **(ADR 0011 §1 (error UX contract))** Every failure-of-intent (user clicked/typed/dragged) surfaces an explicit Dialog; notification-toast-only handling and silent .catch(() => {}) are forbidden for such paths. → *Click 'Copy share link' in a context where navigator.clipboard.writeText rejects (non-secure context, permissions policy): the catalogued S1–S20 silent surfaces are still open, and new Drive-share paths added since B-9a (copy preview link, Manage-access actions) have never been audited — a rejection likely dies in a toast or a swallowed catch, and no test asserts dialog-vs-toast classification for new surfaces.*
- **(features.md (viewer-controlled projection, PR #84))** canvasMode in view-only mode is viewer-local UI state persisted only to that viewer's localStorage — switching projection can neither dirty nor save the diagram. → *A viewer switches to 2D on the /display route; the localStorage key is shared with the editor — the OWNER later opens the editor in the same browser and their diagram opens in 2D with a recentered scroll they never chose, and if any editor-side code treats canvasMode as model-adjacent (e.g. included in a future save payload or dirty-diff), a pure viewer action dirties the document. Tests assert non-dirty in view mode only, not the shared-key bleed into edit mode.*

## Known coverage gaps (from the baseline inventory)

- (Share + Drive display) Re-sharing the same diagram (same UUID vs new link)
- (Share + Drive display) Share link revocation/expiry
- (Share + Drive display) Interactions inside the shared readonly view (zoom/pan/layer switching)
- (Share + Drive display) Signed-in (OAuth) Drive rung — only the anonymous proxy path is tested
- (Share + Drive display) Sharing a diagram that links to other diagrams (link behavior in shared view)
- (Save + error dialogs) localStorage quota-exceeded specific path
- (Save + error dialogs) Autosave / dirty indicator behavior
- (Save + error dialogs) Unsaved-changes warning when navigating away or switching diagrams
- (Backend (Express/Docker) storage + share API) routes.js Express wiring itself (middleware order, JSON body limits, error handler) is untested — only the extracted route handler functions are exercised against the in-memory adapter

## Hypotheses

Novelty baseline for the whole block, read in full: `packages/axoview-backend/src/__tests__/` (routes.diagrams 31, routes.folders 26, routes.share 21, routes.tree-manifest 4, routes.config 5, adapters/fs 21, HttpError 3) and `packages/axoview-worker/src/__tests__/` (app 24, authMiddleware 17, cfAccessJwt 11, isPublicRoute 17). That coverage is dense on **single-request happy paths and 400/404 validation**, and empty on three things: (a) two requests **concurrent** on the same key, (b) the **Express/worker wiring** around the handlers (CORS, body-parser, error middleware, `requireStorage` flags) — the baseline names this gap explicitly, (c) what a **later request does to state an earlier one established** (share → save → share).

| ID | Hypothesis | Source | Nearest existing tests | Probe | Verdict | Evidence |
|----|-----------|--------|------------------------|-------|---------|----------|
| SHARE-01 | A full `PUT /api/diagrams/:id` replaces the whole stored document (`{...body, id, lastModified}`) and the app's autosave payload carries no `shareUuid`, so the first autosave after sharing strips it while `public/<uuid>` stays live — the next Share mints a SECOND uuid and neither unshare nor delete can ever reach the first snapshot | seed seam 1 / persistence | `saveDiagram overwrites existing diagram and refreshes lastModified` (asserts the overwrite, never that a share survives it); `unshareDiagram removes snapshot, strips shareUuid` | `__explore__/S2/share-01-04-15.explore.spec.js` | BUG | create → share → one autosave-shaped PUT: `shareUuid` is gone from `diagrams/d1` while `public/<uuid>` still answers 200. Sharing again mints a different uuid and a second live snapshot; `unshare` then removes only the second, and a subsequent hard `deleteDiagram` cannot reach the first either — it is still served. CONTROL: a PATCH (the shape rename/trash use) preserves the field, so the loss is specific to the full replace. known_issues: *The first autosave after sharing orphans the public snapshot* |
| SHARE-02 | `assertId`'s pattern accepts the reserved keys the fs adapter flattens into (`folders`, `tree-manifest`, `metadata`, `diagrams-index`), so `PUT /api/diagrams/folders` overwrites `folders.json` — the entire folder tree — with a diagram document, and `listDiagramMeta` then hides the diagram that did it | seed seam 2 / boundary | `listDiagrams returns diagram metadata, excluding reserved keys` and fs `listDiagramMeta … skipping reserved keys` (both assert the read-side hiding; neither writes to a reserved id) | `__explore__/S2/share-02-13.explore.spec.js` | BUG | `PUT /api/diagrams/folders` returns 200 and replaces `<STORAGE_PATH>/folders.json` with a diagram document — a two-folder tree becomes `[]`, and the next `createFolder` "heals" the file by finishing the deletion. All four reserved ids (`folders`, `tree-manifest`, `metadata`, `diagrams-index`) are writable this way; `getTreeManifest` afterwards returns a diagram document with no `folders` key. CONTROL: `assertId` rejects `has spaces` with 400 but answers 404 for every reserved id, i.e. they pass validation. Note the in-memory test double hides this entirely (separate keyspace + its own reserved-key filter) — the fs adapter is required. known_issues: *A diagram id can overwrite the folder tree — reserved storage keys are not reserved* |
| SHARE-03 | `createFolder`/`renameFolder`/`moveFolder` read the whole `folders.json` array and rewrite it with no lock or version, so two concurrent requests silently drop one write | seed seam 3 / interleaving | `auto-generated ids stay distinct across rapid sequential calls` (sequential); fs `sequential puts on the same key end in a single coherent value` | `__explore__/S2/share-03-05.explore.spec.js` | BUG | Two concurrent `createFolder` calls both return 201 with distinct ids and leave ONE folder in `folders.json`; a concurrent rename + create returns 200 for both and drops the rename. CONTROL: run sequentially, both land. known_issues: *Concurrent folder writes silently lose one another (folders.json has no locking)* |
| SHARE-04 | Two concurrent `POST /api/diagrams/:id/share` on a never-shared diagram each generate a uuid and write their own snapshot; only the last diagram write records one, leaving a live public snapshot no unshare or delete can reach | seed seam 5 / interleaving | `creates new snapshot + uuid when diagram has no shareUuid`, `reuses existing valid shareUuid` (both single-request) | `__explore__/S2/share-01-04-15.explore.spec.js` | BUG | `Promise.all` of two shares on a never-shared diagram yields two distinct uuids and two entries under `public/`, with only one recorded on the diagram; `unshare` removes the recorded one and the survivor still answers 200 — an untracked live snapshot with no owner. CONTROL: sequential shares are idempotent (same uuid, one snapshot). known_issues: *Two simultaneous shares publish two snapshots and record one* |
| SHARE-05 | Non-recursive `DELETE /api/folders/:id` removes only the target row, leaving child folders with a `parentId` naming a folder that no longer exists and their diagrams unswept (`sweepOrphanedDiagrams` only sweeps `folderId ∈ toDelete`) | seed seam 4 | `non-recursive deletes single folder` (asserts the row is gone, not what happened to its children); `recursive deletes folder + descendants + sweeps orphan diagrams` | `__explore__/S2/share-03-05.explore.spec.js` | BUG | A non-recursive delete of `parent` leaves `child` (parentId `parent`, now dangling) and `grandchild` in `folders.json`, and sweeps only the diagram whose `folderId` was `parent` — the child's diagram survives inside an unreachable subtree, and if it was shared its public snapshot keeps serving. CONTROL: the recursive form clears the subtree and sweeps its diagrams. Reachable: `useFileTree` → `LocalStorageProvider.serverDeleteFolder` forwards `?recursive=<bool>`. known_issues: *A non-recursive folder delete orphans its whole subtree* |
| SHARE-06 | `getPublicSnapshot` has no `deletedAt` gate, so trashing a shared diagram from the file explorer (a PATCH soft-delete that preserves `shareUuid`) leaves its public link serving the snapshot — and with the diagram gone from the tree the owner can no longer reach Unshare. Sibling drift: the worker's Drive read proxy gates on `trashed` and returns 410 for exactly this reason | sibling drift / cross-feature pair | `getPublicSnapshot returns snapshot when present`, `deleteDiagram cascades to public/<uuid>` (the HARD delete only — the app's tree delete is the soft one) | `__explore__/S2/share-06-11.explore.spec.js` | BUG | Trashing a shared diagram with the exact PATCH the file tree sends (`{deletedAt}`) leaves `shareUuid` intact and `GET /api/public/diagrams/<uuid>` returning 200 with the full content; the snapshot carries no `deletedAt` either, so no viewer-side gate is possible. Re-sharing a trashed diagram is also accepted. CONTROL: the PERMANENT delete does cascade — the gap is the trash state, which is the only delete the tree performs. Sibling drift: the worker's Drive proxy answers 410 for a trashed file for exactly this reason. known_issues: *Trashing a shared diagram leaves its public link live, and unreachable* |
| SHARE-07 | The worker read proxy's size cap is `Number(meta.size ?? '0') > 10MB`, and Drive omits `size` for some file types, so a size-less file evaluates as 0, passes the cap, and its body streams unbounded | boundary | `413 when the metadata size exceeds the cap — never reads the body` (present size only) | `__explore__/S2/share-07.explore.spec.ts (worker)` | BUG | Metadata reporting `{trashed:false}` with no `size` lets a 30 MB body through: both Drive reads fire and the proxy returns 200 with all 31457280 bytes. `size:'0'` and a non-numeric `size` behave identically (`Number('unknown') > cap` is false). CONTROLS: a REPORTED 11 MB is capped at 413 without reading the body, and the `trashed` gate on the same metadata read fails closed on a missing field — so the size cap is the outlier. known_issues: *The anonymous Drive proxy's 10 MB cap is skipped when Drive reports no file size* |
| SHARE-08 | Express mounts no error-handling middleware, so a `body-parser` failure (malformed JSON, or a body over the 10 MB limit) returns Express's default HTML error page instead of the `{ error }` JSON every client path parses — and diverges from the worker's `bodyLimit` JSON 413 | parity oracle / documented gap | none — the baseline records "routes.js Express wiring itself (middleware order, JSON body limits, error handler) is untested" | `__explore__/S2/share-08-09-10-14.explore.spec.js` | BUG | Against the real `server.js`: a malformed JSON body returns **400 text/html** whose text contains `SyntaxError` and is unparseable as JSON, and an 11 MB body returns **413 text/html** containing `entity too large` and a stack trace (`at `). CONTROL: a handler-raised 404 is `application/json` `{error:'Diagram not found'}` — so the JSON contract exists and body-parser failures escape it. Express mounts no error-handling middleware; the worker's `bodyLimit` returns JSON and its `onError` deliberately keeps stacks internal. known_issues: *A malformed or oversized request body returns an HTML error page with a stack trace* |
| SHARE-09 | The CORS allowlist withholds the response header but never blocks the request, so a CORS-safelisted cross-origin `POST` (Content-Type `text/plain`, no preflight) still executes on a default `AUTH_MODE=none` deployment — `POST /api/diagrams/:id/share` publishes a snapshot and `POST /api/diagrams` creates documents, drive-by from any page the operator visits | ADR-contract violation (security review 2026-07-05 claim) | none — the CORS allowlist has no test at all | `__explore__/S2/share-08-09-10-14.explore.spec.js` | BUG | Against the real `server.js` with the default `AUTH_MODE=none`: a cross-origin `POST /api/diagrams/<id>/share` with `Origin: https://evil.example` and `Content-Type: text/plain` (CORS-safelisted, no preflight) returns 200 **with no `access-control-allow-origin`** and the diagram really is published — `shareUuid` set, `GET /api/public/diagrams/<uuid>` 200. The same shape also creates documents. CONTROL: a DELETE preflight from that origin gets no ACAO while `http://localhost:3000`'s does, so the allowlist works — it just cannot stop a request that needs no preflight. known_issues: *The CORS allowlist does not stop cross-origin writes — a web page can publish your diagrams* |
| SHARE-10 | `GET /api/public/diagrams/:uuid` is mounted `{ requireStorage: false }`, so `ENABLE_SERVER_STORAGE=false` — the operator kill switch that 503s every other storage route — leaves every published snapshot readable from disk | ADR-contract | `app.spec.ts` covers the worker's 503 sink; nothing exercises Express with storage disabled | `__explore__/S2/share-08-09-10-14.explore.spec.js` | SUSPECT | Confirmed and recorded as a product question. With `ENABLE_SERVER_STORAGE=false` on the same `STORAGE_PATH`, `GET/POST /api/diagrams`, `/api/folders` and both share verbs all return 503, while `GET /api/public/diagrams/<uuid>` still returns 200 with the full snapshot — and the DELETE that would unshare it is itself 503, so the operator has no route to revoke. The `{requireStorage:false}` flag is deliberate and no ADR says whether the kill switch should revoke published links. Recommendation in the product-questions list. |
| SHARE-11 | The share snapshot is a hand-written field whitelist, so top-level model fields outside it — `description` today, anything a later schema wave adds — are silently absent from the shared view with no test or type to catch the omission | persistence sweep | `snapshot falls back to defaults when diagram lacks optional fields`, `coerces non-array items/colors/views/icons` (both assert the whitelisted fields only) | `__explore__/S2/share-06-11.explore.spec.js` | BUG | The snapshot's key set is exactly `colors, fitToScreen, icons, items, name, sharedAt, sourceId, title, views` — `description` and `version` (both `modelSchema` fields) and `requiredPacks` are dropped. The consumer half is the damage: `leanIfModel` moves the pack signal into `requiredPacks` and strips the pack icons from `icons`, so on the snapshot `loadPacksForDiagram`'s fallback items × icons cross-reference resolves to `[]`, and lazy pack loading defaults to ON — a shared diagram built on any icon pack renders with unresolved icons. CONTROLS: the owner's load path keeps `requiredPacks`; a non-lean payload resolves through the fallback, so the resolver is sound. known_issues: *A shared diagram loses its icon packs and its description* |
| SHARE-12 | `LocalModeShareErrorDialog` is the recipient-facing dead-end for a `/display/p/<uuid>` link opened where `serverStorage` is false, and its copy tells that recipient to "Deploy via Docker or Cloudflare" — meaningless to a recipient, and factually wrong for an operator too, since the Cloudflare worker hardcodes `serverStorage: false` and can never serve a snapshot | seed seam 6 / cross-deployment | `share-error.spec.ts` mocks the share POST failure; nothing opens a `/display/p` link against a storage-less config | `__explore__/S2/share-12.explore.test.tsx (app)` | BUG | The dialog's only body copy is "...Deploy via Docker or Cloudflare to view shared diagrams", its only action is OK, and the gate that raises it is `isPublicShareUrl && !serverStorageAvailable` — the route shape alone, with nothing distinguishing a recipient from a mis-deployed operator. The Cloudflare half of the advice is also wrong: `app.ts` hardcodes `serverStorage: false`, has no `/api/public/diagrams` handler, and answers `'Server storage is disabled'`. CONTROL: the sibling `DriveDisplayGate` dead-end tells its viewer to "ask the owner to share the file with you" and never mentions deploying — recipient-aware copy is the house standard. known_issues: *A share link opened on the wrong deployment tells the recipient to deploy a server* |
| SHARE-13 | `generateShareUuid` maps `byte % 64` over a 64-character alphabet, so the distribution is biased and share uuids are more guessable than their length implies | boundary / security | `creates new snapshot + uuid` asserts the uuid's shape, never its distribution | `__explore__/S2/share-02-13.explore.spec.js` | FALSIFIED | No bias. The alphabet is exactly 64 characters and `256 % 64 === 0`, so `byte % 64` is uniform. 2000 real uuids from the route (42000 characters over 64 buckets, mean ~656) landed inside ±35% of the mean in every bucket — a modulo mismatch would show ~2x spreads. Probe kept as a guard: it fails if anyone edits the alphabet to a non-divisor length. |
| SHARE-14 | `getPublicBaseUrl` falls back to the client-controlled `Host` / `X-Forwarded-Proto` headers when `PUBLIC_BASE_URL` is unset, so the `url` the share response advertises can be poisoned to a foreign origin | security / inert-check | none — `publicBaseUrl` is passed as a fixture string in every routes test | `__explore__/S2/share-08-09-10-14.explore.spec.js` | FALSIFIED | The behaviour is real — `Host: evil.example` + `X-Forwarded-Proto: https` produce `url: https://evil.example/display/p/<uuid>` — but inert, and deliberately so. `PUBLIC_BASE_URL` pins it (CONTROL confirms), the fallback is documented in-code as preserving prior behaviour, and a directory walk of `axoview-app/src` finds **no** consumer that reads `.url` off a `shareDiagram` result: `AppToolbar` calls `shareUrlFromUuid`, which re-anchors to the page origin. Filed nothing per the "true but unreachable" rule; the probe pins the grep contract so a future consumer re-opens it. |
| SHARE-15 | `patchDiagram` merges the request body over the stored document with no key filter, so a PATCH can write `shareUuid` directly — pointing diagram A at diagram B's snapshot, after which unsharing or deleting A deletes B's live snapshot while B still advertises it | boundary / identity integrity (E-block theme 1) | `patchDiagram merges body onto existing diagram, refreshes lastModified, keeps id` (asserts the merge is total; never that a reserved field is protected) | `__explore__/S2/share-01-04-15.explore.spec.js` | BUG | `patchDiagram` accepts `{shareUuid: <another diagram's uuid>}` with 200 and stores it. Unsharing the impostor then deletes the victim's snapshot (404 afterwards) while the victim's document still records the uuid, and re-sharing the impostor **republishes the victim's live link with the impostor's content** (`title` and `sourceId` both change). CONTROL: the same PATCH cannot change `id` — that field IS protected, so the merge is selectively unguarded. known_issues: *A PATCH can point one diagram at another's share link, or publish over it* |

## Product questions (SUSPECT verdicts)

**SHARE-10 — should `ENABLE_SERVER_STORAGE=false` revoke already-published share links?**

Measured against the real Express server on an unchanged `STORAGE_PATH`: with storage
disabled, `GET`/`POST /api/diagrams`, `GET /api/diagrams/:id`,
`POST /api/diagrams/:id/share`, `DELETE /api/diagrams/:id/share` and `GET /api/folders`
all return 503, while `GET /api/public/diagrams/<uuid>` returns 200 with the full
snapshot body. The route is mounted `{ requireStorage: false }` — a deliberate flag,
sharing the exemption with `/api/config`.

Both readings are defensible. "Storage disabled" can mean *the diagram API is off*,
in which case a published snapshot is a separate artifact and should keep working;
or it can mean *this instance serves nothing*, in which case a live public read is a
surprise. Nothing in ADR 0009 or ADR 0010 picks a side. What is not defensible either
way is the asymmetry as it stands: the read survives but `DELETE …/share` does not, so
an operator who flips the switch to take content down has **no route left through
which to revoke a link** — the only remedy is deleting files on disk by hand.

**Recommendation:** keep the public read exempt (it is the least surprising reading for
a snapshot that was deliberately published) but exempt `DELETE /api/diagrams/:id/share`
alongside it, so revocation is always reachable. If the owner prefers the stricter
reading, gate the read on `STORAGE_ENABLED` too and say so in `docs/deployment.md` —
either way the pair should move together.

> **CLOSED 2026-07-30 — owner decisions** ([DECISIONS.md](../DECISIONS.md)): SHARE-10 — keep the public read exempt AND exempt DELETE /api/diagrams/:id/share alongside it so revocation is always reachable; document the pair in deployment.md.

**Next:** area closed — nothing outstanding.

## Remediation — FIXED 2026-07-30 (wave 2)

All twelve bugs and the SHARE-10 ruling shipped in `6878df1c`; the CHR-08
runtime-config key landed with the S3 cluster. Per-entry notes are in
`known_issues.md`.

**Probes retired** (ADR 0047 §1 flip rule). The five `__explore__/S2` specs are
gone; their coverage lives in the default suites as
[`routes.shareIntegrity.spec.js`](../../../packages/axoview-backend/src/__tests__/routes.shareIntegrity.spec.js)
(handler tier) and
[`server.wiring.spec.js`](../../../packages/axoview-backend/src/__tests__/server.wiring.spec.js)
(the real Express server, booted as a child process — the probe's rig, kept
whole, because middleware ordering cannot be answered below it). SHARE-07's
worker probe folded into the worker's own `app.spec.ts`. The two FALSIFIED
hypotheses (SHARE-13, SHARE-14) went with them and were not promoted.

**Two notes worth carrying forward.**

1. The SHARE-03/04 fix is a **single-process** mutex. It closes the window the
   probes measured, and it is the right fix for the Express/Docker target as
   deployed — but a multi-worker deployment can still interleave, and the
   durable answer remains per-folder documents or a CAS-capable adapter. Said
   plainly in the entries rather than left implicit in the code.
2. SHARE-11's whitelist was inverted to a **deny-list** rather than extended.
   The entry asked for "derive it from the model schema"; that would mean
   importing the lib's zod model into a package that is deliberately
   dependency-free and shared with the Worker. A deny-list of server-owned
   fields gets the same "the next schema field is not silently dropped"
   property without the dependency.
