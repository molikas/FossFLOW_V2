# R1 — Projection & coordinate transforms (iso/2D/screen, off-grid)

**Status:** DONE · **Counted hypotheses:** 15 / 10 · **Bugs:** 6 · **Hypothesis ID prefix:** `PROJ-`

**Scope:** The two CoordinateTransformStrategy objects (isometricStrategy, cartesian2DStrategy) define tile->SceneLayer-px (toScreen), the inverses (fromCanvasPoint, fromScreen), and grid assets. CanvasModeContext binds them into getTilePosition/screenToTile/getProjectionCss for every consumer. renderedGeometry.ts is the single source of truth for composing the ADR-0023 off-grid `offset` (a post-projection SceneLayer-px residual) onto the integer tile: getRenderedTilePosition, getRenderedAreaCorners, footprintContainsPoint. useIsoProjection turns a tile range into CSS (position + iso matrix, or 2D with the Y-orientation rotate branch). getCanvasModeSwitchScroll preserves the viewport-centre tile across an iso<->2D switch.

**Code:**
- `packages/axoview-lib/src/utils/coordinateTransforms.ts`
- `packages/axoview-lib/src/contexts/CanvasModeContext.tsx`
- `packages/axoview-lib/src/hooks/useIsoProjection.ts`
- `packages/axoview-lib/src/utils/renderedGeometry.ts`
- `packages/axoview-lib/src/utils/isoMath.ts`
- `docs/adr/0023-off-grid-positioning-and-collision.md`

**Dedupe baseline:** [coverage-baseline.md](../coverage-baseline.md) sections — E2E: *Canvas projection modes / rendering / viewport*, *Drag / collision / off-grid / snap*; Unit: *Lib geometry, coordinates, hit-testing & spatial utils*. Then grep the suites directly.

## Seed seams (2026-07-29 mapping — starting points, not a ceiling)

- isometricStrategy.fromScreen's y formula floors a composite `-Math.floor((projY + scaledH/2)/scaledH + projX/scaledW)` while x floors a different composite — the toScreen->fromScreen round-trip is asymmetric near tile boundaries; any hit-test or cull that assumes exact inversion at diamond edges can be off by one tile.
- renderedGeometry.ts hardcodes the iso matrix at 3-decimal precision (ISO_A=0.707, ISO_B=-0.409; same in NodesCanvas' ISO const) while getTilePosition uses the exact halfW=PROJECTED_TILE_SIZE.width/2 ratio — documented drift ~0.05px per tile of extent, so large rect/text-box corners slowly diverge from tile-projected node positions.
- `offset` is a SceneLayer-px residual applied AFTER projection (plain vector add in both modes); any consumer that rounds it into a tile or applies it pre-projection re-creates the 2026-07 bug cluster — renderedGeometry.contract.test.ts guards hand-rolled `getTilePosition(tile) + offset` compositions, but new code paths that never compose the offset at all (omission) are not caught by that grep-style contract.
- applyOriginOffset half-sizes differ per mode (iso halfW≈70.75/halfH≈40.95 vs 2D 50/50) — code that caches an origin-adjusted position across a canvas-mode switch, or mixes origin 'LEFT'/'TOP' constants between modes, misplaces by tens of px.
- useIsoProjection's 2D Y-orientation branch (`translateX(pxSize.height) rotate(90deg)`) is a special-cased transform with no iso-matrix equivalent — text boxes with orientation Y are the only consumer; selection bounds vs rendered content can mismatch if the branch and TransformControls disagree.
- getCanvasModeSwitchScroll round-trips a fractional tile through fromCanvasPoint/toScreen; the `if (!zoom)` degenerate guard returns the raw scroll — a zoom of 0/NaN sneaking in elsewhere silently skips re-projection.
- -0 handling is scattered (`+ 0`, `|| 0` in 2D toScreen/fromScreen) — new inverse-transform code that omits it produces -0 tile coords that fail strict-equality tile comparisons.
- (mapper note) screenToCanvasPoint/cursorCanvasPoint bridge raw mouse px to the off-grid space; hit-testing (hitDetection.ts) and placement (resolvePlacement.ts) consume them — bugs here surface as selection/placement misses rather than visual errors.

## Related harvested invariants (keyword-matched hint list — scan the [full table](../coverage-baseline.md#invariants) too)

- **(canvas-interaction.md §5.2 (computeNodeUpdates))** Node-group drag collision is all-or-nothing — any colliding target means NO node moves that frame; preview always holds the last non-colliding position, and that exact position commits on mouseup. → *Mixed group (nodes + rectangle) dragged into a collision: nodes freeze at the last valid preview but textbox/rectangle/anchor members are explicitly not collision-gated and keep translating — on mouseup the group's rigid relative offsets are torn apart. DragItems tests assert the node all-or-nothing rule in isolation, never mixed-type group rigidity across a blocked frame.*
- **(ADR 0023 §1 + §7 acceptance / clipboard)** Off-grid fields (offset/snap/collides) survive every persistence and duplication path; absent fields default to snapped/colliding. → *Ctrl+C/Ctrl+V (or context-menu Duplicate) of an unsnapped off-grid node: clipboard.ts/useCopyPaste reconstruct view items — if they omit offset/snap/collides, the paste silently re-snaps and re-enables collision. The ADR's round-trip test covers export→import, and ADR 0044 names clipboard as 'the real risk site' for iconScale — the same hole exists for the 0023 trio with no clipboard round-trip test.*
- **(ADR 0023 addendum B + renderedGeometry.contract.test.ts)** No file outside utils/renderedGeometry.ts may hand-roll tile+offset composition; an offset must never be rounded into a tile. → *The contract test greps three specific regex patterns (offset?.x ?? 0, base.x + it.offset.x, ${it.offset.x}px). A new composition written as `const {x: ox = 0} = item.offset ?? {}; left = px.x + ox` evades all three regexes while violating the invariant — the regex gate rots silently as coding style shifts.*
- **(ADR 0023 Consequences + addendum D)** A connector anchored to an offset/unsnapped node must resolve to the RENDERED (offset) endpoint on both the WebGL bulk path and the DOM selected path. → *Select a connector attached to an off-grid node: the sparse DOM <Connector> (selection halo) and the WebGL ConnectorsCanvas body must agree on the endpoint. If one resolves via bare tile and the other via rendered position, selecting/deselecting makes the wire visibly jump at the node. The invariant suite asserts DOM/WebGL equality for rectangle vertices, not connector endpoints at offset nodes.*
- **(testing.md ADR 0023 additions (snap-grid.spec freeze test))** Turning global snap ON does not re-snap existing off-grid items. → *A future load-time normalization or 'cleanup' migration that drops offset when snap is globally on — the e2e freeze test covers the toggle in-session, but a load-path normalization (e.g. in useInitialDataManager, where seeds already run) would re-snap on reload and no reload-with-global-snap-on test exists.*
- **(rendering guidelines §8)** Any GPU layer whose geometry is projected must list strategy.projectionName in its rebuild deps; a DOM hit-proxy and its GPU paint must share one projection. → *Switch iso→2D in VIEW mode using the new viewer projection toggle (PR #84): if any of the four bulk canvases (or a future one) omits projectionName from deps, its paint stays in the old projection while hit-proxies move — the exact Labels bug, now reachable from a brand-new code path (PreviewCanvasModeToggle) that no e2e drives through all four layers.*
- **(technical-review-2026-07-29 finding #5 (useCanvasModeToggle))** Exactly ONE useCanvasModeToggle consumer may be live at a time — two mounted simultaneously each apply the scroll correction, double-jumping the viewport. → *A future surface (mobile chrome, export dialog's hidden Axoview instance, or the present-chrome toggle rendered alongside ToolMenu in some mode combination) mounts a second consumer: every projection switch jumps the viewport by 2× the correction. The invariant is comment-only; the review names it as the same unenforced-invariant class as the 0023 offset cluster, with the contract-test remedy still unwritten.*
- **(ADR 0044 §4 (iconScale resolution order))** Every render reader resolves viewItem.iconScale ?? icon.scale ?? 1 — DOM icon paths, WebGL NodesCanvas, selection-ring extent, and image export must agree. → *A reader outside the four audited ones — the hover outline box, NodeLabelHitLayer's chip stalk anchor, or the ADR 0012 popover's side-anchor offset — computes extent from icon.scale only: a per-node-resized icon shows a ring/label/popover anchored to the wrong extent. Unit tests cover the mode math and schema round-trip; a missed reader is visual-only and CI is pixel-blind (§11).*
- **(ADR 0044 §6 + ADR 0023 addendum (hit-testing))** Icon resize is visual-only — the node keeps a single-tile footprint for collision/hit/anchoring; meanwhile off-grid hit-testing compares px against RENDERED footprints. → *An enlarged (2.5×) off-grid node: getItemAtTile's px footprint test and the tile-footprint rule pull in opposite directions — if hover/selection chrome traces the scaled extent (ADR 0044 third pass) but click hit-testing stays tile-sized, the user can hover-highlight a spot they cannot click. The renderedGeometry invariant suite's corpus predates iconScale and doesn't parametrize over it.*
- **(canvas-interaction.md §5.9 checklist item 5 (Label preview channel))** Canvas-drawn elements publish a live drag preview channel (labelMove/labelMoves) that must be cleared on BOTH mouseup (after commit) and exit (escaped drag). → *Abort a group drag containing Labels via a mid-drag tool hotkey (DragItems.exit path): if clearLabelMoves runs only in the mouseup handler, the stale labelMoves map keeps painting Labels at the aborted offset while the model has them at origin — 'the element jumps when I next touch it'. DragItems.modes.test covers the commit path; the escaped-drag clear is the checklist's own warned gap.*
- **(ADR 0022 addendum (arrow keys) + ADR 0006)** Arrow keys nudge a selected ITEM/RECTANGLE/TEXTBOX one tile per press as a single-undo transaction; with nothing selected they pan. → *Arrow-nudge a selected floating LABEL or an off-grid (offset) item: Labels are absent from the enumerated nudge types (the recurring wired-into-some-surfaces bug), and nudging an off-grid item by one integer tile while keeping its offset may collide-check against the wrong cell. No test nudges a Label or an offset item.*
- **(features.md (viewer-controlled projection, PR #84))** canvasMode in view-only mode is viewer-local UI state persisted only to that viewer's localStorage — switching projection can neither dirty nor save the diagram. → *A viewer switches to 2D on the /display route; the localStorage key is shared with the editor — the OWNER later opens the editor in the same browser and their diagram opens in 2D with a recentered scroll they never chose, and if any editor-side code treats canvasMode as model-adjacent (e.g. included in a future save payload or dirty-diff), a pure viewer action dirties the document. Tests assert non-dirty in view mode only, not the shared-key bleed into edit mode.*

## Known coverage gaps (from the baseline inventory)

- (Drag / collision / off-grid / snap) Escape mid-node-drag cancels and restores the origin position
- (Drag / collision / off-grid / snap) Multi-select drop where only SOME target tiles are occupied (partial collision)
- (Drag / collision / off-grid / snap) Collision semantics between a node and an off-grid/unsnapped neighbour during drag
- (Drag / collision / off-grid / snap) Drag at extreme zoom-out / near canvas edge
- (Drag / collision / off-grid / snap) An explicit 're-snap this item' action
- (Canvas projection modes / rendering / viewport) Mouse-wheel zoom anchoring at the cursor position
- (Canvas projection modes / rendering / viewport) Zoom clamp bounds via buttons/wheel (only touch pinch asserts [0.1,1])
- (Canvas projection modes / rendering / viewport) Fit-to-view exact framing (all items visible) — only 'value changed' asserted
- (Canvas projection modes / rendering / viewport) Projection mode persistence across reload
- (Canvas projection modes / rendering / viewport) 2D label stalk rendering parity
- (Lib geometry, coordinates, hit-testing & spatial utils) packages/axoview-lib/src/utils/pathfinder.ts — zero tests (connector path routing algorithm)
- (Lib geometry, coordinates, hit-testing & spatial utils) packages/axoview-lib/src/utils/hitDetection.ts — zero direct tests (only exercised indirectly through renderer.test getItemAtTile)
- (Lib geometry, coordinates, hit-testing & spatial utils) packages/axoview-lib/src/utils/coordsUtils.ts, sizeUtils.ts, selectableRefs.ts, labelChip.ts, connectorLabels.ts, renderProbe.ts, tooltipWithShortcut.ts — zero tests

## Hypotheses

| ID | Hypothesis | Source | Nearest existing tests | Probe | Verdict | Evidence |
|----|-----------|--------|------------------------|-------|---------|----------|
| PROJ-01 | `getProjectBounds` extends a text box by `+size.height` in tile-Y, but a text box grows to `tile.y − (height−1)` (`getTextBoxEndTile`), so the project bounding box excludes a multi-line box's lower rows and pads empty space above them — fit-to-view and Export-Image both frame the wrong region | boundary + coordinate-convention | *none* — `getProjectBounds`/`getUnprojectedBounds`/`getFitToViewParams` have zero tests; `isoMath.test.ts` covers `getTextBoxEndTile` in isolation | src/__explore__/R1/bounds-proj-01-02-03-04.explore.test.ts | BUG | a 6-row text box at tile.y=0 gives bounds lowY=-3 / highY=+9: its own rows below the anchor are OUTSIDE the frame and 6 empty tiles above are inside; the miss grows 1:1 with row count. Consumers: fit-to-view and ExportImageDialog. known_issues: *The project bounding box mis-frames the diagram* |
| PROJ-02 | `getProjectBounds` enumerates items/connectors/rectangles/textBoxes but not floating **labels**, so a Label dragged outside the item bounds is outside fit-to-view and outside the exported image | cross-feature (labels × projection) | *none* (see PROJ-01); `label-entity.spec.ts` never fits or exports | src/__explore__/R1/bounds-proj-01-02-03-04.explore.test.ts | BUG | item at (0,0) + label at (40,40) -> bounds are exactly (-3,-3)..(3,3); the label is 37 tiles outside. Same known_issues entry. |
| PROJ-03 | `getProjectBounds`/`getUnprojectedBounds` ignore the ADR-0023 `offset` (offset-omission class, un-audited consumer), so an off-grid item at the diagram edge is clipped by up to half a tile in fit-to-view and export | bug-class recurrence (offset-omission) | `renderedGeometry.contract.test.ts` (grep gate, does not see an *omission*); `renderedGeometry.invariant.test.tsx` (render/hit consumers only) | src/__explore__/R1/bounds-proj-01-02-03-04.explore.test.ts | FALSIFIED | the composition IS omitted (off-grid and snapped views give byte-identical bounds) but PROJECT_BOUNDING_BOX_PADDING = 3 TILES per side absorbs the largest possible residual (half a tile) with >2x margin, so nothing is ever clipped. Latent invariant gap, not a reachable defect. |
| PROJ-04 | `getUnprojectedBounds` feeds **pixel** corner positions to `getBoundingBoxSize`, which adds the inclusive **tile-count** `+1`, so the reported project width/height are each 1 px too large (the same unit-mix `getFitToViewParams` fixed for the centre but not for the size) | boundary | *none* (see PROJ-01) | src/__explore__/R1/bounds-proj-01-02-03-04.explore.test.ts | BUG | getUnprojectedBounds().width === projected-px-extent + 1 exactly, on both axes; the +1 propagates into the fit-to-view zoom. Cosmetic magnitude. Same known_issues entry. |
| PROJ-05 | In 2D mode a Y-orientation text box is drawn via `translateX(pxSize.height) rotate(90deg)`, which lands its content in a footprint **perpendicular** to the tile range selection/hit-testing use, so the drawn text sits outside its own selection box and clicking it misses | parity (iso vs 2D) + seed seam | `TransformTextBox.test.ts` (orientation flip only), `renderedGeometry.invariant.test.tsx` (X orientation only), `canvas-modes.spec.ts` (nodes only) | src/__explore__/R1/geometry-proj-05-10-11-12.explore.test.tsx | BUG | confirmed, though narrower than predicted: the 2D-Y branch in `TextBox.tsx` drops `size.height` entirely (`from = tile`), so the drawn wrapper is ALWAYS 1 tile thick after the rotate, while `getTextBoxEndTile` gives the hit/selection range `size.height` tiles of thickness. For a 4-line box the tile range claims 4 tiles where 1 is drawn: a click 2 tiles beside the visible text still selects it, and rows 2..N are drawn outside the 100px wrapper. X orientation in 2D is unaffected (the branch is Y-only). |
| PROJ-06 | `getRenderedAreaCorners` keeps 3-decimal ISO constants (0.707/0.409) while `getTilePosition` uses the exact 0.7075/0.4095 ratio, so a rectangle's drawn quad drifts ~0.05 px per tile of extent; at a realistic extent (≥20 tiles) the far corner is >1 px away from a node drawn on that same tile | seed seam + sibling drift | `renderedGeometry.invariant.test.tsx` "WebGL rect corners carry the offset" (compares the two *offset* paths, both of which use ISO_A/B) | src/__explore__/R1/transforms-proj-06-13-14-15.explore.test.ts | SUSPECT | drift confirmed at exactly hypot(0.05,0.05) px per tile of width: a 20-tile rectangle's far corner is 1.41 px from the exact projection of the tile it claims, a 40-tile one 2.83 px. It never flips a one-tile hit-test (141x82 px footprint) and shrinks with fit-to-view zoom, and the code comment says the 3-decimal constants were kept deliberately so extraction changed no pixel. No spec says which side wins -> product question. |
| PROJ-07 | `offset` is a post-projection SceneLayer-px residual and is **not** re-projected on an iso↔2D switch, so an item nudged off-grid in ISO lands at a different sub-tile position in 2D (up to 70 px of a 100 px tile) and can visually overlap a neighbour it did not overlap in ISO | metamorphic (op-then-switch ≡ switch-then-op) | `canvas-mode-zoom-preserve.spec.ts` (zoom/centre only), `off-grid-pointer.spec.ts` (ISO only), `snap-grid.spec.ts` (ISO only) | tests-exploratory/R1-projection/proj-07-08-09-15.explore.spec.ts | SUSPECT | confirmed as described, but no contract says which way it should go. A real drag committed a residual of (58, 0) SceneLayer px — inside the ISO tile diamond (58/70.75 = 0.82) and outside the 2D tile square (58 > 50) — and the iso->2D switch carried it byte-identical, tile unchanged. So an item drawn inside its own cell in ISO is drawn mostly over the NEIGHBOURING cell in 2D, where tile-based collision will happily let a second item sit. ADR 0023 defines `offset` as a post-projection px residual deliberately, and nothing specifies projection-switch semantics -> product question. |
| PROJ-08 | The wheel handler is bound to `rendererEl`, whose box spans the whole window with the docks rendered **inside** it, and it never asks whether the event was on the canvas (standing thread B), so a mouse wheel over the Elements / Layers panel zooms the diagram instead of scrolling the panel | standing thread B + coverage gap | `viewport.spec.ts` (zoom **buttons** only — no wheel test exists anywhere) | tests-exploratory/R1-projection/proj-07-08-09-15.explore.spec.ts | FALSIFIED | standing thread B does not reproduce here. A point over the open Elements panel that IS inside the renderer element’s bounding rect exists (the characterization finds one), but a wheel there leaves `zoom` untouched, while the identical gesture on the canvas changes it (positive control in the same test). The wheel listener is bound to the renderer ELEMENT and delivery follows DOM containment, not rect overlap; the left dock overlaps the rect without being a descendant. |
| PROJ-09 | Wheel zoom-to-cursor does not keep the tile under the cursor fixed — the anchor math mixes `rendererRef.getBoundingClientRect()` with the ResizeObserver `rendererSize`, so an off-centre cursor drifts by a tile or more per step | coverage gap (*Mouse-wheel zoom anchoring at the cursor position*) | `viewport.spec.ts` (buttons), `touch-pinch-zoom.spec.ts` (pinch centroid, touch path only) | tests-exploratory/R1-projection/proj-07-08-09-15.explore.spec.ts | FALSIFIED | four real wheel steps at a point 72% across / 30% down the canvas: `zoom` drops each step, `scroll.position` moves (so the anchoring code really ran), and the tile under the cursor is identical before and after. `rendererRef.getBoundingClientRect()` and the ResizeObserver `rendererSize` measure the same element, so there is no mismatch to exploit. |
| PROJ-10 | `itemAtPoint` resolves an ITEM hit by `scene.items` array order (last wins) and ignores the real paint order (layer bucket > zIndex > isoDepth, `renderOrder.ts`), so with two overlapping items the click selects the one painted **underneath** | sibling drift (hit order vs paint order) | `renderer.test.ts` (stacked **rectangles** honour zIndex; items only via the raw tile index), `renderOrder.test.ts` (paint order alone) | src/__explore__/R1/geometry-proj-05-10-11-12.explore.test.tsx | BUG | two items sharing one tile (reachable with `collides: false`): `itemAtPoint` scans `scene.items` backwards and returns the LAST array entry regardless of `zIndex`, while `NodesCanvas` paints in `resolveRenderOrder(layer, zIndex, isoDepth)` order. The item with `zIndex: 5` is drawn on top and the click selects the `zIndex: 0` one underneath; swapping the array order flips the verdict. Sibling drift inside one function — the RECTANGLE branch of the same `getItemAtTile` explicitly re-sorts into paint order first. |
| PROJ-11 | `tileFootprintAt` sizes an item's hit footprint from the tile alone and ignores `iconScale` (ADR 0044), so a node resized to 2.5× shows hover/selection chrome over its scaled extent but is only clickable on its single tile — hover-highlightable pixels that cannot be clicked | seed invariant (ADR 0044 §6 × ADR 0023 hit-testing) | `renderedGeometry.invariant.test.tsx` (corpus predates `iconScale`), `icons.spec.ts` (resize round-trip, no hit-test) | src/__explore__/R1/geometry-proj-05-10-11-12.explore.test.tsx | FALSIFIED | the predicted asymmetry does not exist: hover is resolved by the same `getItemAtTile` call as the click, so a pixel outside the tile diamond is neither hoverable nor clickable. The one-tile hit footprint is an explicit ADR 0044 section 6 contract (icon resize is visual-only), so the chrome-vs-hit difference is specified rather than drifted. Residue worth noting but not filing: at 2.5x about six sevenths of the drawn icon is inert. |
| PROJ-12 | A connector anchored to an off-grid node resolves its endpoint through `connectorEndpointVertexDelta` on one path and the bare tile on the other, so selecting the connector (DOM sparse path) makes the wire jump at the node relative to the WebGL bulk path | seed invariant (ADR 0023 addendum D) | `renderedGeometry.invariant.test.tsx` (WebGL/DOM parity for **rectangle** vertices only), `connector-parity.spec.ts` (on-grid) | src/__explore__/R1/geometry-proj-05-10-11-12.explore.test.tsx | BUG | `connectorEndpointVertexDelta` is correct (it inverts the projection exactly in both modes) but has exactly ONE caller: the DOM `Connector.tsx`. `ConnectorsCanvas.tsx` maps every path tile straight through the projection and never reads a view item's `offset`. Since `Renderer.connectorHybridIds` promotes a connector to the DOM path the moment it is selected, selecting a connector attached to an off-grid node moves its endpoint by the full node offset (41.6 px for a (37,-19) residual). This is ADR 0023 addendum D's invariant, previously unasserted. |
| PROJ-13 | `toScreen → fromScreen` is not an exact inverse across the zoom ladder: at `incrementZoom`-produced zooms (0.05 steps down to 0.1) the round-trip lands on a neighbouring tile for some tiles, so a click at low zoom selects the tile next to the one under the cursor | boundary sweep | `coordinateTransforms.test.ts` round-trips **one** tile per mode at zoom 1, scroll 0; `renderer.test.ts` screenToIso across a few zoom/scroll combos | src/__explore__/R1/transforms-proj-06-13-14-15.explore.test.ts | FALSIFIED | 625 tiles x the full decrementZoom ladder (1.0 down to 0.1, 19 steps) x both projections x fractional non-tile-multiple scrolls: zero round-trip failures. toScreen -> fromScreen is exact at tile centres everywhere the UI can go. |
| PROJ-14 | `-0` guards are applied inconsistently (`2D toScreen` guards y not x; `isometricStrategy.fromScreen` guards y not x; `isoMath.screenToIso` guards neither and `renderer.test.ts:127` pins `y: -0`), so a `-0` tile coordinate reaches the `${x},${y}` spatial-index key and `"-0,0"` misses the item stored at `"0,0"` | boundary + sibling drift | `renderer.test.ts:127` asserts the `-0` *exists*; nothing asserts a consumer survives it | src/__explore__/R1/transforms-proj-06-13-14-15.explore.test.ts | FALSIFIED | the premise is wrong: `String(-0)` is `"0"`, so a -0 tile can never poison a `${x},${y}` index key, and `-0 === 0` for every comparison in the codebase. The legacy `screenToIso` really does emit `y: -0` and the strategy path really does guard it, but no consumer can observe the difference. Seed seam corrected. |
| PROJ-15 | `getRenderedDragTransform` interpolates the residual straight into `calc(... + ${offset.x}px)`; a value that stringifies in exponent form (`1e-7`, `1e+21`) is invalid CSS, so the **whole** transform is dropped — the item snaps back to its grid cell and the live drag delta stops applying too | degenerate input | `renderedGeometry.invariant.test.tsx` (well-formed offsets only) | src/__explore__/R1/transforms-proj-06-13-14-15.explore.test.ts + tests-exploratory/R1-projection/proj-07-08-09-15.explore.spec.ts | FALSIFIED | falsified by the browser, not by the arithmetic. The placement residual really can land in the exponent range (4.547473508864641e-13 at zoom 0.7) and `getRenderedDragTransform` really does interpolate it unguarded — but CSS Values 4 permits scientific notation and Chromium implements it: the computed transform is `matrix(1, 0, 0, 1, 25, 0)`, i.e. accepted, with the live drag var intact. (A declaration containing `var()` always round-trips at the inline level, so `getComputedStyle` is the only valid oracle — the first pass got this wrong.) `Infinity` IS rejected, but only an unvalidated imported model can supply it, which is the already-filed CLIP-14/15 class. |

## Product questions (SUSPECT verdicts)

| ID | Question | State |
|----|----------|-------|
| PROJ-07 | Should the off-grid `offset` be re-projected when the canvas switches between ISOMETRIC and 2D? | **OPEN** — it is not today: a residual of (58, 0) px sits inside the ISO tile diamond but 8 px outside the 2D tile square, so an item drawn inside its own cell in ISO is drawn mostly over the neighbouring cell in 2D, and tile-based collision will let a second item occupy that cell. ADR 0023 makes `offset` a post-projection px residual on purpose (it is what makes composition a plain vector add in both modes), so re-projecting it is a real design change: the alternative is to clamp the residual to the *smaller* of the two cell shapes at commit time. Recommendation: re-project on switch (`toScreen_new(fromCanvasPoint_old(offset))`) — it preserves the item's logical sub-tile position, which is what the user chose, and it is the same one-line map `getCanvasModeSwitchScroll` already uses for the viewport centre. |
| PROJ-06 | Should the ISO area quads use the exact projection ratio (0.7075 / 0.4095) instead of the 3-decimal constants moved verbatim from RectanglesCanvas? | **OPEN** — the drift is exactly hypot(0.05, 0.05) px per tile of width, so a 20-tile rectangle draws its far corner 1.41 px away from where a node on that same tile is drawn, and a 40-tile one 2.83 px. It never flips a hit-test (a one-tile footprint is 141 x 82 px) and fit-to-view shrinks it further. The code comment says the constants were kept deliberately so the extraction changed no rendered pixel — switching to the exact ratio costs nothing at runtime but does move pixels, which is a product call. |
