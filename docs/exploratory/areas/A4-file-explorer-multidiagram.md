# A4 — File explorer, folders & multi-diagram management

**Status:** DONE · **Counted hypotheses:** 16 / 10 · **Bugs:** 15 · **Hypothesis ID prefix:** `FEX-`

**Closed 2026-07-30.** All 16 rows carry a verdict: 15 BUG, 1 FALSIFIED (FEX-07), 0 SUSPECT. FEX-16 was added by anomaly capture while probing FEX-11. The `FileExplorer.tsx` rows were probed through the real component with a react-arborist capture stub — see `__explore__/A4/harness.tsx`, whose header records the two rig traps this cost (jsdom has no `ResizeObserver`; `[...map]` and `for…of` over a Map silently yield nothing under the app package's `es5` ts-jest target).

**Historical (pre-close):** FEX-01..07 closed first (all of `useFileTree`, which had zero tests, plus the two dead-path sweeps). The remaining eight rows — FEX-08..15 — are all inside the 1306-line `FileExplorer.tsx` (DnD `handleMove`, `confirmDelete` ordering, `handleRenameSubmit`, `placeOfId`, `handleMoveToDrive`, `driveRootMissing`, the MigrateSessionDialog auto-offer) and each needs the component rendered with two `useFileTree` instances and react-arborist mounted. Start from the A1 harness pattern (`__explore__/A1/harness.tsx`) for the provider stubs and add a `FileExplorer` render helper; the DOM hooks that already exist are `dock-file-explorer-toggle` (lib LeftDock), `file-explorer-row` + `data-diagram-name` + `data-diagram-type`, `file-explorer-rename-input`, `file-explorer-context-menu-*`, `file-explorer-delete-confirm{,-dialog,-cancel}` and `file-explorer-tree` — see `packages/axoview-e2e/pom/FileExplorerPOM.ts`, which documents the surface better than the component does.

**Rig note.** `filetree-fex-01-to-07` drives `useFileTree` through `renderHook` with a `rig()` storage double that can reject a *named* listing call (`r.failing.add('getTreeManifest')`) — that per-call granularity is what separates FEX-04 from a generic load failure. Its `flatIds()` walker is how to assert "is this row anywhere in the tree", since a missing node and a node nested three levels down look the same to a shallow check.

**Scope:** One react-arborist tree composed from TWO useFileTree instances (session + Drive) with synthetic 'place' section roots and 'placeState' rows (loading skeletons, signin/reconnect/scope/error/setup/empty). placeOfId maps flat ids to places; create/rename/delete/duplicate/share/export route to the node's place provider. DnD supports within-place moves plus exactly one cross-place gesture (session diagram -> Drive = moveDiagramsToDrive MOVE). Inline create via injected '__pending__' node submitted through the rename handler. MigrateSessionDialog bulk-moves session diagrams to Drive after a fresh sign-in. SessionStorageGauge estimates sessionStorage usage via the 'axoview-session-changed' event.

**Code:**
- `packages/axoview-app/src/components/fileExplorer/FileExplorer.tsx`
- `packages/axoview-app/src/hooks/useFileTree.ts`
- `packages/axoview-app/src/components/fileExplorer/FileTreeNode.tsx`
- `packages/axoview-app/src/components/fileExplorer/FileTreeToolbar.tsx`
- `packages/axoview-app/src/components/fileExplorer/ContextMenuItems.tsx`
- `packages/axoview-app/src/components/fileExplorer/SessionStorageGauge.tsx`
- `packages/axoview-app/src/components/MigrateSessionDialog.tsx`

**Dedupe baseline:** [coverage-baseline.md](../coverage-baseline.md) sections — E2E: *File explorer / diagram management*, *Multi-diagram links + element links*; Unit: *App utils, runtime config & shell components/dialogs*. Then grep the suites directly.

## Seed seams (2026-07-29 mapping — starting points, not a ceiling)

- Name-collision dialog says 'Replace it?' but confirmMove only calls moveItem — the existing same-name sibling is NOT deleted, producing two identically-named items after the user confirmed a 'replace'
- handleMove iterates dragIds but `return`s (not continue) on same-parent reorder and on collision — a multi-select drag silently abandons the remaining dragged items
- confirmDelete calls notifyDiagramDeletedFromTree (canvas reset, autosave cancel) BEFORE the storage delete — if hardDeleteDiagram then fails, the diagram survives in storage but the canvas has already been blanked
- handleRenameSubmit decides folder-vs-diagram via `tree.folders.some(f => f.id === id)` against possibly-stale state, and resolves the place via placeOfId which drops entries mid-refresh — rename can hit the wrong entity type or wrong provider
- handleMoveToDrive on the OPEN diagram: saveAllDirty flush -> move -> notifyDiagramDeletedFromTree -> reopen from Drive; an autosave tick or edit landing between the flush and the source delete is lost (moveDiagramsToDrive copies the persisted blob, not the in-memory model)
- moveDiagramsToDrive recreates folder paths by NAME match against a locally-mutated Drive listing — duplicate-named Drive folders resolve to the first hit, and a concurrent tab's creations aren't seen (stale one-shot listing per run)
- driveRootMissing keys on the synchronous getCachedRootId() while driveTree.status is async — timing window right after DriveSetupGate configures the root where the tree shows 'setup' state over real data or vice versa
- MigrateSessionDialog auto-offer races the DriveSetupGate: pendingOfferRef consumed by tryAutoOffer only if getCachedRootId() is set; the 'axoview-drive-root-ready' event and the auth-status effect can interleave so the offer fires zero or two times
- placeOfId: if the same id somehow exists in both places (import/copy artifacts), Drive wins (last Map.set) — session node operations route to the Drive provider

## Related harvested invariants (keyword-matched hint list — scan the [full table](../coverage-baseline.md#invariants) too)

- **(ADR 0001 import semantics §1)** Project-zip import rewrites every ID and updates all cross-references: folderId, parentId, and cross-diagram link refs inside diagram models. → *Cross-diagram links now also live in Quill content HTML (ADR 0034 link-to-diagram in text runs and link cards) and in connector labels' headerLink — the importer's rewrite list predates these surfaces ('item-level link fields, view connector refs'). Import a zip whose text-box content links to a sibling diagram: the href still carries the OLD id → dead link. projectZip.test asserts ID rewriting for the original ref sites only.*
- **(ADR 0036 §2 + known_issues (root-folder detection))** ADR 0036 promises the provider detects a deleted/trashed Drive root folder; as-built, isAvailable() only checks auth and the cached root id is never revalidated. → *Trash the app folder in Drive's own UI mid-session: autosaves keep 200-OK patching files in the trash for the rest of the session; loss surfaces only at next full listing. The cheap fix (invalidate on zero-listing or 404) is catalogued but unimplemented — any test asserting 'save succeeded ⇒ durable' is false here.*
- **(local-mode autosave / session keys (known_issues e2e entry + features.md))** Session-place work autosaves to localStorage and survives reloads; explorer persistence is a coherent triple (axoview-diagrams, axoview-last-opened, axoview-last-opened-data). → *A quota-exceeded write during autosave (5MB cap, gauge at 90%+): if the three keys are written non-atomically and one write throws, axoview-last-opened can point at an entry missing from axoview-diagrams — reload lands on the empty state or the wrong diagram while the save indicator claimed success. LocalStorageProvider tests never inject QuotaExceededError mid-triple.*

## Known coverage gaps (from the baseline inventory)

- (File explorer / diagram management) Dragging a diagram into/out of a folder
- (File explorer / diagram management) Folder rename and folder delete (incl. non-empty folder)
- (File explorer / diagram management) Deleting the currently-open diagram (what the editor shows after)
- (File explorer / diagram management) Duplicate diagram/folder names
- (File explorer / diagram management) Creating a NEW diagram from the explorer while one is open
- (Multi-diagram links + element links) Link to a diagram that is later DELETED (broken link)
- (Multi-diagram links + element links) Circular diagram links (A->B->A)
- (Multi-diagram links + element links) Removing/clearing an existing diagram link
- (Multi-diagram links + element links) headerLink survival through export/import round-trips
- (App utils, runtime config & shell components/dialogs) packages/axoview-app/src/hooks/useAutoSave.ts and useFileTree.ts — zero tests (autosave debounce/dirty logic, tree building)
- (App utils, runtime config & shell components/dialogs) packages/axoview-app/src/providers/DiagramLifecycleProvider.tsx and AuthProvider.tsx — only source-grep contract tests, no behavioral tests of load/save/dirty lifecycle
- (App utils, runtime config & shell components/dialogs) packages/axoview-app/src/components/DiagramManager.tsx and fileExplorer/FileExplorer.tsx, FileTreeNode.tsx, FileTreeToolbar.tsx, ImportDialog.tsx, ContextMenuItems.tsx, ExportProjectZipDialog.tsx, SessionStorageGauge.tsx — zero behavioral tests (rename/move/DnD/context menus)
- (App utils, runtime config & shell components/dialogs) app shell components with zero tests: AppToolbar.tsx, AuthControl.tsx, ErrorBoundary.tsx, LoadDialog.tsx, SaveDialog.tsx, ExportPopover.tsx, ExportSingleDiagramDialog.tsx, MigrateSessionDialog.tsx, DriveDisplayGate.tsx, DriveSetupGate.tsx, DriveRootFolderDialog.tsx, NotificationStack.tsx, StatusCluster.tsx, LocalModeBanner.tsx, LocalModeShareErrorDialog.tsx, PublicShareLoadErrorDialog.tsx, ReadonlyLoadErrorDialog.tsx, ConfirmDialog.tsx, NotFound.tsx
- (App utils, runtime config & shell components/dialogs) packages/axoview-app/src/utils/apiBaseUrl.ts, authDebug.ts, bootScreen.ts, downloadBlob.ts, isoGridBackground.ts — zero tests; also diagramUtils.ts, appBase.ts, serviceWorkerRegistration.ts

## Hypotheses

| ID | Hypothesis | Source | Nearest existing tests | Probe | Verdict | Evidence |
|----|-----------|--------|------------------------|-------|---------|----------|
| FEX-01 | `buildTree` only collects diagrams whose `folderId` chain reaches `null`, so a diagram with a dangling `folderId` renders nowhere — and `trashData` only holds `deletedAt` rows, so it is unreachable by any route | cross-area consumer (A2/STOR-03, A2/STOR-13, A3/ZIP-06) | none — `useFileTree.ts` has zero tests | `__explore__/A4/filetree-fex-01-to-07` | BUG | T1 confirmed. `diagrams` holds both rows; the tree holds only the folder and the reachable diagram; `trashData` is empty because it keys on `deletedAt`. So the orphan A2/STOR-03, A2/STOR-13 and A3/ZIP-06 each produce is reachable from no surface at all, while still counting against the budget and every listing. known_issues: A4/FEX-01. |
| FEX-02 | The whole soft-delete / trash machine is unreachable: every provider implements `deleteDiagram(id, true)` + `restoreDiagram`, `useFileTree` exposes `softDeleteDiagram` / `restoreDiagram` / `trashData`, and no UI calls any of them — the explorer's delete is always `hardDeleteDiagram` | dead-path sweep | none | `__explore__/A4/filetree-fex-01-to-07` | BUG | T1 + source sweep confirmed. The hook exposes `softDeleteDiagram`, `restoreDiagram` and `trashData`; a sweep of FileExplorer, FileTreeNode, FileTreeToolbar, ContextMenuItems, DiagramManager, App.tsx and DiagramLifecycleProvider finds no caller for any of the three, and the explorer's delete is `await tree.hardDeleteDiagram(target.id)` - irreversible. Every provider and the backend implement the soft path. known_issues: A4/FEX-02. |
| FEX-03 | `TreeManifest` is write-only end to end: `useFileTree` fetches it on every load (an extra request per place, plus a `findManifestId` listing on Drive) and `buildTree` sorts alphabetically instead, `FileExplorer` reads neither `manifest` nor `updateManifest`, and `importProject` ignores the copy in every zip (A3/ZIP-10) | dead-path sweep / ADR 0001 | none | `__explore__/A4/filetree-fex-01-to-07` | BUG | T1 + source sweep confirmed. A manifest ordering Bravo before Alpha is fetched (1 `getTreeManifest` call per load) and exposed on the hook, and `buildTree` still renders Alpha, Bravo; no component reads `manifest` or `updateManifest`. With A3/ZIP-10 (exported into every zip, parsed on import, applied by nothing) the type is write-only end to end. known_issues: A4/FEX-03. |
| FEX-04 | `load()`'s `Promise.all([listFolders, listDiagrams, getTreeManifest])` fails the whole tree when any one rejects, so the unread tree manifest can take down a tree that would otherwise render — and it is the consumer that turns A2/STOR-05's unguarded parse into an unusable explorer | interleaving / cross-area consumer | none | `__explore__/A4/filetree-fex-01-to-07` | BUG | T1 confirmed. With only `getTreeManifest` rejecting, `listFolders` and `listDiagrams` both ran and would have succeeded, yet `status` is `error` and `treeData` is empty - the explorer is unusable because of a value nothing reads. This is also the consumer that turns A2/STOR-05 (unguarded `JSON.parse` of `axoview-tree-manifest`) into a dead file tree. known_issues: A4/FEX-04. |
| FEX-05 | A refresh that fails after a successful load flips `status` to `'error'` while `folders`/`diagrams` still hold the previous rows, so the section renders an error state over live data (or live data under an error row) | boundary | none | `__explore__/A4/filetree-fex-01-to-07` | BUG | T1 confirmed. After a good load, a refresh whose `listDiagrams` rejects leaves `treeData` holding the previous row while `status` flips to `error` - the section is simultaneously errored and populated. The hook already draws the first-load/refresh distinction for exactly this reason ("stale rows stay visible instead of blanking"); the error path skipped it. known_issues: A4/FEX-05. |
| FEX-06 | Folders can never appear in `trashData`: no provider's `deleteFolder` sets `deletedAt` (session removes the row, Drive trashes the Drive file, the server takes `?recursive`), so the folder branch of the trash builder is unreachable | dead-path sweep | none | `__explore__/A4/filetree-fex-01-to-07` | BUG | T1 confirmed. The real session provider's `deleteFolder` removes the row and sets no `deletedAt`; the Drive provider patches the DRIVE file's `trashed` flag and the string `deletedAt` appears nowhere in it (its `listFolders` query filters `trashed=false` server-side, so a trashed folder simply vanishes). The folder branch of the trash builder can never produce a row. Filed with A4/FEX-02. |
| FEX-07 | `LocalStorageProvider`'s `uniqueSuffix` comment claims a self-parented folder makes "the recursive tree builder walk forever" — but `buildTree` starts at `parentId === null`, so a self-parented (or cyclic) folder is simply never rendered and never walked; the comment names the wrong failure mode | baseline/comment correction (thread S-f) | none | `__explore__/A4/filetree-fex-01-to-07` | FALSIFIED | A self-parented folder and an A->B->A pair produce no hang and no walk: `buildTree` descends from `parentId === null`, so nothing in a cycle is ever visited - only the reachable folder renders. The `uniqueSuffix` comment in LocalStorageProvider ("a folder whose parentId equals its own id, which the recursive tree builder then walks forever") names the wrong failure mode; the real consequence is silent invisibility, the FEX-01 shape. Note the genuine unbounded walk found this session is in `importProject`/`wipeWorkspace` (A3/ZIP-01), not here. |
| FEX-08 | Deleting the open diagram blanks the canvas *before* the storage delete, so a failed delete leaves the diagram in storage with the editor already reset and the autosave cancelled — the toast says "Delete failed" while the work is off screen | seed seam | none | `__explore__/A4/fileexplorer-fex-08-to-12` | BUG | T1 confirmed through the rendered explorer (select row → Delete → confirm dialog). With `deleteDiagram` rejecting, the ordered trace is `['canvas-reset(d1)', 'storage-delete']`, the "Delete failed" toast is shown, and the diagram is still in the provider listing — the user sees a blank canvas over work that was never deleted. The folder branch does no reset at all, so the two branches already disagree. known_issues: A4/FEX-08. |
| FEX-09 | The name-collision dialog offers "Replace it?" but `confirmMove` only calls `moveItem`, so confirming a replace produces two identically-named siblings | seed seam | none | `__explore__/A4/fileexplorer-fex-08-to-12` | BUG | T1 confirmed. After pressing Replace: one `moveItem(b,diagram,null)`, zero deletes, and two rows named `Report` in the destination. "Replace" performs exactly the operation the non-colliding path performs; `detectCollision` only decides whether to ask. known_issues: A4/FEX-09. |
| FEX-10 | `handleMove` `return`s (not `continue`s) on a same-parent reorder or a collision, so a multi-select drag silently abandons every remaining dragged item | seed seam | none | `__explore__/A4/fileexplorer-fex-08-to-12` | BUG | T1 confirmed, both branches. Dragging `[already-in-f1, mover]` into `f1` yields zero `moveItem` calls, `mover` still at the root and an empty notification queue; the collision variant parks one id in `collisionDialog` (which holds a single `dragId`, so Replace cannot resume the rest) and drops the non-colliding item with it. known_issues: A4/FEX-10. |
| FEX-11 | `handleRenameSubmit` decides folder-vs-diagram from possibly-stale `tree.folders` and resolves the place through `placeOfId`, which drops entries mid-refresh — a rename can hit the wrong entity type or the wrong provider | seed seam | `rename.spec.ts` (happy path, one place) | `__explore__/A4/fileexplorer-fex-08-to-12` | BUG | T1 confirmed (entity-type half; the provider half is FEX-12). With the folder dropped from the listing while the inline editor is open, the submit produces `renameDiagram(f1,Renamed)`, no `renameFolder`, and no notification — the node arborist passes to `onRename` carries the right `type` and is never consulted. known_issues: A4/FEX-11. |
| FEX-12 | `placeOfId` lets Drive win when the same id exists in both places (last `Map.set`), so session-node operations route to the Drive provider | seed seam | none | `__explore__/A4/fileexplorer-fex-08-to-12` | BUG | T1 confirmed, and the reachable half is the *other* direction: `placeOfId.get(id) ?? 'local'` sends an operation whose place is unknown to the session provider. With the Drive tree gated off mid-rename (`AUTHENTICATED`→`RECONNECTING` clears its rows), the session provider logs `renameDiagram(gd1,Renamed)` and Drive logs nothing. The duplicate-id half reproduces as predicted (Drive wins) but has no demonstrated trigger — both ids are provider-minted. known_issues: A4/FEX-12. |
| FEX-13 | `handleMoveToDrive` on the OPEN diagram moves the *persisted* blob, so an edit or autosave tick landing between the `saveAllDirty` flush and the source delete is lost | seed seam / ADR 0037 §5 | `driveTransfer.test` (create/verify/delete ordering only) | `__explore__/A4/movetodrive-fex-13-14` | BUG | T1 confirmed through the cross-place drag. With an edit landing during the Drive listing (the window is ≥2 listings + root resolution wide), the Drive copy holds the pre-move flush, the later edit is still only in memory, and the source is deleted and reopened from Drive — the edit has nowhere left to flush to. Nothing re-checks dirtiness before `source.deleteDiagram`. known_issues: A4/FEX-13. |
| FEX-14 | `driveRootMissing` keys on the synchronous `getCachedRootId()` while `driveTree.status` is async, so right after `DriveSetupGate` configures a root the tree can show the 'setup' state over real data | seed seam | none | `__explore__/A4/movetodrive-fex-13-14` | BUG | T1 confirmed, with the trigger corrected. `driveRootMissing` is a render-time read of a cache with **no subscription** — the explorer never listens for `axoview-drive-root-ready`. The seed's named path is covered by luck: `DriveSetupGate.handleConfirm` calls `refreshFileTree()` right after dispatching. The gate's *other* dispatch site (`hasConfiguredRoot()` true) does not, nor does a second tab or `ensureRoot()` on a write. Measured: after the root id appears and the event fires, the section still renders `placeState:setup` and refuses session→Drive drops; a bare refresh-token bump clears it. known_issues: A4/FEX-14. |
| FEX-15 | The `MigrateSessionDialog` auto-offer can fire zero or two times: `pendingOfferRef` is consumed by `tryAutoOffer` only when `getCachedRootId()` is already set, and the `axoview-drive-root-ready` event and the auth-status effect can interleave either way | seed seam / mode-transition race | none | `__explore__/A4/migrate-fex-15` | BUG | T1 confirmed for the zero half, FALSIFIED for the two half. Zero: one rejecting `listDiagrams` at the moment of the grant burns both refs — `tryAutoOffer` clears `pendingOfferRef` before its first await and `enumerateSession` swallows the error into `[]`, indistinguishable from "nothing to move" — so the dialog never opens again this session (`offeredThisGrantRef` resets only on UNAUTHENTICATED/SESSION_EXPIRED), and the session-only work it exists to rescue stays unsaved. Two: does not reproduce; the ref is cleared synchronously, so repeated `axoview-drive-root-ready` events return at the first line (probe FEX-15a, both healthy orderings characterized). known_issues: A4/FEX-15. |
| FEX-16 | A failed rename is rolled back in the tree only — `notifyDiagramRenamedFromTree` fires before the storage call and the `catch` undoes just `tree.refresh()`, so the open diagram keeps a name that exists nowhere in storage (and reloads its model with it) | anomaly capture (while probing FEX-11) | none | `__explore__/A4/fileexplorer-fex-08-to-12` | BUG | T1 confirmed. With `renameDiagram` rejecting: the row reverts to `Original`, the "Rename failed" toast is shown, and the only lifecycle notification is `notifyDiagramRenamedFromTree(d1,New name)` — never re-notified with the stored name. The notifier is not display-only: it `setCurrentDiagram`s and re-`load`s the model with `title: trimmed`, so the next autosave persists the name the user was told did not stick. known_issues: A4/FEX-16. |

## Product questions (SUSPECT verdicts)

*none yet*
