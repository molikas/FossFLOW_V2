# R4 — Renderer orchestration (culling, hybrid promotion, fit-to-view)

**Status:** OPEN · **Counted hypotheses:** 0 / 10 · **Bugs:** 0 · **Hypothesis ID prefix:** `RND-`

**Scope:** Renderer.tsx is the composition root: computes coarse tile bounds from the 4 viewport corners (screenToTile + 4-tile padding), throttles re-culling during continuous gestures (250/180/120ms windows) via a store subscriber that setStates coarseBounds, and derives visibleItems/visibleConnectors/visibleLabels with useStableList ref-reuse so memoised layers bail. It decides the DOM/GPU hybrid split: selected node, drag set, label-drag node, icon-resize targets -> DOM <Nodes> (skipNodes for the canvas); selected/lasso'd/degenerate/unroutable connectors -> DOM <Connectors>; dragged rects -> DOM <Rectangles>; inline-edited text box promoted above the interactions box. It also applies deferred fit-to-view synchronously in useLayoutEffect (getBoundingClientRect before first paint) and gates everything behind isWebGL2Supported. SceneLayer applies translate(scroll) scale(zoom) about the renderer centre via a store subscription (no React on pan) — the CSS mirror of the GL u_view transform.

**Code:**
- `packages/axoview-lib/src/components/Renderer/Renderer.tsx`
- `packages/axoview-lib/src/components/Renderer/WebGLUnsupportedScreen.tsx`
- `packages/axoview-lib/src/components/SceneLayer/SceneLayer.tsx`
- `packages/axoview-lib/src/components/SceneLayers/Nodes/Nodes.tsx`
- `packages/axoview-lib/src/components/SceneLayers/Connectors/Connectors.tsx`
- `packages/axoview-lib/src/components/SceneLayers/TextBoxes/TextBoxes.tsx`

**Dedupe baseline:** [coverage-baseline.md](../coverage-baseline.md) sections — E2E: *Canvas projection modes / rendering / viewport*, *Performance*; Unit: *Lib UI components*. Then grep the suites directly.

## Seed seams (2026-07-29 mapping — starting points, not a ceiling)

- Culling keys on item.tile / label.tile only — the ADR-0023 `offset` is ignored, so an item whose px residual pushes it visually into the viewport while its tile sits beyond the 4-tile padding is culled while on-screen (offsets are normally sub-tile, but nothing clamps them).
- Connector culling uses hitConnectors' path.rectangle AABB then maps back to raw connectors by id — a connector present in `connectors` but missing from `hitConnectors` (or with a stale path after an edit) silently disappears from both DOM and GPU sets.
- coarseBounds is recomputed ONLY inside the store subscriber (scroll/zoom/rendererSize writes) — on a canvasMode switch the effect re-subscribes with the new screenToTile but never recomputes bounds itself; correctness relies on the mode-switch action also writing scroll. If it doesn't (or writes an identical ref), culling runs with the OLD projection's tile bounds.
- The gesture throttle (PAN_GESTURE_GAP_MS/PAN_BOUNDS_THROTTLE_MS/PAN_SETTLE_MS) means mid-fling the visible set lags up to ~180ms — pop-in beyond the 4-tile padding at high fling speed on large monitors; the trailing settle flush depends on the `pending` closure surviving to the timeout.
- Hybrid promotion is a two-sided contract: Renderer must EXCLUDE promoted ids from the canvas set AND include them in the DOM overlay, while NodesCanvas flushes skipNodes synchronously (useLayoutEffect drawNow) — any timing gap draws a node twice (ghosting) or zero times (flash). Same duality for connectors: ConnectorsCanvas independently re-derives the skip (unroutable / tiles.length<2 from sceneStore) while Renderer derives connectorHybridIds from hitConnectors — two sources that must agree.
- Promotion keys are comma-joined id strings (draggingKey, resizingNodesKey, selectedConnectorKey) chosen for primitive-selector stability — an id containing a comma, or a mode that moves items without a matching promotion path (new transform tool), silently falls back to per-frame canvas staleness.
- Rectangles and text boxes are NOT viewport-culled at all (whole arrays passed down) — fine today, but a scale assumption that differs from nodes/connectors/labels.
- pendingFitToView useLayoutEffect: falls back from getBoundingClientRect to store rendererSize; if both are 0 it returns and waits for a rendererSize write — a container that never resizes (display:none mount) leaves pendingFitToView stuck true.
- SceneLayer's CSS transform and the GL u_view are two implementations of the same mapping (translate+scale about center vs zoom*tile+W/2+scroll) — sub-pixel rounding differences between DOM-promoted and GPU-bulk copies of the same element are visible as a 1px jump on selection/deselection.
- (mapper note) The long Renderer comments are load-bearing history (why culling is throttled, why each promotion exists). useStableList's element-wise ref reuse is what keeps the connector layers from re-rendering per pan frame — breaking referential stability upstream (useScene) cascades into per-frame GPU rebuilds.

## Known coverage gaps (from the baseline inventory)

- (Canvas projection modes / rendering / viewport) Mouse-wheel zoom anchoring at the cursor position
- (Canvas projection modes / rendering / viewport) Zoom clamp bounds via buttons/wheel (only touch pinch asserts [0.1,1])
- (Canvas projection modes / rendering / viewport) Fit-to-view exact framing (all items visible) — only 'value changed' asserted
- (Canvas projection modes / rendering / viewport) Projection mode persistence across reload
- (Canvas projection modes / rendering / viewport) 2D label stalk rendering parity
- (Performance) Perf thresholds that FAIL the suite on regression
- (Performance) Zoom/pan frame-rate measurement
- (Performance) Large-diagram load time
- (Lib UI components) zero-test lib components: Renderer/Renderer.tsx, SceneLayer, Grid (formula tested only via perf grep-free math), AnnotationLayer/AnnotationLayer.tsx, AnnotationPalette, CanvasContextMenu, ConnectorAnchorOverlay, ConnectorSettings, LayersPanel, ZoomControls, SettingsDialog, ZoomSettings, HotkeySettings, IconPackSettings, HelpDialog, LeftDock, BottomDock, RightSidebar.tsx, DragAndDrop, ModeHint, PreviewLayerSwitcher, PreviewCanvasModeToggle, TopBarStyleControls, ElementLinkCard, CanvasCompositorOverlay, DOMErrorBoundary, Cursor, Lasso/FreehandLasso overlays, Svg, UiElement
- (Lib UI components) ItemControls panels (NodeSettings/ConnectorSettings/RectangleSettings/TextBoxSettings etc.) have only the two-section parity test — no per-control behavior tests
- (Lib UI components) ExportImageDialog behavior beyond waitForIconsDrawn is covered only by static source-grep tests (initialLoad/memo)

## Hypotheses

| ID | Hypothesis | Source | Nearest existing tests | Probe | Verdict | Evidence |
|----|-----------|--------|------------------------|-------|---------|----------|
| RND-01 | `getFitToViewParams` clamps zoom to `[0, MAX_ZOOM]` — no `MIN_ZOOM` floor — so fit-to-view on a large diagram sets a zoom every other path refuses to produce (buttons/wheel clamp via `incrementZoom`/`decrementZoom`, pinch clamps to `[0.1,1]`), and the first pinch afterwards snaps the view back up | boundary | `viewport.spec.ts` (fit asserts only "changed"), `touch-pinch-zoom.spec.ts` ([0.1,1] for pinch only), `renderer.test.ts` (increment/decrement clamps) | src/__explore__/R4/fit-rnd-01-09-10.explore.test.ts | **BUG** | Expected zoom >= MIN_ZOOM (0.1); actual 0.0829 for a 100x100-tile diagram on 1280x720. Threshold is ~85 tiles across. Only the UPPER clamp (MAX_ZOOM) is enforced. known_issues: *Fit-to-view can zoom below the MIN_ZOOM floor every other zoom path enforces* |
| RND-02 | `ConnectorLabels`/`ConnectorLabel` never consult `useLayerContext`, unlike every sibling scene layer, so hiding a layer removes the connector body (DOM + GPU both gate) but leaves its label chips painted on the canvas | bug-class recurrence (thread A) + sibling drift | `layers.spec.ts` (DOM/store only), R3 `gpu-08-layer-visibility.explore.spec.ts` (bulk canvases only), `connector-dot-and-label-placement.spec.ts` | tests-exploratory/R4-renderer/rnd-02-07-overlay-gates.explore.spec.ts | **BUG** | Hiding the layer takes the connector body to 0 painted pixels on the bulk canvas; the label chip count is UNCHANGED (1 -> 1). ConnectorLabels/ConnectorLabel are the only scene layers that never import useLayerContext. known_issues: *Hiding a layer leaves its connectors label chips on the canvas* |
| RND-03 | `coarseBounds` is recomputed only inside the store subscriber; a projection switch re-subscribes with the new `screenToTile` but never re-culls, so culling runs with the OLD projection's tile bounds until the next scroll/zoom write | seed seam | `canvas-modes.spec.ts`, `canvas-mode-zoom-preserve.spec.ts` (both store-level), R3/GPU-06 (rebuild, not culling) | tests-exploratory/R4-renderer/rnd-03-04-06-12-culling.explore.spec.ts | FALSIFIED | After a ToolMenu iso->2D switch the bulk data-draw-count equals the count after a forced isolated re-cull, so the toggle's setScroll lands AFTER the effect re-subscribes with the new screenToTile and the bounds are current. The seam is real but closed by React's tree-order effect flush (Renderer commits before UiOverlay) - noted as fragile in the area seams |
| RND-04 | The promotion keys are comma-joined id strings, so an item id containing a comma (ids are unvalidated — CLIP-01/14) is split into fragments that match nothing: the dragged node is neither promoted to the DOM overlay nor skipped on the canvas, so it has no `--ff-drag` preview and appears frozen until release | boundary + seed seam | `multi-select-drag.spec.ts`, `css-preview-mid-drag.spec.ts` (well-formed ids only) | tests-exploratory/R4-renderer/rnd-03-04-06-12-culling.explore.spec.ts | **BUG** | CONTROL: a normal id gives 1 [data-drag-id] overlay and drawCount 0. With the id rewritten to "imported,node" the drag set is exactly [ITEM:imported,node] yet the overlay count is 0 and drawCount is 1 - the node stays on the bulk with no --ff-drag preview and does not move until release. Reachable via any import/load (ids are unvalidated - CLIP-01/14) |
| RND-05 | Below `LABEL_LOD_ZOOM` (0.25) `NodesCanvas` draws no name chips, but a node lifted into the DOM overlay (selected / dragging / label-drag / icon-resize) renders its label through `Label.tsx`, which has no LOD gate — so at zoom 0.2 exactly one node shows a name | parity / sibling drift | R3/GPU-05 (hit layer vs draw threshold — different pair), `readable-labels.spec.ts` | tests-exploratory/R4-renderer/rnd-05-13-14-15-promotion.explore.spec.ts | **BUG** | At zoom 0.2 the bulk reports data-labels-drawn=0 (readableLabels off) while the selected node's DOM chip is still in the canvas subtree - exactly one named node on an otherwise nameless diagram. Same entry as RND-13/15 is NOT appropriate; filed separately. known_issues: *Below the label LOD zoom the selected node still shows its name* |
| RND-06 | Fit-to-view measures the renderer container, which spans the whole window with the docks overlaying it, so after a fit the outermost content sits behind the left dock / right sidebar instead of inside the visible canvas | cross-feature | `viewport.spec.ts` (no framing assertion — listed as a coverage gap) | PROPOSED | | |
| RND-07 | The full-viewport `canvas-interactions` box is mounted ABOVE the resting `TextBoxes` layer, so `TextBox.onRestingClick` (ADR 0034's `#diagram:` navigation, and any plain `<a>`) can never receive a click — links inside a text box are dead at rest in every editor mode | seed seam (mount order) + ADR-contract | `textbox-ops.spec.ts`, `textbox-text-edit-move.spec.ts` (no link cases) | tests-exploratory/R4-renderer/rnd-02-07-overlay-gates.explore.spec.ts | **BUG** | document.elementFromPoint at the anchor centre returns data-axoview-id="canvas-interactions", not the <a>; neither a plain click nor the documented Ctrl+click fires axoview-navigate-to-diagram (0 events). The interactions box is mounted AFTER the resting TextBoxes SceneLayer. known_issues: *A link inside a text box cannot be clicked* |
| RND-08 | Viewport culling keys on `item.tile` only and ignores the name chip's extent (`labelHeight` up to +280 canvas px), so a node just outside the 4-tile padding is culled together with a chip that would still be on screen | boundary | `canvas-node-render.spec.ts`, `node-label-decouple.spec.ts` | src/__explore__/R4/fit-rnd-01-09-10.explore.test.ts | FALSIFIED | Swept every tile in a 12-tile band beyond the padded bounds on a 1600x900 viewport: no culled tile can put its chip back on screen. Four tiles of padding move a point ~2x LABEL_OFFSET_MAX up the screen, so the margin is not accidental |
| RND-09 | `getProjectBounds` enumerates `item.tile` only, so a node whose name chip is dragged far above it (up to +280 px) has its chip clipped by fit-to-view — a fourth omission in the same function as PROJ-01/02/04 | bug-class recurrence | R1 `bounds-proj-01-02-03-04.explore.test.ts` (text boxes, labels, +1 extent) | src/__explore__/R4/fit-rnd-01-09-10.explore.test.ts | **BUG** | getProjectBounds is byte-identical with and without a labelHeight at the +280 clamp; after a height-limited fit the node sits inside the viewport and its chip anchor lands at NEGATIVE screen y. Same function, fourth omission after PROJ-01/02/04 - filed as an addendum to that entry |
| RND-10 | Fit-to-view is not idempotent: a second fit click moves the viewport again (the params are recomputed from a viewport the first fit changed) | metamorphic | `viewport.spec.ts` (single fit only) | src/__explore__/R4/fit-rnd-01-09-10.explore.test.ts | FALSIFIED | getFitToViewParams is a pure function of (view, viewport, projection) and reads no current scroll/zoom, so a second fit is a no-op |
| RND-11 | The gesture throttle's trailing settle-flush is armed on a `setTimeout` owned by the subscriber effect; a projection switch (the only dep change) tears the effect down within `PAN_SETTLE_MS` of the last motion and the cleanup clears the timer without committing `pending`, stranding the cull one throttle window stale | seed seam + interleaving | *none* | PROPOSED | | |
| RND-12 | A connector with no scene path at all is promoted by neither side of the hybrid — `connectorHybridIds` only fires on `unroutable` or `path.tiles.length < 2`, and `ConnectorsCanvas` skips a missing path — so it renders nowhere while staying hit-testable | seed seam (two-sided contract) | `connector-parity.spec.ts`, `connector-deep.spec.ts` | tests-exploratory/R4-renderer/rnd-03-04-06-12-culling.explore.spec.ts | FALSIFIED | Not reachable: every view connector kept a scene entry across a page add, a changeView back, and undo/redo. The consequence is real (removing the entry paints 0 pixels and promotes nothing) but nothing in the product produces it - syncConnector always writes either a path or an unroutable stub, and unroutable IS promoted. A pure-math-sweep-style inert finding |
| RND-13 | Hybrid promotion changes paint order: the DOM overlays are mounted above their bulk canvases, so selecting (or dragging) a node lifts it in front of every canvas node/connector/label regardless of layer order or `zIndex`, and drops it back on deselect — the same for a dragged rectangle vs the rectangle bulk | seed seam (mount order) + parity | `z-order.spec.ts`, `rectangle-overlap-zorder.spec.ts`, R3/GPU-13 (cross-canvas order, not DOM-vs-canvas) | tests-exploratory/R4-renderer/rnd-05-13-14-15-promotion.explore.spec.ts | **BUG** | elementsFromPoint (rig-controlled against the known rects<nodes<labels mount order) puts the promoted DOM copy ABOVE axoview-nodes-canvas, so a selected node paints in front of every canvas node whatever its layer order or zIndex - and drops back on deselect. known_issues: *Selecting or dragging an element restacks it above the rest of the diagram* |
| RND-14 | `hybridNodes` is filtered through `visibleItems`, so panning the selected node outside the padded bounds silently drops its DOM overlay while `itemControls` still names it — F2 inline-rename and the label-drag handle become inert with no feedback | seed seam (culling × promotion) | `rename.spec.ts`, `label-drag.spec.ts` (never pan away first) | tests-exploratory/R4-renderer/rnd-05-13-14-15-promotion.explore.spec.ts | SUSPECT | Confirmed: after panning the node's tile outside the padded bounds the bulk draws 0, the DOM overlay is unmounted, itemControls still names the item, and F2 mounts no editor (0 contenteditable). Nothing specifies what a rename shortcut should do for an off-screen selection - recorded as a product question, not filed |
| RND-15 | The DOM `<Nodes>` overlay is mounted ABOVE `LabelsCanvas`, so selecting a node flips its stacking against a floating Label that at rest paints above it (ADR 0031's documented cross-layer order) | seed seam (mount order) | `label-entity.spec.ts`, R3/GPU-13 | tests-exploratory/R4-renderer/rnd-05-13-14-15-promotion.explore.spec.ts | **BUG** | Same measurement: the promoted DOM copy is above axoview-labels-canvas too, inverting ADR 0031's "a Label paints ABOVE nodes" for the duration of a selection. Same known_issues entry as RND-13 |

## Product questions (SUSPECT verdicts)

*none yet*
