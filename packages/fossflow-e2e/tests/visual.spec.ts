/**
 * visual.spec.ts — Screenshot regression baselines (Phase 6)
 *
 * 5 tests, Visual project only (Chromium, fixed 1280×800 viewport).
 * Run with: npm run test:visual
 * Update baselines: npx playwright test --project=visual --update-snapshots
 *
 * Status: STUB — implementation in Phase 6
 */
import { test } from '@playwright/test';

test.fixme('V-1: empty canvas baseline', async () => {});
test.fixme('V-2: canvas with one node placed', async () => {});
test.fixme('V-3: canvas with two nodes and connector', async () => {});
test.fixme('V-4: node item controls panel open', async () => {});
test.fixme('V-5: lasso selection active', async () => {});
