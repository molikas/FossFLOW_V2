/**
 * lasso.spec.ts — Lasso selection tests (Phase 4)
 *
 * 5 tests, Chromium + Firefox.
 * Covers: lasso draw, selection, toolbar propagation regression guard.
 *
 * Status: STUB — implementation in Phase 4
 */
import { test } from '@playwright/test';

test.fixme('L-1: drag empty canvas → lasso rectangle visible', async () => {});
test.fixme('L-2: lasso captures items in bounds → selection non-empty', async () => {});
test.fixme('L-3: toolbar click in Lasso mode → no context menu', async () => {});
test.fixme('L-4: right-click in Lasso → clears selection', async () => {});
test.fixme('L-5: Escape → exits Lasso, returns to Cursor', async () => {});
