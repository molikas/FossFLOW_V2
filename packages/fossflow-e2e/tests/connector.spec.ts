/**
 * connector.spec.ts — Connector draw and undo tests (Phase 4)
 *
 * 4 tests, Chromium + Firefox.
 * Replaces: e2e-tests/tests/test_connector_undo.py
 *
 * Status: STUB — implementation in Phase 4
 */
import { test } from '@playwright/test';

test.fixme('C-1: place two nodes, draw connector → connector appears', async () => {});
test.fixme('C-2: undo connector → removed', async () => {});
test.fixme('C-3: redo connector → restored', async () => {});
test.fixme('C-4: select connector + Delete key → removed', async () => {});
